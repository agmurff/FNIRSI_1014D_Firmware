# Code & AI-instruction review — 2026-08-21

Multi-agent review (workflow `wf_e2f2eb8a-11f`): 10 reviewers (5 instruction-doc groups, 5 code dimensions), one adversarial verifier per findings batch, coverage critic. 74 raw findings.

## Executive summary

**Verdict:** the port is in good shape — no showstoppers found in the graft itself — but the review surfaced a handful of real, previously unknown code bugs on the active 1014D build, one systematic seam class (pecostm32 `int16` fields kept as Atlan4 `uint16`), and heavy staleness in the instruction docs, whose newest "Known issues" state is six weeks behind the code.

**Numbers:** 74 raw findings; 71 survived adversarial verification, and the coverage critic overturned one of the 3 refutations (the F25-guard finding at `fpga_control.c:631` is in fact correct), so the working set is **72 confirmed**. Both variants were build-tested after the review: 1013D and 1014D compile clean, 1014D rebuilt last (`bootloader_1014d_base.bin` at 0x8000).

**Top code findings (new, on the current 1014D build):**

1. **`uint16` wrap class** (imported pecostm32 code assumes `int16` fields; this tree keeps Atlan4 `uint16`): `traceposition` wraps on rotary-down at the bottom with fast move speed — trace jumps bottom→top (`sm_1014d.c:1571`, high); time-cursor positions wrap the same way (`sm_1014d.c:292/309`, medium). Fix pattern is one signed local before the clamp.
2. **Roll-mode half of F25 still dead**: `fpga_set_long_timebase()` never sends `0x28/0x01`, and the stock capture wants it (plus `0x0D`) per read cycle — long timebases still run the FPGA in fast-mode select (`fpga_control.c:599`, high). Both FPGA_NOTES and PORT_AUDIT misattribute the commented write to `fpga_set_time_base`, which contains no 0x28 at all.
3. **Two more F16-class missing pushes**: v/div change never re-pushes the trigger level although the FPGA level is a function of volt/div (`sm_1014d.c:1553`); channel-enable toggle leaves the FPGA trigger source on a disabled channel — `fpga_swap_trigger_channel()` has no 1014D caller (`sm_1014d.c:1511`). Both freeze normal/single-mode acquisition until an unrelated key pokes the FPGA.
4. **Buffer seams**: `MAX_SAMPLE_BUFFER_SIZE` 16382 vs 16380-byte `uint32[4095]` arrays — 2-byte overruns in the math/reference paths (`variables.h:529`); USB CDC debug command `f<number>` sets `totalsamples` unbounded → huge acquisition-buffer overrun from the host side (`usb_interface.c:1542`).
5. **Wave-view seam**: `sm_set_time_base` dropped `!waveviewmode` from the Atlan4 condition it mirrors — timebase turns in wave view corrupt the viewed capture's sample rate and re-arm (`sm_1014d.c:1480`).
6. **F25 restore is `#if PORT_1014D`-guarded** — the 1013D variant keeps the sawtooth regression its own upstream never had (`fpga_control.c:631`).
7. Smaller: 1014D `get_fattime` returns 0 (invalid FAT timestamps on every save), unguarded RTC repaint block reachable via restored config, X-Y mode negative-index reads, capture-geometry numbers off 2× (per-ADC vs interleaved) in FPGA_NOTES/ROADMAP/analyzer, acqprobe reads CH1 unconditionally while the analyzer divides by enabled channels.

**Instruction-file rot (the "AI instruction review" half):** CLAUDE.md's Known-issues section is frozen at 2026-07-12 — F26–F35, both open bugs, and the entire capture-geometry thread are missing (high). FPGA_NOTES' command table still calls 0x28 dead, contradicting its own SOLVED section; its claim that the 1014D build never sends generator commands 0x50–0x52 is false (they go out every boot from `main()`, which also weakens the ROADMAP-14 hang hypothesis). Both FPGA_NOTES and the bring-up memory claimed `fpga/` is tracked — it is entirely untracked, a real data-loss hazard for the only copy of the built bitstream. CLAUDE.md tells the reader to rebuild `mksunxi`/`flashfilepacker` "from the checked-in .c sources" — no such sources exist in the tree (critic finding). AGENTS.md's display-geometry rules predate F17's 705-wide blit. ROADMAP still queues the solved sawtooth A/B (1b) and calls the built FPGA retarget "real remaining work" (16).

**Critic's next-pass list (what this review did NOT cover):** `test.c` (the actual roll-mode implementation, ~1400 lines, essentially unread — most relevant file to the open roll defect); USB mass-storage runs raw SD writes in IRQ context against the same card FatFs uses (unaudited); the CPU-infrastructure layer (D-cache enable is inert without an MMU on ARM926 — the data side runs uncached, a silent performance loss; hard-wired stacks with no guard); three tracked loader source trees at repo root nobody assessed; FatFs/SD/display_lib only byte-diffed vs Atlan4, not audited.

## Confirmed — high

*(from doc-claude-repo)*

### Known-issues section frozen at 2026-07-12 — omits F26–F35, two open bugs, and the capture-geometry thread

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/CLAUDE.md:191`
- **Severity:** high

The 'Known issues' section's newest content is the F25 sawtooth solve (2026-07-12) and its closing line presents 'Remaining: chrome fragments… null menu items… backlog in ROADMAP.md' (lines 234–236) as the complete open-issue state. Reality since then: F26 (anti-glitch double-check restored), F27 (heisenbug verdict — F24/F26 non-causal, acquisition audited code-identical to pecostm32, pivot-to-pecostm32-base taken off the table), F28 honest-precision/hi-res-Vavg (implemented, hw-verify pending), F29 (−22 V on disabled channel — display guard bench-verified 2026-07-17, but the stale-value SOURCE stays open under F30), F30 (Base-cal non-repeatable +7 mV residual on a shorted input — explicitly 'the next real bug to fix', open, gated on a pecostm32 A/B), F31 (AC/DC coupling toggle NEVER reached the FPGA — a real inherited acquisition bug, fixed in code, hw-verify pending), F32 (time readouts 16× low, Q16-vs-Q20 seam, fixed, pending), F33 (overclock-blind frequency, fixed, pending), F34 (spacing, bench-verified), F35 (one-shot offset jump, unreproduced), plus the whole acqprobe/capture-geometry thread (ring mod-4096, ~2500-sample post-trigger fill CLOSED 2026-07-21). A fresh session taking the section at face value would believe no acquisition-affecting bugs are open and could re-diagnose the −22 V reading, the above-0V cal residual, or the inert coupling toggle from scratch.

**Evidence:** git log -- CLAUDE.md: last commits touching it are 3de9c3d (2026-07-19, diff adds only the bench/tools section) and 100f9ff (2026-07-17, adds only the donwulff-notes bullet); the Known-issues text was last changed by b793048 (2026-07-12); ceeae17 (2026-07-21, 'post-trigger fill CLOSED') touches no CLAUDE.md. PORT_AUDIT.md:663–694 carries F26–F35: line 679 F28 'IMPLEMENTED… hardware-verify pending'; line 682 F29 '…stays open under the F30 pecostm32-A/B umbrella'; line 684 F30 'not yet root-caused… the next real bug to fix once the A/B is in'; line 686 F31 'coupling toggle never reached the FPGA… FIXED in code, hw-verify pending'; line 677 F27 'the graft is now at pecostm32 parity… pivot… off the table'. FPGA_NOTES.md:91 'Stock-FPGA capture geometry (code + RE 2026-07-17; bench-measured 2026-07-19)'. CLAUDE.md:234–236 'Remaining: "chrome" fragments… backlog in ROADMAP.md'.

**Suggested action:** Refresh the Known-issues paragraph to the F35 state: name the open bugs (F30, F29's stale-value source), the implemented-pending items (F28, F31–F33), the F27 parity/pivot verdict, and the closed capture-geometry results — or replace the blow-by-blow narrative with a dated one-paragraph status plus pointers to PORT_AUDIT F26–F35 and FPGA_NOTES §capture geometry.

**Verifier:** Independently reproduced every evidence leg. git diff b793048..HEAD -- CLAUDE.md shows exactly one hunk (@@ -60,12 +60,28 @@ — the donwulff-notes bullet from 100f9ff plus the bench/tools section from 3de9c3d); the Known-issues section (lines 191-236) is byte-untouched since b793048 (2026-07-12), whose diff added the F25 SOLVED sentence. ceeae17 (2026-07-21, 'post-trigger fill CLOSED') touches only FPGA_NOTES.md, ROADMAP.md, tools/acqprobe_analyze.py — no CLAUDE.md. PORT_AUDIT.md carries F26 (line 663), F27 with 'pivot… off the table' (677), F28 'IMPLEMENTED… hardware-verify pending' (679), F29 display guard bench-verified with source 'open under the F30 pecostm32-A/B umbrella' (682), F30 'not yet root-caused… the next real bug to fix' (684), F31 'coupling toggle never reached the FPGA… FIXED in code, hw-verify pending' (686), F32/F33 fixed-pending (688/690), F34 bench-verified (692), F35 unreproduced (694). FPGA_NOTES.md:91 is the capture-geometry section heading. CLAUDE.md's closing 'Remaining: chrome fragments… backlog in ROADMAP.md' (234-236) presents a complete open-issue state that omits all of this — the wrong-action scenario (re-diagnosing the -22 V reading, the +7 mV shorted-cal residual, or the coupling toggle from a doc that says they don't exist) is real, so high severity is justified. Mitigating note only: the Key-documents section does say to read PORT_AUDIT.md before touching acquisition.

*(from doc-memory)*

### Bringup memory claims fpga/AL3_1014D/ is '(tracked)' — it is entirely untracked

- **Where:** `/home/jsantala/.claude/projects/-home-jsantala-src-FNIRSI-1014D-Firmware/memory/atlan4-1014d-bringup.md:351`
- **Severity:** high

The 2026-07-18 entry states the FPGA retarget project is committed: 'Full retarget project at repo `fpga/AL3_1014D/` (tracked)'. Reality: `git ls-files fpga/` returns 0 files and `git status` shows `?? fpga/` — the whole directory (patched zaklad.v, zaklad_1014d.adc/.sdc, build.sh/build.tcl, and the only copy of the built out/FPGA_1014D.bin, 282,898 B) exists only as untracked working-tree files. The same memory contradicts itself 32 lines later ('`fpga/` itself stays uncommitted on that thread', line 383), so which claim a reader lands on is luck. Concrete failure: an agent trusting '(tracked)' could run `git clean -xfd`, skip a backup, or assume git history protects the project — destroying the sole copy of the retargeted bitstream. Stakes are elevated because the TD toolchain license (verified in ~/tools/anlogic-td/Anlogic_20251116.lic) expires 2026-08-31 — 11 days from today — after which the bitstream cannot be rebuilt.

**Evidence:** atlan4-1014d-bringup.md:351 'retarget project at repo **`fpga/AL3_1014D/`** (tracked)' vs :383 '`fpga/` itself stays uncommitted'. `git ls-files fpga/ | wc -l` = 0; `git status -sb` = '?? fpga/'. ls confirms fpga/AL3_1014D/{build.sh,build.tcl,zaklad.v,zaklad_1014d.adc,out/FPGA_1014D.bin (282898 B)} exist on disk only. License file header 'Generated on Sat Aug 30 19:19:11 2025', grep hit '2026-08-31'.

**Suggested action:** Fix the memory to say fpga/ is UNTRACKED (delete '(tracked)'), and surface the real state + license deadline in the MEMORY.md hook; separately recommend the user commit fpga/ (or back it up) before the 2026-08-31 license expiry.

**Verifier:** Independently reproduced every element. atlan4-1014d-bringup.md line 350 reads 'retarget project at repo `fpga/AL3_1014D/` (tracked)' while lines 382-383 read '`fpga/` itself stays uncommitted on that thread' — a direct self-contradiction. `git ls-files fpga/ | wc -l` = 0; `git status -sb` shows '?? fpga/' (untracked, not ignored — .gitignore excludes only /fpga/AL3_1014D/build/, and its comment 'curated artifacts live in fpga/AL3_1014D/out/ instead' shows out/ was meant to be committed but never was). No nested .git anywhere under fpga/, so the working tree really is the sole git-side copy of out/FPGA_1014D.bin (282,898 B, verified on disk). License evidence verified: ~/tools/anlogic-td/Anlogic_20251116.lic header 'Generated on Sat Aug 30 19:19:11 2025', grep hit '2026-08-31' — 10 days from today (the finding's '11 days' was correct on its 2026-08-20 run date). The concrete failure scenario is real: even plain `git clean -fd` (no -x needed, since fpga/ is untracked rather than ignored) would delete the only copy, and after 2026-08-31 the bitstream cannot be rebuilt server-side. High severity is justified.

*(from code-fpga)*

### Roll-mode 0x28/0x01 mode-select still missing (F25's other half), and FPGA_NOTES points the fix at the wrong function

- **Where:** `fnirsi_1013d_scope/fpga_control.c:599`
- **Severity:** high

F25 restored the 0x28/0x00 mode-select for short timebases in fpga_do_conversion, but the roll-mode half is still Atlan4-commented-out: fpga_set_long_timebase() only sends 0x0F/0x01 (trigger disable) while the 0x28/0x01 write remains dead code at lines 598-600 ('0X28 not support in FPGA???'). The stock-FPGA reference shows roll mode needs 0x28=0x01, and needs it (plus a 0x0D rate write) before EVERY 0x24/0x26 read cycle, not just once at mode entry — the current long-mode read loop (scope_get_long_timebase_data -> fpga_average_trace_data) sends neither, so on stock fw_FPGA==1 the FPGA stays in the fast-mode select (0x00, last written by fpga_do_conversion) for all long timebases. Bench already calls roll rendering 'glitchy on this unit' (sm_1014d.c:1449-1450 comment). Additionally FPGA_NOTES.md documents this follow-up but names the wrong function twice ('restore roll-mode 0x28/0x01 in fpga_set_time_base') — fpga_set_time_base is the 0x0E function sent on every short-timebase change; adding 0x28/0x01 there would actively break short mode.

**Evidence:** fpga_control.c:595-600: 'fpga_write_cmd(0x0F); fpga_write_byte(0x01); ... //fpga_write_cmd(0x28); //0X28 not support in FPGA??? //fpga_write_byte(0x01);'. pecostm32-RE/'FPGA explained/FPGA explained.txt':76 '0x28 ... 0x00 for all time base settings below 100mS. 0x01 for all from 100mS and up' and :202 '0x28 is written 0x01 after or before every read sequence. 0x0D is also written'; '100mS_50S-div_signal_data_read_sequence.txt':64-82 shows the repeating cycle 0x0D(0x07D0) -> 0x28/0x01 -> 0x24 read 10 -> 0x26 read 10. FPGA_NOTES.md:193 and :201 both say the commented write is 'in fpga_set_time_base'; it is actually in fpga_set_long_timebase (fpga_control.c:576-601). fpga_do_conversion writes 0x28/0x00 (fpga_control.c:639-640) but is only called on the short-mode path (fnirsi_1013d_scope.c:391-414 routes long_mode to scope_get_long_timebase_data in test.c:824, which never writes 0x28/0x0D).

**Suggested action:** Fix FPGA_NOTES.md:193/201 to name fpga_set_long_timebase. When implementing the follow-up, mirror the stock capture: send 0x0D + 0x28/0x01 per roll-mode read cycle (e.g. in scope_get_long_timebase_data or fpga_average_trace_data), not only once in fpga_set_long_timebase; bench-verify against the 100mS_50S capture.

**Verifier:** Every cited fact reproduced. fpga_control.c:595-600: fpga_set_long_timebase sends only 0x0F/0x01 with the 0x28/0x01 write dead-commented ('0X28 not support in FPGA???'). pecostm32-RE 'FPGA explained.txt':76 and :202 say exactly what is quoted, and '100mS_50S-div_signal_data_read_sequence.txt':61-82 shows the repeating stock cycle 0x0D(0x07D0) -> 0x28/0x01 -> 0x24 x10 -> 0x26 x10. The current long-mode path never sends either per cycle: fnirsi_1013d_scope.c:391-393 routes long_mode to scope_get_long_timebase_data (test.c:824), whose read loop (test.c:950-957) calls only fpga_average_trace_data, which writes just settings->averagecommand (fpga_control.c:1440); 0x0D is pushed once at mode entry (sm_1014d.c:1486) but 0x28/0x01 never, so the stock FPGA's last mode-select remains the 0x28/0x00 from fpga_do_conversion (fpga_control.c:639-640). The doc error is real: FPGA_NOTES.md:193 and :201-202 both name fpga_set_time_base, but that is the 0x0E short-timebase function (fpga_control.c:552-572) called on every short-timebase change (sm_1014d.c:1475) and in the fw_FPGA>1 reset window (fpga_control.c:688) — adding 0x28/0x01 there would select roll mode during short timebases. The dead code actually lives in fpga_set_long_timebase (fpga_control.c:576-601). PORT_AUDIT F25's own follow-up note also flags the roll-mode restore as pending (and repeats the same wrong function name). The 'glitchy on this unit' bench comment is at sm_1014d.c:1449-1450 as cited. Whether the FPGA strictly requires 0x28/0x01 per cycle is a hardware question, but the divergence from the silicon-tied reference protocol is proven and the repo's own doctrine (match reference, never invent) makes the conclusion follow.

*(from code-gotchas)*

### uint16 traceposition wraps on rotary-down: channel trace jumps bottom-to-top

- **Where:** `fnirsi_1013d_scope/sm_1014d.c:1571`
- **Severity:** high

sm_set_channel_position() does `settings->traceposition += speedvalue;` then clamps with `< VERTICAL_POINTER_POS_MIN` / `> VERTICAL_POINTER_POS_MAX`. The code was imported from pecostm32's 1014D statemachine.c where traceposition is int16 (reference FNIRSI_1014D_Firmware/fnirsi_1014d_scope/variables.h:358), but this tree keeps Atlan4's `uint16 traceposition` (variables.h:605). With the position clamped at the bottom (VERTICAL_POINTER_POS_MIN=1, variables.h:176) and fast move speed (speedvalue=-10, set at sm_1014d.c:122 from movespeed, MOVE_SPEED_FAST=10 in statemachine.h:102), 1+(-10) stores as 65527; the `<1` test is false, the `>399` test (VERTICAL_POINTER_POS_MAX=TRACE_MAX_HEIGHT=399, variables.h:149/177) is true, and the position is clamped to 399. Concrete failure: hold the channel-position rotary down at fast speed — when the trace reaches the bottom, the next detent slams it to the top of the screen instead of stopping. Repeats cyclically. Port-introduced seam (Atlan4's touch UI only ever assigned absolute on-screen values to this field, so uint16 was safe there).

**Evidence:** sm_1014d.c:1571 `settings->traceposition += speedvalue;`, guards at 1574 `if(settings->traceposition < VERTICAL_POINTER_POS_MIN)` and 1579 `else if(... > VERTICAL_POINTER_POS_MAX)`; variables.h:605 `uint16 traceposition;`; pecostm32 reference variables.h:358 `int16 traceposition;` with byte-identical handler logic (reference statemachine.c:1463-1480); variables.h:176-177 POS_MIN=1, POS_MAX=TRACE_MAX_HEIGHT; variables.h:146-149 TRACE_MAX_HEIGHT=399; speedvalue int8 set negative at sm_1014d.c:121-122.

**Suggested action:** Compute in a signed local (int32 pos = settings->traceposition + speedvalue; clamp; store back), or change tagChannelSettings.traceposition to int16 as in pecostm32 after auditing all consumers (display code like scope_preset_values 442-traceposition, config save/restore).

**Verifier:** Independently reproduced every link: sm_1014d.c:1571 '+= speedvalue' with wrap-then-clamp guards at 1574/1579; traceposition is uint16 in this tree (variables.h:605) but int16 in the pecostm32 source of the handler (reference variables.h:358; handler at reference statemachine.c:1463 with the same +=/clamp logic); VERTICAL_POINTER_POS_MIN=1/MAX=TRACE_MAX_HEIGHT=399 (variables.h:176-177, 146/149); speedvalue is int8 (variables.h:1518) set to -10 at fast speed (sm_1014d.c:121-122, MOVE_SPEED_FAST=10 statemachine.h:102). Holding the rotary down at fast speed always lands the position in 1..10, and the next detent computes 1..9 + (-10) which wraps to >=65526, fails the <1 test, passes >399, and clamps to 399 — the opposite end. Handler is wired to the CH1/CH2 position rotaries (sm_1014d.c:907/912/1022/1027). Atlan4's own uint16 (Atlan4 variables.h:450) was safe because its touch UI assigns absolute values (e.g. statemachine.c:6124), so this is a genuine port seam.

*(from doc-fpga-boot)*

### Generator commands ARE sent by the 1014D build every boot

- **Where:** `FPGA_NOTES.md:73`
- **Severity:** high

The doc claims the generator commands 0x50-0x52 are 'reachable only from the 1013D touch UI paths (statemachine.c ~3600, generator.c); the 1014D build never sends them today', and builds the ROADMAP-14 hypothesis on it ('the boot hang is consistent with an unknown command wedging the stock FPGA's parser'). In reality main() calls fpga_on_off_generator() unconditionally in both variants, and that function sends 0x51 (freq), 0x52 (duty), and 0x50 (on/off) in BOTH the enable and disable branches — so the 1014D build sends all three generator commands to the stock FPGA at every boot, and boots fine on the bench (per the docs' own hardware-working status). The claim was already wrong when written: the call has been in main() since the pristine Atlan4 vendor commit 9daa91f. This also directly undercuts the parser-wedge hypothesis for the scope_generator_settings() hang (that function is pure UI drawing, no FPGA writes — menu.c:2538+).

**Evidence:** fnirsi_1013d_scope/fnirsi_1013d_scope.c:183 `fpga_on_off_generator();` (unguarded, in main() before the PORT_1014D-conditional blocks); fnirsi_1013d_scope/fpga_control.c:241-297 — both branches of fpga_on_off_generator() call fpga_set_generator_freq (cmd 0x51, line 278), fpga_set_generator_duty (cmd 0x52, line 293), then `fpga_write_cmd(0x50)` (lines 249/260); `git log -L183,183:fnirsi_1013d_scope/fnirsi_1013d_scope.c` shows the line introduced by vendor commit 9daa91f; Atlan4-1.00o5/fnirsi_1013d_scope/fnirsi_1013d_scope.c:161 has the same call. scope_generator_settings (menu.c:2530+) contains only display_* calls, no fpga_* writes.

**Suggested action:** Rewrite the paragraph at FPGA_NOTES.md:72-78: 0x50-0x52 are sent once at every boot from main() on both variants (with gen_enable from config, default 1); only the interactive generator UI is 1013D-only. Re-examine the ROADMAP-14 'unknown command wedges the stock FPGA parser' hang hypothesis, since the commands demonstrably go out each boot without wedging.

**Verifier:** Independently reproduced: fnirsi_1013d_scope.c:183 calls fpga_on_off_generator() unguarded in main() (surrounding PORT_1014D guards close at line 132 and reopen at 197), and fpga_control.c:241-264 sends 0x51 (line 278), 0x52 (line 293), and 0x50 (lines 249/260) in BOTH the enable and disable branches — so the 1014D build sends all three generator commands to the stock FPGA at every boot. git log -L183 traces the call to vendor commit 9daa91f (pristine Atlan4 has it at line 161), so FPGA_NOTES.md:72-73's 'the 1014D build never sends them today' was wrong when written. scope_generator_settings() (menu.c:2530-2721) contains only display_* calls; its fpga_set_generator_* calls were already commented in the vendor commit (git log -L2630,2640 shows them born commented), so the parser-wedge hypothesis for its hang (lines 77-78) has no mechanism — and the scope demonstrably boots and runs on bench while sending 0x50-0x52 with disable values.

## Confirmed — medium

*(from doc-claude-repo)*

### Internal contradiction: sawtooth declared SOLVED, but the pre-solution investigation narrative is retained as if live

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/CLAUDE.md:217`
- **Severity:** medium

Lines 204–207 correctly state the ≤200 ns/div sawtooth was SOLVED 2026-07-12 (F25, bench-confirmed gone). But lines 217–220 still carry the 2026-07-10 mid-investigation text as current status — 'Sawtooth status: cal does reduce it ~60%… residual is either v/div dependence… or a gain component… position-sweep test recipe in FPGA_NOTES.md §Screenshot analysis' — and lines 227–231 continue with 'Interleave-mismatch verdicts' and a cal-diagnostic description from the closed F22–F25 arc, none of it marked historical. A fresh session could burn time running the position-sweep recipe or reasoning about a 'residual' that no longer exists.

**Evidence:** CLAUDE.md:204 '**SOLVED (2026-07-12, F25)**… bench-confirmed gone' vs CLAUDE.md:217–220 'Sawtooth status: cal *does* reduce it ~60 %… residual is either v/div dependence… position-sweep test recipe' and CLAUDE.md:227–231 'Interleave-mismatch verdicts…'. FPGA_NOTES.md:190 heading 'SOLVED: sawtooth at ≤200 ns/div' with the old notes explicitly filed under line 206 '### Original investigation notes (2026-07-09, pre-solution)' — the same historical framing CLAUDE.md lacks.

**Suggested action:** Delete the stale sawtooth-status and interleave-verdict sentences or prefix them with 'historical (superseded by F25):' so the SOLVED statement is the only live claim.

**Verifier:** CLAUDE.md:204-207 declares F25 SOLVED (bench-confirmed gone), yet lines 217-220 retain the 2026-07-10 'Sawtooth status: cal does reduce it ~60%… residual is either v/div dependence… or a gain component… position-sweep test recipe' and 227-231 the 'Interleave-mismatch verdicts' cal-diagnostic text, with no historical marker. b793048's diff proves the mechanism: it replaced only the one '**Open:** sawtooth…' sentence with the SOLVED text and left the surrounding pre-solution narrative intact. FPGA_NOTES.md:190 ('## SOLVED: sawtooth at ≤200 ns/div…') explicitly files the same material under line 206 '### Original investigation notes (2026-07-09, pre-solution)' — the framing CLAUDE.md lacks. One caveat on wording: a small residual does still exist post-F25 (it is F30's open +7 mV thread, and the interleave cal/trim tooling remains live), so 'a residual that no longer exists' slightly overstates; but the retained hypotheses (v/div-dependence vs gain component) and the position-sweep recipe are definitively superseded by F25, so the contradiction and wasted-effort risk stand. Medium is right.

*(from doc-claude-repo)*

### fpga/ tree (built 1014D-retarget bitstream + TD toolchain) is absent from CLAUDE.md

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/CLAUDE.md:47`
- **Severity:** medium

CLAUDE.md enumerates every other repo-root directory (six vendor trees, bench/, tools/) but never mentions fpga/ — the 2026-07-18 FPGA thread's own work product: a complete self-contained Anlogic TD project (fpga/AL3_1014D/) that retargets Atlan4's AL3 design to the stock 1014D board, builds headless on this server, and has a BUILT-but-NEVER-FLASHED bitstream in out/. Its README also carries a safety-critical warning (Atlan4's own bitstream must never be flashed unmodified — pin functions differ between boards). A fresh session would not know the retarget project or the working headless TD flow exists and could redo the work or treat Atlan4-FPGA/ as the only TD project.

**Evidence:** ls repo root shows fpga/ containing AL3_1014D and README.md; git status header shows '?? fpga/'. fpga/README.md:1–11: '# fpga/ — 1014D retarget of Atlan4's AL3 replacement-FPGA design… **Status 2026-07-18: BUILT, NEVER FLASHED.**… Atlan4's own bitstream must never be flashed unmodified'. Commit 3de9c3d message: 'The fpga/ tree itself stays uncommitted on that thread.' FPGA_NOTES.md:628 '### TD rebuild of the AL3 project'. CLAUDE.md sections 'Reference trees' (47–71) and 'Local bench data and host tools' (73–83) list every other root dir but not fpga/.

**Suggested action:** Add an fpga/ bullet (e.g. under the local-tools section): untracked local TD project, status built-never-flashed, pointer to fpga/README.md and FPGA_NOTES.md §TD rebuild, and the never-flash-Atlan4's-bitstream warning.

**Verifier:** fpga/ exists at repo root (git status '?? fpga/', containing AL3_1014D/ and README.md); fpga/README.md opens '# fpga/ — 1014D retarget of Atlan4's AL3 replacement-FPGA design' with '**Status 2026-07-18: BUILT, NEVER FLASHED.**' and the safety warning 'Atlan4's own bitstream must never be flashed unmodified' (pin-function differences enumerated); AL3_1014D/out/ holds FPGA_1014D.bin/.bit plus reports. Commit 3de9c3d's message confirms 'The fpga/ tree itself stays uncommitted on that thread', and FPGA_NOTES.md:628 is the '### TD rebuild of the AL3 project' heading. grep 'fpga/' over CLAUDE.md returns zero hits — the file enumerates all six vendor trees (lines 47-71), bench/ and tools/ (73-83), but never the repo's own FPGA work product or its headless TD flow. Medium severity fits: a fresh session could redo the retarget or miss the never-flash warning.

*(from doc-claude-repo)*

### Factory settings menu description stale: missing 'Acquisition probe' item and the Sampling-clock Trim entries

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/CLAUDE.md:151`
- **Severity:** medium

Line 151–152 lists the Factory settings menu as 'Restore defaults / Reboot / FEL firmware update / Sampling clock' (repeated at line 198 as 'defaults/reboot/FEL'). The menu has had five items since 2026-07-17: 'Acquisition probe' was added as the launcher for the on-scope capture probe. This is the only way to run the acqprobe whose output files CLAUDE.md's bench section (lines 75–83) describes — the file documents the probe's data but never says how to invoke it. The Sampling clock submenu description (lines 155–158, '50 stock/57/67/80 MHz… plus the auto clock search') is also missing the 'Trim CH1'/'Trim CH2' interleave-trim items added 2026-07-11.

**Evidence:** fnirsi_1013d_scope/menu_1014d.c:5207–5214: const char *factory_menu_texts[5] = { "Restore defaults", "Reboot", "FEL firmware update", "Sampling clock", "Acquisition probe" }. menu_1014d.c:5292–5301: clock_menu_texts[7] ends '"Auto search", "Trim CH1", "Trim CH2"'. Commit 100f9ff (2026-07-17): 'Factory settings -> Acquisition probe: arm->flag + full-readout rate measurement and a 5x4608-sample raw ring dump'; commit ee3fc97 (2026-07-11): 'manual interleave trim in the Sampling clock menu'. CLAUDE.md:151–158 and 198 list only the four/older items.

**Suggested action:** Update both menu lists to the five-item reality, note that Factory settings → Acquisition probe is what produces the acqprobe.txt/ringdump.bin pairs the bench section describes, and add Trim CH1/CH2 to the Sampling-clock line.

**Verifier:** menu_1014d.c:5207-5214: factory_menu_texts[5] = { "Restore defaults", "Reboot", "FEL firmware update", "Sampling clock", "Acquisition probe" } — five items, exactly as cited. clock_menu_texts[7] (~line 5292) ends '"Auto search", "Trim CH1", "Trim CH2"'. History confirms the dates: 100f9ff (2026-07-17) 'Factory settings -> Acquisition probe: arm->flag + full-readout rate measurement and a 5x4608-sample raw ring dump'; ee3fc97 (2026-07-11) 'manual interleave trim in the Sampling clock menu'. CLAUDE.md:151-158 lists only the four items and describes the clock submenu as '50 stock/57/67/80 MHz… plus the auto clock search' with no Trim entries; line 198 repeats 'defaults/reboot/FEL'. The 'only way to run the acqprobe' claim matches the commit message (the probe is a Factory-settings item), and CLAUDE.md's bench section indeed documents the probe's output files without ever saying how to invoke it.

*(from doc-memory)*

### MEMORY.md hook still lists F29 'disabled-CH2 −22V' as an open bug — its visible symptom was fixed and bench-verified

- **Where:** `/home/jsantala/.claude/projects/-home-jsantala-src-FNIRSI-1014D-Firmware/memory/MEMORY.md:3`
- **Severity:** medium

The bringup hook says 'open bugs F29–F30 (disabled-CH2 −22V, Base-cal non-repeatable +7mV…)'. PORT_AUDIT.md F29 records the display guard 'IMPLEMENTED + bench-verified 2026-07-17' (both renderers dash disabled channels' slots), and commit 100f9ff is titled 'F34 spacing + F29 dash guard (bench-OK)'. The memory body itself records this ('F34 + F29 dash guard verified working', body line ~326). Only the underlying stale-value root cause (why −22 V specifically) remains open, folded under the F30 pecostm32-A/B umbrella. A reader skimming only the index would re-investigate or re-fix a symptom that no longer reproduces.

**Evidence:** MEMORY.md:3 '**open bugs F29–F30** (disabled-CH2 −22V, …)'. PORT_AUDIT.md:682 'F29 — … display guard IMPLEMENTED + bench-verified 2026-07-17 … The underlying stale-value question … stays open under the F30 pecostm32-A/B umbrella.' git log: 100f9ff '1014d: F34 spacing + F29 dash guard (bench-OK)…'.

**Suggested action:** Reword the hook to 'open: F30 (+ F29 root cause under its umbrella; F29 display symptom fixed/bench-OK)'.

**Verifier:** All cited evidence reproduced. MEMORY.md line 3 says '**open bugs F29–F30** (disabled-CH2 −22V, Base-cal non-repeatable +7mV/trace-above-0V, gated on pecostm32 A/B)'. PORT_AUDIT.md:682 states the F29 header as 'display guard IMPLEMENTED + bench-verified 2026-07-17' with 'The underlying stale-value question (why −22 V specifically) stays open under the F30 pecostm32-A/B umbrella'. Commit 100f9ff (2026-07-17) is titled '1014d: F34 spacing + F29 dash guard (bench-OK)…'. The memory's own body agrees at line 326: 'F34 + F29 dash guard verified working'. Aggravating detail: the hook line was clearly rewritten after 2026-07-19 (it carries the acqprobe and fill-geometry content) yet the stale 'open bugs F29–F30' phrasing survived that rewrite. A hook-only reader would treat the fixed −22V display symptom as a live bug. Medium is the right severity.

*(from doc-memory)*

### Broken [[atlan4-merge-plan]] link in notes-repo.md — no such memory file exists

- **Where:** `/home/jsantala/.claude/projects/-home-jsantala-src-FNIRSI-1014D-Firmware/memory/notes-repo.md:23`
- **Severity:** medium

notes-repo.md ends 'See [[atlan4-merge-plan]] and [[dev-flash-workflow]]', but atlan4-merge-plan.md does not exist in the memory directory (10 files present, none by that name; grep across all memories finds this as the only reference). The merge-plan memory was evidently deleted/renamed after the merge landed (its successor is atlan4-1014d-bringup.md). A dangling pointer sends a future session hunting for a plan that no longer exists. All other [[links]] across the memory set resolve correctly.

**Evidence:** notes-repo.md:23 'See [[atlan4-merge-plan]] and [[dev-flash-workflow]].'; ls of the memory dir shows no atlan4-merge-plan.md; grep -n 'atlan4-merge-plan' across memory/*.md matches only this line.

**Suggested action:** Replace [[atlan4-merge-plan]] with [[atlan4-1014d-bringup]] (or drop it).

**Verifier:** Reproduced exactly. notes-repo.md:23 reads 'See [[atlan4-merge-plan]] and [[dev-flash-workflow]].' The memory directory contains MEMORY.md plus 10 topic files (1014d-no-onboard-test-signal, anlogic-td-toolchain, ask-vs-act-calibration, atlan4-1014d-bringup, bench-report-convention, dev-flash-workflow, intermittent-symptom-discipline, notes-repo, pecostm32-1014d-reference, user-terminal-misclicks) — no atlan4-merge-plan.md. grep across the whole memory dir finds this line as the only reference. I also verified the finding's side-claim: every other [[link]] target across all memory files (atlan4-1014d-bringup, pecostm32-1014d-reference, dev-flash-workflow, intermittent-symptom-discipline, anlogic-td-toolchain, bench-report-convention, ask-vs-act-calibration) resolves to an existing file. The link is a genuine dangling pointer whose successor content lives in atlan4-1014d-bringup.md.

*(from doc-memory)*

### atlan4-1014d-bringup.md has decayed into a 33 KB session log duplicating the committed repo docs, with a stale lede

- **Where:** `/home/jsantala/.claude/projects/-home-jsantala-src-FNIRSI-1014D-Firmware/memory/atlan4-1014d-bringup.md:11`
- **Severity:** medium

The file is 33,462 bytes of chronological append-only entries (2026-07-09 → 2026-07-19) that restate at length what PORT_AUDIT.md, FPGA_NOTES.md, BOOT_NOTES.md, and CLAUDE.md now own (those docs are committed since 204201d and updated in every subsequent commit — the memory even says so: 'deep contracts in repo BOOT_NOTES.md/FPGA_NOTES.md'). It opens with a now-false state snapshot: 'State as of 2026-07-09 evening: commit 4a5fa61 … plus an uncommitted audit-and-fix pass … user commits themselves' — everything described was committed 2026-07-10/11 (2ab7296, 77af34b, 204201d). Superseded chapters (e.g. the 2026-07-11 'parked as hardware question' sawtooth verdict, lines 211-232, overturned by the 2026-07-12 SOLVED entry) are retained; the supersession markers help, but a reader must consume ~400 lines to extract current truth. Per-line repo-doc facts inside it will silently rot (the '(tracked)' claim already has). Also stale within it: 'Branch 23 ahead of origin, nothing pushed' (line 295) and 'Nothing pushed' (line 407) — the branch is now 0 ahead of origin/atlan4-base, everything pushed.

**Evidence:** wc -c = 33462. Lines 11-13 vs git log (2ab7296/77af34b/204201d all dated 2026-07-10 23:58-59). Line 295 'Branch 23 ahead of origin, nothing pushed'; line 407 'Nothing pushed.' vs `git status -sb` '## atlan4-base...origin/atlan4-base' and `git log origin/atlan4-base..HEAD | wc -l` = 0. Repo docs exist and carry the same content (PORT_AUDIT.md F25/F29/F35 verified present).

**Suggested action:** Rewrite as a short current-state memory (what works, open items F30/F35/A-B, fpga/ untracked status, pointers to PORT_AUDIT/FPGA_NOTES sections); delete superseded chapters and all git-state claims ('N ahead', 'nothing pushed') that git answers authoritatively.

**Verifier:** Every factual sub-claim reproduced: wc -c = 33,462 bytes of dated append-only entries (frontmatter modified 2026-07-21). The lede (lines 11-13) still frames the file's state as 'commit 4a5fa61 … plus an uncommitted audit-and-fix pass … user commits themselves' — git shows that pass was committed 2026-07-10 23:58-23:59 (2ab7296, 77af34b, 204201d), so the framing has been false for six weeks. Line 295 'Branch 23 ahead of origin, nothing pushed' and line 407 'Nothing pushed.' verified in place; current reality is `## atlan4-base...origin/atlan4-base` with 0 commits ahead — everything pushed. The superseded 2026-07-11 'Parked as hardware question' sawtooth verdict (≈lines 211-216) is retained ahead of the 2026-07-12 'SOLVED … NOT EMI/thermal' entry. The duplication-rot hazard is proven, not hypothetical — the '(tracked)' claim (finding 0) is exactly such a rotted per-line repo fact. One fairness note: the entries are individually dated, so they are point-in-time statements rather than lies; but a state-snapshot memory whose lede and push-status lines no longer describe reality, at 33 KB, matches the finding's characterization. Medium severity is right.

*(from doc-agents)*

### AGENTS.md display-geometry rules stale after F17 (705-wide 1014D blit, per-frame sidebar redraw)

- **Where:** `AGENTS.md:157`
- **Severity:** medium

AGENTS.md teaches: the cleared rect (2,48,728,432) is what gets blitted, and 'Chrome at y < 48 ... or x >= 730 (right measurement column) persists without per-frame redraw.' On the current 1014D build this is wrong twice: (1) the per-frame blit was narrowed to (2,48,705,432) by F17 (commit 77af34b, 2026-07-10), so anything drawn into displaybuffertmp at x 707..729 never reaches the screen — a developer following AGENTS.md's 'must be redrawn INSIDE this function if x < 730' rule and placing chrome at x=710 would get invisible chrome; conversely on-screen content at x >= 707 (not 730) is what survives the blit; (2) the right measurement column does NOT persist without per-frame redraw — ui_update_measurements() is now called every frame after the blit (F12). The adjacent chrome-call list (AGENTS.md:159-164) also omits ui_draw_outline() (drawn first) and ui_update_measurements(). AGENTS.md was last committed 2026-07-10 23:59 (204201d), same day as the code change, and never caught up.

**Evidence:** fnirsi_1013d_scope/scope_functions.c:3578 'display_fill_rect(2, 48, 728, 432);' (clear, still 728-wide); scope_functions.c:3780-3788 '#if PORT_1014D ... display_copy_rect_to_screen(2, 48, 705, 432); ... ui_update_measurements();' with comment 'the trace window ends at x=706 ... Copying Atlan4's full 728-wide trace buffer wiped the sidebar edge'; scope_functions.c:3768 'ui_draw_outline();' opens the 1014D chrome block; 1013D path keeps 728: scope_functions.c:3794. git log -S"705, 432" → 77af34b 2026-07-10 'GUI seam fixes F10-F17'; AGENTS.md last commit 204201d 2026-07-10.

**Suggested action:** Rewrite the 'clears rect' paragraph to state: clear is (2,48,728,432) into displaybuffertmp, but the 1014D blit is (2,48,705,432) — visible trace area ends at x=706, chrome for x>=707 must be drawn to the visible screen (or via ui_update_measurements), and the sidebar IS redrawn every frame. Add ui_draw_outline() and ui_update_measurements() to the chrome-call list.

**Verifier:** Independently reproduced all evidence: scope_functions.c:3578 still clears (2,48,728,432) but the 1014D blit at :3785 is display_copy_rect_to_screen(2,48,705,432) (1013D path at :3794 keeps 728), so chrome drawn into displaybuffertmp at x 707..729 never reaches the screen — AGENTS.md:157's 'x < 730 must be redrawn / x >= 730 persists' rule is wrong for the current 1014D build. ui_update_measurements() is called every frame at :3788 and ui_draw_outline() opens the chrome block at :3768; AGENTS.md:159-164's chrome list omits both (grep: 0 hits). Git confirms the timeline: the 705 blit landed in 77af34b (2026-07-10 23:58:59, 'GUI seam fixes F10-F17'); AGENTS.md was last committed in 204201d 16 seconds later and never caught up. One phrasing nuance does not change the verdict: sidebar content at x>=707 still mechanically survives the blit (nothing wipes it), so 'persists' is not strictly false — but the boundary is 707 not 730, and the column is in fact redrawn every frame, so the doc's geometry teaching is stale exactly as claimed.

*(from doc-agents)*

### AGENTS.md still lists the <=200 ns/div sawtooth as Open — solved 2026-07-12 (F25)

- **Where:** `AGENTS.md:69`
- **Severity:** medium

AGENTS.md ends its fix list with '**Open:** ≤200 ns/div sawtooth — FPGA_NOTES.md has the analysis and the P14-binary A/B recipe.' The issue was root-caused and fixed 2026-07-12: Atlan4 had commented out the FPGA 0x28 mode-select in fpga_do_conversion; restoring it on the fw_FPGA==1 path was bench-confirmed to eliminate the sawtooth (PORT_AUDIT F25). A reader of AGENTS.md would burn effort on the obsolete A/B recipe or distrust fast timebases that now work.

**Evidence:** AGENTS.md:69 '**Open:** ≤200 ns/div sawtooth'; PORT_AUDIT.md:612 'Update (2026-07-12): superseded by F25 — 0x28 was the actual cause', PORT_AUDIT.md:617 'F25 — Atlan4 dropped the FPGA 0x28 mode-select the stock silicon needs every conversion'; CLAUDE.md Known issues: 'SOLVED (2026-07-12, F25) ... bench-confirmed gone'.

**Suggested action:** Change the Open note to: SOLVED 2026-07-12 (PORT_AUDIT F25 — restored FPGA 0x28 mode-select on the stock fw_FPGA==1 path).

**Verifier:** AGENTS.md:69 ends the fix list with '**Open:** ≤200 ns/div sawtooth — FPGA_NOTES.md has the analysis and the P14-binary A/B recipe.' PORT_AUDIT.md:612 ('Update (2026-07-12): superseded by F25 — 0x28 was the actual cause'), :617 (F25 heading), and :646 ('CONFIRMED ON BENCH (2026-07-12): 0x28 was the root cause — the sawtooth is gone') document the issue as solved; CLAUDE.md's Known-issues section states the same. A reader of AGENTS.md would pursue an obsolete A/B recipe for a solved bug.

*(from doc-agents)*

### PORT_A.md atlan4-base status sections (2026-07-06) give stale flash/FEL guidance for the active branch with no local stale marker

- **Where:** `PORT_A.md:220`
- **Severity:** medium

The top banner (lines 3-8) frames the file as documenting the first-generation port on the PORT_A branch, but the trailing sections 'Port-forward onto Atlan4 — status (branch atlan4-base, updated 2026-07-06)' and 'Phase 3 graft — DONE' (lines 220-300) describe the ACTIVE branch in present tense and are now wrong on operational details: (1) lines 232-236 say the packed image base is bootloader_base.bin with the scope overlaid at 0x5BC00 and give a verify recipe 'xxd -s 0x5BC00 ... eGON.EXE' — the default 1014D variant now uses bootloader_1014d_base.bin at offset 0x8000; (2) lines 247-249 present FEL entry via 'boot-config value 2 at STARTUP_CONFIG_ADDRESS (0x81BFFC1F); the loader enters FEL on warm reboot' — dead with the current 1014D bootloader (no persistent boot byte; FEL is Factory settings menu or power-on key-hold → F3); (3) line 250 'Both self-update entries currently reachable only by touch — Phase 3 binds a port_a key to each' — the Factory settings menu now provides FEL/reboot; (4) lines 267/282 describe port_a.c/.h and port_a.o as present (the banner does contradict this one). A reader landing mid-file (e.g. grepping for FEL or 0x5BC00) gets stale procedures presented as branch status; only #4 is covered by the banner.

**Evidence:** PORT_A.md:232-236 'committed as fnirsi_1013d_scope/bootloader_base.bin ... Verify a build with: xxd -s 0x5BC00'; PORT_A.md:247-249 'FEL ... boot-config value 2 at STARTUP_CONFIG_ADDRESS (0x81BFFC1F)'; PORT_A.md:250 'reachable only by touch'. Current reality: fnirsi_1013d_scope/Makefile:123-130 'PORT_1014D (1014D) -> bootloader_1014d_base.bin, overlay at 0x8000' with BOOTLOADER_BASE/OFFSET selection; AGENTS.md:126 'The old sector-710 byte poke does NOT work with the current 1014D bootloader (BOOT_NOTES.md)'; sm_1014d.c:2605 sm_open_factory_menu / 2667 sm_enter_fel_mode (key-driven FEL entry exists).

**Suggested action:** Add a one-line local marker to the 'Port-forward onto Atlan4' heading (e.g. '⚠️ snapshot of 2026-07-06, before the modules-import overhaul — flashing/FEL details superseded; see CLAUDE.md/BOOT_NOTES.md'), so mid-file readers can't take the 0x5BC00/boot-byte/touch-only claims as current.

**Verifier:** All cited PORT_A.md content reproduced: lines 220-236 present bootloader_base.bin/0x5BC00 with the xxd verify recipe as atlan4-base status; 247-249 give FEL via boot-config value 2 at 0x81BFFC1F; 250 says both self-update entries are touch-only; 267/282 describe port_a.c/.h and port_a.o as present (port_a.* confirmed absent from the tree). Current reality contradicts all of it for the default variant: Makefile:124-130 selects bootloader_1014d_base.bin at 0x8000 when PORT_1014D=1; BOOT_NOTES.md:29-50 states pecostm32's 1014D loader never reads the boot-config byte and the poke workflow is 'obsolete on this chain'; sm_1014d.c:2605 (sm_open_factory_menu), :2667 (call), :2897 (sm_enter_fel_mode, 0xFFFF0020) provide key-driven FEL/reboot. The sections at 220-300 carry no local stale marker — the line-3 banner frames only the port_a-era content and points elsewhere, but these sections claim in their own headers to be atlan4-base status. Minor context (does not refute): the guidance was accurate when written on 2026-07-06 (pre-1014D-bootloader switch) and 0x5BC00 remains correct for the 1013D variant — but presented unqualified as branch status it now misleads for the default 1014D variant, exactly the hazard claimed.

*(from doc-agents)*

### AGENTS.md never updated for two PORT_AUDIT §3 corrections it elsewhere accepted (inline-if-chain dispatch; 'untracked files')

- **Where:** `AGENTS.md:87`
- **Severity:** medium

PORT_AUDIT §3 corrected 'Key dispatch — Inline in main() loop' (superseded by sm_init()/sm_handle_user_input()) and 'menu_1014d.c + sm_1014d.c ... as untracked files' (they are committed; objects listed explicitly). AGENTS.md was partially updated — line 81 states the sm_1014d.c dispatch and line 101 says 'committed (4a5fa61) ... listed explicitly' — but two spots kept the corrected text: line 87 ('Architecture — key insight') still says 'the 1014D path is an inline if-chain in main()', and the line 95 plan-table row still says '(already imported from fork as untracked files)'. The file now contradicts itself, and line 87's claim describes an input architecture that no longer exists.

**Evidence:** AGENTS.md:87 'the 1014D path is an inline if-chain in `main()`' vs AGENTS.md:81 'Key dispatch — sm_init() + sm_handle_user_input() in sm_1014d.c ... the old inline-in-main() dispatch is gone'; AGENTS.md:95 'already imported from fork as untracked files' vs AGENTS.md:101 'committed (4a5fa61) ... listed explicitly in nbproject/Makefile-Debug.mk (no wildcard)'; PORT_AUDIT.md:210-214 (§3 errata, both items); code: sm_1014d.c dispatch confirmed (sm_1014d.c:2605 etc.), commit 4a5fa61 'working state' exists.

**Suggested action:** Edit line 87 to say the 1014D input path is the sm_1014d.c state machine (keep the 'no input abstraction layer' insight for the 1013D touch side), and drop 'as untracked files' from the line 95 table row.

**Verifier:** Self-contradiction reproduced verbatim: AGENTS.md:81 says dispatch is sm_init()+sm_handle_user_input() in sm_1014d.c and 'the old inline-in-main() dispatch is gone', yet :87 still asserts 'the 1014D path is an inline if-chain in main()' in present tense; AGENTS.md:101 says the modules are 'committed (4a5fa61) ... listed explicitly ... (no wildcard)', yet the :95 plan-table row still says '(already imported from fork as untracked files)'. PORT_AUDIT.md:210-214 contains exactly the two §3 corrections. Commit 4a5fa61 ('working state') exists, and current fnirsi_1013d_scope.c:297/:441 confirm the sm_1014d.c dispatch is what actually runs — line 87 describes an architecture that no longer exists.

*(from doc-audit-roadmap)*

### ROADMAP 1b still queues a bench A/B for the sawtooth solved 2026-07-12 (F25)

- **Where:** `ROADMAP.md:13`
- **Severity:** medium

Item 1b, sitting in the '## Now / next bench session' action queue, says 'Sawtooth at ≤200 ns/div — run Base calibration first, then the P14-binary A/B test (FPGA_NOTES.md §sawtooth); fix direction depends on the outcome.' The sawtooth was root-caused and bench-confirmed fixed on 2026-07-12 (missing FPGA 0x28/0x29 mode-select, PORT_AUDIT F25); the referenced FPGA_NOTES section is now literally headed 'SOLVED'. The item was never struck despite the file's own convention ('Strike items as they land or get rejected', line 4). A reader working the 'Now / next' queue would plan a bench A/B for a closed bug.

**Evidence:** ROADMAP.md:13-15 (unstruck item); PORT_AUDIT.md:646-648 'CONFIRMED ON BENCH (2026-07-12): 0x28 was the root cause — the sawtooth is gone'; FPGA_NOTES.md:190 '## SOLVED: sawtooth at ≤200 ns/div — missing FPGA 0x28 mode-select (bench, 2026-07-12)'; commits 1a18ce8 ('restore FPGA 0x29/0x28 dual-ADC mode-select in do_conversion (F25)') and b793048 ('mark sawtooth solved'); fix present in code at fnirsi_1013d_scope/fpga_control.c:631-643.

**Suggested action:** Strike item 1b with a 'Done 2026-07-12 (PORT_AUDIT F25)' annotation pointing at FPGA_NOTES §SOLVED, matching the style of the struck items 2 and 16.

**Verifier:** ROADMAP.md:13-15 still carries item 1b unstruck in the 'Now / next bench session' queue, prescribing the P14-binary A/B for the sawtooth; ROADMAP.md:4 states the strike convention. PORT_AUDIT.md:646-648 says 'CONFIRMED ON BENCH (2026-07-12): 0x28 was the root cause — the sawtooth is gone'; FPGA_NOTES.md:190 is literally headed 'SOLVED: sawtooth at ≤200 ns/div'. Commits 1a18ce8 (restore 0x29/0x28, F25) and b793048 ('mark sawtooth solved') exist; the fix is live in fpga_control.c:631-642 (#if PORT_1014D + fw_FPGA==1, 0x29/0x01 then 0x28/0x00). A reader working the queue would plan a bench session for a closed bug. All evidence independently reproduced.

*(from doc-audit-roadmap)*

### PORT_AUDIT §4 checklist item 1 prescribes the pre-F21 timebase behavior (8↔11 skip) that F21 replaced with 6↔11

- **Where:** `PORT_AUDIT.md:223`
- **Severity:** medium

Hardware-verify checklist item 1 tells the bench tester to exercise 'the long/short boundary (the 20/50 ms skip at indices 8↔11)'. F21 (2026-07-11, commit 87cf94b) deliberately changed that boundary: roll is kept for ≤500 ms only and 'crossing jumps 6↔11, making those four settings sweep-only' — i.e. 200/100/50/20 ms are no longer long-timebase and there is no 8↔11 skip any more. The checklist was never updated, so following it verbatim would make a tester report the intended F21 behavior as a regression. (The checklist item is also largely de facto exercised: the F21 bench session stepped the dial through this exact region.)

**Evidence:** PORT_AUDIT.md:221-224 vs PORT_AUDIT.md:444-448 ('the sm_set_time_base() boundary now keeps roll for ≤500ms only (crossing jumps 6↔11), making those four settings sweep-only'); code confirms: fnirsi_1013d_scope/sm_1014d.c:1456-1457 'if(timeperdiv == 7) { timeperdiv = 11; long_mode = 0; } / if(timeperdiv == 10) { timeperdiv = 6; long_mode = 1; }'; commit 87cf94b 'cal at run level, honest residual readout, symmetric trim, roll-mode overlap'.

**Suggested action:** Update §4 item 1 to describe the F21 boundary (roll ≤500 ms, crossing jumps 6↔11; 200/100/50/20 ms sweep-only), or strike it with a pointer to F21's bench session.

**Verifier:** PORT_AUDIT.md:222-224 (checklist item 1) tells the tester to exercise 'the 20/50 ms skip at indices 8↔11'. Commit 87cf94b's diff shows exactly that boundary being replaced: 'timeperdiv == 9 → 11 / == 10 → 8' became '== 7 → 11 / == 10 → 6', and the current sm_1014d.c:1456-1457 matches the finding's quote verbatim (6↔11 jump, indices 7..10 unreachable, 200/100/50/20 ms sweep-only per the comment at 1447-1453). PORT_AUDIT.md:444-448 (F21) documents the new boundary. The checklist is still live guidance (CLAUDE.md and ROADMAP item 1 both point bench work at §4), so following it verbatim would flag intended F21 behavior as a regression. The 'de facto exercised' aside is consistent with F21's bench narrative (the dial was stepped through this region on 2026-07-11).

*(from doc-audit-roadmap)*

### ROADMAP 16 tail says TD toolchain 'not yet installed' and retarget is 'real remaining work' — both done 2026-07-18

- **Where:** `ROADMAP.md:101`
- **Severity:** medium

Item 16 (struck-done for the vendoring half) still states: 'Real remaining work before any flash: retarget zaklad.v to the 1014D pinout' (lines 101-102) and 'Toolchain downloaded 2026-07-10 to ~/tools/anlogic-td/ (TD 5.0.3 Linux + 4.6.4 + licenses), not yet installed' (lines 106-108). Both are stale: FPGA_NOTES records the laptop TD 5.0.4 install + working license (2026-07-17/18), the Linux headless install DONE (2026-07-18), the headless flow VALIDATED, and the 1014D retarget bitstream BUILT (fpga/AL3_1014D/out/FPGA_1014D.bin exists on disk, with build.tcl/zaklad_1014d.adc/.sdc). The remaining pre-flash gate is item 17's loader 0x1432 patch, not the retarget.

**Evidence:** ROADMAP.md:101-102, 106-108; FPGA_NOTES.md:578-585 ('Status 2026-07-17: TD 5.0.4 ... installed on the user's Windows laptop ... License confirmed working in practice 2026-07-18 ... The Linux 5.0.3 headless install on this server is DONE the same day'); FPGA_NOTES §TD rebuild follow-on ('2026-07-18 (later): Linux headless flow VALIDATED, 1014D bitstream BUILT ... FPGA_1014D.bin = 282,898 B'); verified on disk: fpga/AL3_1014D/out/{FPGA_1014D.bin,FPGA_1014D.bit,timing_1014d.rpt} and ~/tools/anlogic-td/TD_5.0.3_28716_NL/ (unzipped install + shim).

**Suggested action:** Rewrite item 16's tail: retarget built (fpga/AL3_1014D/, FPGA_NOTES §TD rebuild); toolchain installed both ends (license expires 2026-08-31); remaining pre-flash work = item 17 loader patch + flashing route.

**Verifier:** ROADMAP.md:101-102 says 'Real remaining work before any flash: retarget zaklad.v to the 1014D pinout' and :106-108 says the toolchain is 'not yet installed'. Both are demonstrably stale: FPGA_NOTES.md:578-585 records the laptop TD 5.0.4 install, license confirmed working 2026-07-18, and the Linux 5.0.3 headless install DONE; FPGA_NOTES.md:674 says 'Linux headless flow VALIDATED, 1014D bitstream BUILT' and :700 gives FPGA_1014D.bin = 282,898 B. Verified on disk: fpga/AL3_1014D/out/FPGA_1014D.bin is exactly 282,898 bytes (dated Jul 18), alongside FPGA_1014D.bit, timing_1014d.rpt, and the retarget inputs zaklad_1014d.adc/.sdc + build.tcl/build.sh; ~/tools/anlogic-td/TD_5.0.3_28716_NL/ is an unzipped install with license/, a shim dir, and td_20260718_* logs. The remaining pre-flash gate is indeed item 17's loader 0x1432 patch.

*(from doc-audit-roadmap)*

### ROADMAP 15's premises are stale: 'sawtooth suspect' disproved by F25, and the preset menu + auto clock search it proposes already exist

- **Where:** `ROADMAP.md:84`
- **Severity:** medium

Item 15 says 'Order matters: fix interleave calibration at stock rate first (item 11, the sawtooth suspect); only then consider vetted overclock presets', that an auto-calibrate clock loop 'needs a pass/fail oracle', and that 'fixed validated presets from a debug menu are the sane shape'. Three staleness points: (1) the sawtooth was not a calibration problem — F25 proved it was the missing 0x28 mode-select; (2) the 'sane shape' was already implemented 2026-07-10: Factory settings → Sampling clock offers 50/57/67/80 MHz presets plus trims; (3) an auto clock search exists and was repaired/decoupled from Base cal 2026-07-10 (FPGA_NOTES has two dated sections on it). Item 28 partially re-scopes clock work post-F25 but item 15 was never reconciled.

**Evidence:** ROADMAP.md:80-92; PORT_AUDIT.md:646-648 (F25 bench-confirmed); FPGA_NOTES.md:409 '### Auto clock search in Base calibration (Grok Build's, repaired 2026-07-10)' and :448 '### Clock search decoupled from Base calibration (2026-07-10, evening)'; CLAUDE.md 'Sampling clock (2026-07-10) opens a submenu for manual Si5351 selection (50 stock/57/67/80 MHz ...) plus the auto clock search'; code: fnirsi_1013d_scope/sm_1014d.c:2799-2810 (clock_menu_p1b preset dispatch + auto-search item).

**Suggested action:** Annotate item 15: presets menu + auto search landed 2026-07-10; sawtooth ordering constraint retired by F25; remaining live content is the CLK2 cal-output idea and the interleave-statistics auto-trim, with clock experiments continuing under item 28.

**Verifier:** ROADMAP.md:84-92 (item 15) still says 'fix interleave calibration at stock rate first (item 11, the sawtooth suspect)' and calls fixed presets from a debug menu 'the sane shape' as if hypothetical. All three staleness points reproduce: (1) PORT_AUDIT.md:646-648 proves the sawtooth was the missing 0x28 mode-select, not calibration; (2) the preset menu exists — menu_1014d.c:5304 clock_menu_p1b[4]={0x06,0x05,0x04,0x03} dispatched at sm_1014d.c:2799-2807 (CLAUDE.md maps these to 50/57/67/80 MHz), items 5/6 are the trims; (3) the auto clock search exists at menu item 4 (sm_1014d.c:2808-2811, sm_do_clock_auto_search) and FPGA_NOTES.md:409/:448 document its repair and decoupling on 2026-07-10. Item 15 carries no strike or reconciliation annotation, while item 28 (ROADMAP:209-222) re-scopes clock work separately.

*(from code-recent)*

### Fill-geometry 'CLOSED' claims mix per-ADC ring addresses with interleaved samples (2x)

- **Where:** `FPGA_NOTES.md:131`
- **Severity:** medium

Commit ceeae17 records the post-trigger fill question as CLOSED, but three derived statements are off by exactly 2x from mixing per-ADC ring addresses with interleaved (2-ADC) screen samples: (1) FPGA_NOTES.md:131 'display window ring 161…3161 (= raw − 750)' — the standard readout fetches only nofsamples=1500 samples per ADC from the translated pointer, so the readout window spans 1500 ring addresses (161…1661), not 3000; (2) FPGA_NOTES.md:132-133 (and ROADMAP.md:200, tools/acqprobe_analyze.py:325-326, both commit messages) 'pre-trigger fresh = 910, of which 750 are on-screen → trigger sits at 25 % of the 3000-sample window' — 750 pre-trigger ring addresses = 1500 interleaved samples = 50 % of the 3000-sample window (750/3000 divides an address count by a sample count); this also contradicts the firmware's own naming: settriggerpoint == samplecount/2 is labeled 50 % (menu.c:6283) and the FPGA is written settriggerpoint/2 = 750 (fpga_control.c:681) — exactly the fw1 −750 constant; (3) FPGA_NOTES.md:140-141 / analyzer output 'the gap sits just past the display's right edge' — with the true window edge at ring 1661, the held-run gap (starts ring 3418) is ~1757 addresses past it and the noisy-run gap (2616) ~955 past; the region 1661…gap-start is fresh but off-window data, not gap. The same section's own earlier bullet (FPGA_NOTES.md:113-114: 'read pointer = trigger − 750 (per ADC), then 1500 samples per ADC × 2 ADCs → 3000 interleaved per channel centered on the trigger', 'readout uses only ~37 % of the ring' = 1500/4096) states the correct model, so the doc is internally contradictory. Related: analyzer line 196 prints '4096-sample ring spans 4096/rate per ADC' using the combined rate — half the true per-ADC span (bench 5 ms/div prints 204.8 ms; true 409.6 ms). The per-ADC-unit numbers themselves (910 pre / 2507 post / gap 679–1481 / fixed start ~0 / 'post-trigger ≈2500 per ADC' at ROADMAP.md:229) are correct.

**Evidence:** scope_functions.c:865 'scopesettings.nofsamples = fpgasettings.totalsamples/2' (=1500); fpga_control.c:1072 'count = scopesettings.nofsamples' clocks per ADC command; fpga_control.c:798/870 interleave the two 1500-sample ADC reads into the 3000-entry tracebuffer (stride 2, offsets 1/0) — so a capture window occupies samplecount/2 ring addresses. menu.c:6283 'settriggerpoint == scopesettings.samplecount / 2)//50%'; fpga_control.c:681 'fpga_write_short(fpgasettings.settriggerpoint/2)'. Empirical from the commit's own bench data: autocorrelating bench/acqprobe/2026-07-19-trigger-probe/ringdump_5ms.bin block 0 gives a 50 Hz hum period of 198–200 ring addresses — i.e. addresses tick at combined-rate/2 (20 KSa/s combined → 10 KSa/s per address), so 3000 interleaved samples = 1500 addresses. Analyzer re-run reproduces the doc numbers (trigger ring 911, pre 910, post 2507, gap 679) plus the wrong prints ('display window ring 161..3161', 'trigger at ~25% width').

**Suggested action:** Correct FPGA_NOTES.md:131-141 and ROADMAP.md:200 (window = raw−750 … raw+750 in ring addresses; trigger mid-window = 50 %; gap sits ~950–1750 addresses past the readout window with fresh off-window data in between), and fix tools/acqprobe_analyze.py: line 196 use rate/2 for the per-ADC span, line 325 use samplecount//2 for the window end, line 326 report 50 % (750 addresses = 1500 of 3000 interleaved samples). Re-check any future serial-reply/multi-sampling window math against the corrected geometry.

**Verifier:** Independently re-derived the unit model from the code and reproduced the empirical test from the bench data. Code: nofsamples = totalsamples/2 = 1500 (scope_functions.c:865, totalsamples=3000 at :7828); fpga_read_adc_data clocks nofsamples samples per ADC command (fpga_control.c:1072); fpga_read_sample_data writes the same 0x1F pointer before each of the two per-ADC reads and interleaves them stride-2 into the 3000-entry tracebuffer (fpga_control.c:787-798, 858-873) — so one capture window occupies 1500 ring addresses, matching the doc's own '~37 % of the ring' (FPGA_NOTES.md:113) and '3000 interleaved ... centered on the trigger' (line 114), and contradicting line 131 ('ring 161…3161') and line 133 ('25 %'). Firmware corroboration verified: menu.c:6283 labels settriggerpoint==samplecount/2 as //50%; fpga_control.c:681 writes settriggerpoint/2 = 750; fw1 translation is data<750 ? +3345 : −750 (scope_functions.c:810-822). Decisive empirical check reproduced from scratch: autocorrelating block 0 of both driven dumps in bench/acqprobe/2026-07-19-trigger-probe/ gives a 50 Hz hum period of 198-199 ring addresses (peak at lag 198/199, ac(200)=4535 vs ac(400)=3630 for 5ms; 1562 vs 1232 for tt5ms) — addresses tick at combined-rate/2 = per-ADC, so 750 pre-trigger addresses = 1500 interleaved samples = 50 % of the window, and analyzer line 196's '204.8 ms per ADC' at 20 KSa/s is half the true 409.6 ms per-ADC span. Re-running tools/acqprobe_analyze.py reproduces every disputed print (lines 324-331: 'display window ring 161..3161', 'trigger at ~25% width', 'gap sits past the display's right edge'). The 25 % claim confirmed present in ROADMAP.md:200 and commit ceeae17's message; gap distances check out (held gap start 3418 − true window end 1661 = 1757 addresses; noisy 2616 − 1661 = 955). Also verified the finding's concession that the per-ADC-unit numbers (910 pre / 2507 & 1705 post / gap 679 & 1481 / fresh start ring ~1 / ROADMAP.md:228-229 '≈2500 post-trigger samples per ADC') are correct as stated.

*(from code-seams)*

### sm_set_time_base samplerate clause dropped !waveviewmode — wave view + single-trigger corrupts the viewed capture's sample rate and re-arms

- **Where:** `fnirsi_1013d_scope/sm_1014d.c:1480`
- **Severity:** medium

Class (ii)/(iii) drift in the F1 rebuild. The F1 fix mirrored Atlan4's scope_set_timebase, whose sample-rate-update condition is '((previousytouch < 240)&&(!scopesettings.waveviewmode)&&(scopesettings.triggermode == 1))||(scopesettings.long_mode)||(scopesettings.runstate)'. The port correctly discarded the stale previousytouch term but also dropped '!waveviewmode' from the triggermode clause: ours reads '(runstate == RUN_STATE_RUNNING) || long_mode || (triggermode == 1)'. P14's original only updated on 'runstate == RUN_STATE_RUNNING'. The TIME rotary IS routed in wave view (sm_button_dial_wave_view_handling, sm_1014d.c:972-1052 routes UIC_ROTARY_TIME_ADD/SUB to sm_set_time_base; the function's own waveview regime clamp lines 1430-1440 permits movement within the current regime). So when viewing a saved waveform whose stored triggermode is 1 (single), every TIME detent: overwrites scopesettings.samplerate — which in wave view holds the viewed buffer's acquisition rate that the code's own comment says must be preserved ('Otherwise the sample rate of the acquired buffer still is valid') and which feeds freq_calc_data/time_calc_data readouts and scope_calculate_sample_range_properties — pushes fpga_set_sample_rate to the live FPGA, forces runstate = RUN_STATE_RUNNING with ui_display_run_stop_text(), and calls scope_preset_values(), all while a file is on screen. Both references avoid this: Atlan4 via !waveviewmode, P14 via runstate-only.

**Evidence:** sm_1014d.c:1480 'if((scopesettings.runstate == RUN_STATE_RUNNING) || scopesettings.long_mode || (scopesettings.triggermode == 1))' followed by samplerate = time_per_div_sample_rate[...], fpga_set_sample_rate, 'scopesettings.runstate = RUN_STATE_RUNNING;' and scope_preset_values() (lines 1482-1499). Atlan4 reference Atlan4-1.00o5/fnirsi_1013d_scope/scope_functions.c:204 'if(((previousytouch < 240)&&(!scopesettings.waveviewmode)&&(scopesettings.triggermode == 1))||(scopesettings.long_mode)||(scopesettings.runstate))'. P14 original FNIRSI_1014D_Firmware/fnirsi_1014d_scope/statemachine.c (sm_set_time_base): 'if(scopesettings.runstate == RUN_STATE_RUNNING)' guards the samplerate update. Wave-view routing: sm_1014d.c sm_button_dial_wave_view_handling handles 'case UIC_ROTARY_TIME_ADD/SUB: sm_set_time_base();'.

**Suggested action:** Gate the triggermode clause on scope mode: '(runstate == RUN_STATE_RUNNING) || long_mode || ((triggermode == 1) && !scopesettings.waveviewmode)' — and consider whether the single-mode re-arm block (runstate = RUNNING + preset) should also be skipped in waveviewmode, matching both references.

**Verifier:** Independently reproduced every element. (1) sm_1014d.c:1480 reads '(scopesettings.runstate == RUN_STATE_RUNNING) || scopesettings.long_mode || (scopesettings.triggermode == 1)' — no !waveviewmode — followed by samplerate = time_per_div_sample_rate[...] (1483), fpga_set_sample_rate (1486), runstate = RUN_STATE_RUNNING + ui_display_run_stop_text under triggermode==1 (1489-1495), and scope_preset_values (1497). (2) Atlan4's reference condition at Atlan4-1.00o5/fnirsi_1013d_scope/scope_functions.c:222 carries '(previousytouch < 240)&&(!scopesettings.waveviewmode)&&(scopesettings.triggermode == 1)' — the port dropped BOTH the touch term (correctly) and !waveviewmode (the regression). (3) P14's original guards the samplerate update with runstate-only (reference statemachine.c:1389-1393), explicitly preserving the viewed buffer's rate. (4) The TIME rotary IS live in wave view: sm_button_dial_wave_view_handling routes UIC_ROTARY_TIME_ADD/SUB to sm_set_time_base (sm_1014d.c:1045-1048), and the function's own wave-view clamp (1430-1440) only restricts the regime, not the update. (5) The scenario is concrete: loading a wave file restores timeperdiv/samplerate/triggermode/long_mode from the file (scope_functions.c:5502ff via scope_restore_setup_from_file) and forces runstate=0/waveviewmode=1 (scope_functions.c:6250-6251); with stored triggermode==1 each detent then overwrites the viewed capture's samplerate (contradicting the code's own comment at 1478-1479), pushes it to the live FPGA, and re-arms — and since scope_acquire_trace_data gates only on runstate (scope_functions.c:752), the forced RUN_STATE_RUNNING starts a real conversion from the main loop (fnirsi_1013d_scope.c:412) that will overwrite the viewed trace buffers, arguably worse than the finding states (the long_mode clause additionally fires in wave view for long-timebase files even with triggermode!=1). git shows the condition was introduced by commit 77af34b replacing the P14 runstate-only guard, with no documented intent to drop wave-view protection.

*(from code-fpga)*

### V/div change never re-pushes the trigger level - the fifth F16-class handler

- **Where:** `fnirsi_1013d_scope/sm_1014d.c:1553`
- **Severity:** medium

sm_set_channel_sensitivity pushes fpga_set_channel_voltperdiv + fpga_set_channel_offset when running, but never fpga_set_trigger_level, even though the FPGA level is a function of samplevoltperdiv (level = ((trigverpos - dcoffset - traceposition) * 2097152) / signal_adjusters[voltperdiv] + 128, fpga_control.c:512). F16 established that in this tree's non-blocking acquire the level is only re-sent when arming, so any handler that changes the level mapping must push directly; the fix covered level rotary, trigger-channel toggle, 50% and origin (sm_1014d.c:1382, 867, 875, 1402) but not the sensitivity dial. Failure scenario: normal/single mode armed and waiting, turn the trigger channel's v/div toward less sensitive (e.g. 100mV->2V/div) — the waveform shrinks in ADC counts while the FPGA keeps the level computed for the old sensitivity, which now lies outside the signal; the conversion never completes and the scope sits frozen until the user happens to turn the level dial. pecostm32's identical handler was safe only because his acquire is blocking and re-arms (re-pushing level) after every user input; Atlan4's touch flow (match_volt_per_div_settings, statemachine.c:6578) has the same latent gap. PORT_AUDIT.md:698's sweep claim 'volt/div + trigger settings all push to the FPGA on change' is true for the v/div command itself but misses this derived-level staleness. (Smaller same-class residue: the F31 coupling fix at sm_1014d.c:2485-2493 zeroes dcoffset, which also shifts the level mapping without a level push; dcoffset is nonzero after AUTO on a DC-offset signal, scope_functions.c:3159/3218.)

**Evidence:** sm_1014d.c:1547-1560: 'if(scopesettings.runstate == RUN_STATE_RUNNING) { settings->samplevoltperdiv = settings->displayvoltperdiv; fpga_set_channel_voltperdiv(settings); fpga_set_channel_offset(settings); timer0_delay(50); }' — no fpga_set_trigger_level. fpga_control.c:489/499 use samplevoltperdiv in the level calc. Arming-time-only push: scope_functions.c:752-765 (fpga_set_trigger_level only inside the runstate&&display_data_done arm block). F16 precedent: PORT_AUDIT.md:347 and the pushes at sm_1014d.c:865-867, 874-875, 1378-1382, 1401-1402.

**Suggested action:** In sm_set_channel_sensitivity, after the FPGA v/div/offset push, re-push fpga_set_trigger_level() when the trigger is on this channel (the condition at sm_1014d.c:1539-1543 already exists); consider the same one-liner in the coupling case after dcoffset is zeroed.

**Verifier:** sm_1014d.c:1522-1562: sm_set_channel_sensitivity pushes fpga_set_channel_voltperdiv + fpga_set_channel_offset in the RUN_STATE_RUNNING branch and never fpga_set_trigger_level. fpga_set_trigger_level derives the FPGA level byte from samplevoltperdiv (fpga_control.c:489/499, formula at :512), so a v/div change invalidates the level last written to the FPGA. The arming-only push is verified at scope_functions.c:752-756 (fpga_set_trigger_level only inside the runstate && display_data_done arm block; fpga_done_conversion sends no level), matching PORT_AUDIT F16's own description (~line 347). The four F16 fixes exist exactly where cited (sm_1014d.c:865-867, 874-875, 1378-1382, 1401-1402) and the sensitivity dial is the one level-mapping-changing handler without a push — so in normal/single mode, armed and waiting, a v/div turn toward less sensitivity leaves the FPGA holding a level outside the shrunken signal span and the conversion never completes until another handler pushes the level. pecostm32's handler is byte-identical (his statemachine.c:1420-1459, no level push) but his blocking acquire re-pushes fpga_set_trigger_level at the top of every acquisition (his scope_functions.c:~35), so his gap self-heals; Atlan4's touch flow match_volt_per_div_settings (statemachine.c:6579) has the same latent gap. The PORT_AUDIT sweep-note quote ('volt/div + trigger settings all push to the FPGA on change') and the F31-coupling residue (sm_1014d.c:2485-2493 zeroes dcoffset, which enters the level formula at fpga_control.c:493/503, with dcoffset nonzero after AUTO per scope_functions.c:3159/3218) also check out.

*(from code-fpga)*

### Channel enable toggle leaves the FPGA trigger source on a disabled channel (fpga_swap_trigger_channel unreachable on 1014D)

- **Where:** `fnirsi_1013d_scope/sm_1014d.c:1511`
- **Severity:** medium

sm_toggle_channel_enable only flips settings->enable and repaints. Atlan4's proven 1013D touch flow calls fpga_swap_trigger_channel() + scope_calculate_trigger_vertical_position() on exactly this event, so the FPGA trigger source is moved off a channel being disabled; on the 1014D build fpga_swap_trigger_channel (fpga_control.c:440) has no callers at all (only statemachine.c:1868, 1013D touch path). Failure scenario: trigger on CH1, both channels on, user disables CH1 — FPGA 0x15 still selects CH1, whose front end no longer produces meaningful crossings; in normal/single mode the armed conversion never completes (F16-style freeze until the trigger-channel button is pressed), in auto mode the trigger never locks and software trigger processing (scope_process_trigger, scope_functions.c:924-932) keeps searching the stale channel1tracebuffer that fpga_read_sample_data no longer refreshes (enable guard at scope_functions.c:869/883). This is distinct from the already-documented decision not to push fpga_set_channel_enable at runtime (PORT_AUDIT F31 note / ROADMAP 24): that note covers the enable command only, not the trigger-source swap that Atlan4's reference flow performs.

**Evidence:** sm_1014d.c:1511-1518 (toggle + ui_display_channel_settings only). Atlan4-1.00o5/fnirsi_1013d_scope/statemachine.c:1864-1871: 'settings->enable ^= 1; //Update the trigger channel selection in the FPGA as needed fpga_swap_trigger_channel(); scope_calculate_trigger_vertical_position();'. Current-tree callers of fpga_swap_trigger_channel: only fnirsi_1013d_scope/statemachine.c:1868 (1013D touch). PORT_AUDIT.md:686 documents only the fpga_set_channel_enable aspect as a checked non-issue.

**Suggested action:** Mirror the Atlan4 touch flow in sm_toggle_channel_enable: call fpga_swap_trigger_channel() and scope_calculate_trigger_vertical_position() (plus a trigger display refresh) after toggling enable.

**Verifier:** The structural claims all verify: sm_1014d.c:1511-1518 only flips settings->enable and repaints; Atlan4's touch flow at Atlan4-1.00o5/statemachine.c:1864-1871 calls fpga_swap_trigger_channel() + scope_calculate_trigger_vertical_position() on exactly this event; the only caller of fpga_swap_trigger_channel (fpga_control.c:440) in this tree is statemachine.c:1868, reachable solely through the 1013D touch flow, so on the 1014D the FPGA 0x15 trigger source is never moved off a channel being disabled. The stale-buffer consequence is real: scope_functions.c:869/883 skip fpga_read_sample_data for a disabled channel while scope_process_trigger (scope_functions.c:924-932) keeps selecting that channel's frozen tracebuffer for display alignment. The distinction from the documented F31/ROADMAP-24 enable-command non-issue (PORT_AUDIT.md line 686) is also accurate. However the freeze half of the failure scenario is overstated — see correction.

**Correction:** Because fpga_set_channel_enable is pushed only at boot and in the clock search (fnirsi_1013d_scope.c:153-154, scope_functions.c:1595/1735 — never from the toggle, per the F31 note and stock-capture behavior), the FPGA-side CH1 front end and trigger comparator are unaffected by the UI disable: with a signal still on the disabled channel's BNC, hardware crossings continue and normal/single conversions still complete — no immediate F16-style freeze, and auto mode still locks. The demonstrable code-level consequences are (a) the trigger source silently stays on a now-invisible channel (Atlan4's flow would auto-move it), so pulling that probe later stalls normal/single with no visual cue, and (b) software trigger alignment runs against the stale tracebuffer, misaligning the visible channel's display. The never-completes/never-locks wording as an immediate result of the toggle is wrong; the after-reboot state (config saved with enable=0 and pushed at boot) is where a genuinely dead trigger source could materialize.

*(from code-fpga)*

### FPGA_NOTES claim '1014D build never sends 0x50-0x52' is false - fpga_on_off_generator sends them to the stock FPGA at every boot

- **Where:** `fnirsi_1013d_scope/fnirsi_1013d_scope.c:183`
- **Severity:** medium

main() unconditionally calls fpga_on_off_generator() at boot on both variants, and that function sends 0x51 (freq), 0x52 (duty) and 0x50 (output) in BOTH branches — even with gen_enable==0 it sends 0x51/100Hz, 0x52/0%, 0x50/0 (fpga_control.c:243-264). FPGA_NOTES.md states the generator commands are 'reachable only from the 1013D touch UI paths ... the 1014D build never sends them today', and builds the ROADMAP-14 boot-hang theory ('unknown command wedging the stock FPGA's parser') partly on that. The claim is stale: every 1014D boot already feeds the stock 0x1432 FPGA these three un-REd command bytes plus 4-byte payloads, and the scope demonstrably boots — which is itself useful evidence that 0x50-0x52 do NOT wedge the stock parser at boot, redirecting the scope_generator_settings() hang investigation. Inherited verbatim from Atlan4 main (Atlan4-1.00o5/fnirsi_1013d_scope.c:161) where it is harmless because Atlan4 targets replacement FPGAs that implement 0x50-0x52.

**Evidence:** fnirsi_1013d_scope.c:183 'fpga_on_off_generator();' with no PORT_1014D or fw_FPGA guard (context lines 153-191 all run on 1014D). fpga_control.c:254-264 else-branch: 'fpga_set_generator_freq(100); fpga_set_generator_duty(0); fpga_write_cmd(0x50); fpga_write_int(0);'. FPGA_NOTES.md:71-73: 'The generator commands are reachable only from the 1013D touch UI paths ... the 1014D build never sends them today'. pecostm32-RE command inventory ('FPGA explained.txt') has no 0x50-0x52; pecostm32's 1014D fpga_control.c never writes them.

**Suggested action:** Correct FPGA_NOTES.md (and the ROADMAP 14 hang hypothesis) to record that 0x50-0x52 are sent at every boot and tolerated; decide explicitly whether to guard fpga_on_off_generator() with fw_FPGA > 1 for stock-FPGA protocol purity.

**Verifier:** fnirsi_1013d_scope.c:183 calls fpga_on_off_generator() with no PORT_1014D or fw_FPGA guard (the surrounding init sequence 140-195 is unconditional; the first #if is PORT_A_KEYDEBUG at 197), and fpga_control.c:241-265 shows both branches send 0x51 (fpga_set_generator_freq, cmd at :278), 0x52 (fpga_set_generator_duty, cmd at :293) and 0x50 — the gen_enable==0 branch sends 0x51/100Hz, 0x52/0, 0x50/0. FPGA_NOTES.md:71-73 states verbatim that the generator commands are 'reachable only from the 1013D touch UI paths ... the 1014D build never sends them today' and lines 74-76 tie the scope_generator_settings boot hang to 'an unknown command wedging the stock FPGA's parser' — so the doc claim is factually false for the boot path. Inheritance verified: Atlan4-1.00o5/fnirsi_1013d_scope.c:161 has the same call, present since the vendor commit 9daa91f. pecostm32's 1014D fpga_control.c contains no 0x50-0x52 writes and the pecostm32-RE command inventory lists none. Since 1014D SD boot with traces is bench-confirmed working (CLAUDE.md known-issues section), the corollary — three un-REd commands hit the stock 0x1432 parser every boot without wedging it — follows and legitimately redirects the ROADMAP-14 hang theory.

*(from code-guards)*

### F25 FPGA 0x28 mode-select restore is 1014D-only; 1013D branch keeps the regression vs its own upstream

- **Where:** `fnirsi_1013d_scope/fpga_control.c:631`
- **Severity:** medium

The F25 fix (restoring the FPGA 0x29 dual-ADC + 0x28/0x00 mode-select in fpga_do_conversion for fw_FPGA==1) is wrapped in #if PORT_1014D, so the 1013D variant still omits the 0x28 write that Atlan4 commented out on the (audit-proven wrong) "0x28 not support" guess. pecostm32's 1013D upstream sends 0x28/0x00 unconditionally at exactly this point, so a 1013D-variant build on a stock FPGA retains the omission that F25 demonstrated causes the <=200ns/div dual-ADC interleave sawtooth on the 1014D. The in-code comment justifies restricting to fw_FPGA==1 vs the PECO paths but never explains excluding the 1013D; the evidence for the 1013D (its own upstream source) is stronger than the bus captures that justified the 1014D fix.

**Evidence:** fnirsi_1013d_scope/fpga_control.c:631-642: "#if PORT_1014D / if(fpgasettings.fw_FPGA == 1) { fpga_write_cmd(0x29); fpga_write_byte(0x01); fpga_write_cmd(0x28); fpga_write_byte(0x00); } #endif" with comment "Atlan4 commented these out on a wrong '0x28 not support' guess". Reference FNIRSI_1013D_Firmware/fnirsi_1013d_scope/fpga_control.c:494-496: "//Set the FPGA for short time base mode / fpga_write_cmd(0x28); / fpga_write_byte(0x00);". Atlan4-1.00o5/fnirsi_1013d_scope/fpga_control.c:622-624 shows the same lines commented out ("//fpga_write_cmd(0x28);").

**Suggested action:** Extend the 0x28/0x00 restore to the 1013D branch for fw_FPGA==1 (matching pecostm32's 1013D upstream, which sends only 0x28/0x00 here — keep the 0x29 dual-ADC byte 1014D-only since it comes from 1014D bus captures), or explicitly record the deliberate exclusion and its rationale in PORT_AUDIT.md so the 1013D bring-up checklist knows to expect the sawtooth.

**Verifier:** Independently reproduced all three citations: fnirsi_1013d_scope/fpga_control.c:631-642 wraps the F25 restore (0x29/0x01 + 0x28/0x00 under fw_FPGA==1) in #if PORT_1014D, so a PORT_1014D=0 build compiles it out; the pecostm32 1013D upstream (FNIRSI_1013D_Firmware/fnirsi_1013d_scope/fpga_control.c:494-496) sends 0x28/0x00 unconditionally at exactly this point in fpga_do_conversion (and sends no 0x29, matching the finding); Atlan4-1.00o5/fnirsi_1013d_scope/fpga_control.c:622-624 has the same writes commented out. So the 1013D variant genuinely retains Atlan4's omission that F25 proved wrong on the same stock-FPGA protocol (0x1432), against its own upstream's evidence. The in-code comment indeed only justifies fw_FPGA==1 vs the PECO paths and says nothing about the PORT_1014D guard.

**Correction:** One sub-claim needs tempering: the exclusion is not undocumented overall — PORT_AUDIT.md F25 (lines 638-639) explicitly records it as a deliberate choice ('#if PORT_1014D + fw_FPGA==1 so the untestable 1013D and the PECO fw_FPGA>1 paths are untouched'), i.e. conservatism about a never-hardware-tested variant, not an oversight. The 'never explains' statement is literally true only of the in-code comment, which does reference PORT_AUDIT.md F25. The retained-omission fact and the medium-severity latent hazard stand.

*(from code-guards)*

### CLAUDE.md module table misdocuments statemachine.c/PC_interface.c as 1013D-only; they are load-bearing, unguarded shared code in the 1014D build

- **Where:** `CLAUDE.md:179`
- **Severity:** medium

CLAUDE.md:179-180 lists menu.c, statemachine.c, touchpanel.c, DS3231.c, power_and_battery.c, generator.c, PC_interface.c as "1013D-only UI" and the imported modules as "1014D-only". In reality none of these files is compiled out in either variant (both variants build the identical object list), and two of the "1013D-only" files export utilities the 1014D build calls unconditionally: main() calls ini_SysParam() (PC_interface.c), the shared acquisition path calls match_volt_per_div_settings() (statemachine.c) from scope_functions.c, usb_interface.c calls usb_CDC_send_data() (PC_interface.c), and menu.c's export code uses my_strcpy/my_strcat/my_uint_to_str/usb_send_uint (PC_interface.c). Conversely uart.c, clock_synthesizer.c, and font_1.c ("1014D-only") carry no guards at all and are fully compiled and linked into 1013D images (the link at nbproject/Makefile-Debug.mk:107 has no -ffunction-sections/--gc-sections, so dead variant code ships in both images). Wrong-action risk: the repo's own documented remedy for variant files is the whole-file-guard-after-includes pattern (the F3/F4 lesson, applied to menu_1014d.c/sm_1014d.c/fonts_1014d.c) — applying it to statemachine.c or PC_interface.c per the table would break the 1014D link; the table also misleads audits like this one into treating those files as dead code on 1014D.

**Evidence:** CLAUDE.md:179-180: "1013D-only UI: `menu.c`, `statemachine.c`, ... `PC_interface.c`. 1014D-only: the imported table above." nbproject/Makefile-Debug.mk:38-82: single object list for both variants (statemachine.o, PC_interface.o, uart.o, clock_synthesizer.o, font_1.o all always built); line 107 link command has no --gc-sections. fnirsi_1013d_scope.c:279 "ini_SysParam();" (unguarded, in main) -> PC_interface.c:57 "void ini_SysParam(void)". scope_functions.c:99 "if(scopesettings.runstate) match_volt_per_div_settings(&scopesettings.channel1);" (unguarded; also 120, 158, 179, 2177-2178) -> defined in statemachine.c. usb_interface.c:1650 -> PC_interface.c usb_CDC_send_data. menu.c:9499-9501 my_uint_to_str/my_strcpy/my_strcat (unguarded). grep confirms statemachine.c, generator.c, PC_interface.c, uart.c, clock_synthesizer.c, font_1.c contain zero PORT_1014D occurrences.

**Suggested action:** Reword the CLAUDE.md module table to distinguish the three real mechanisms: (a) compiled-out whole-file guards (font_0/2/3/4.c vs fonts_1014d.c, menu_1014d.c, sm_1014d.c); (b) per-function-body stubs (touchpanel.c, DS3231.c, power_and_battery.c); (c) always-compiled shared files with variant-dead sections and shared utilities (menu.c, statemachine.c, generator.c, PC_interface.c, uart.c, clock_synthesizer.c, font_1.c) — and note explicitly that whole-file guards must NOT be added to group (c).

**Verifier:** Reproduced the CLAUDE.md:179-180 table text verbatim. nbproject/Makefile-Debug.mk has one variant-independent object list (PC_interface.o:39, font_1.o:50, generator.o:56, statemachine.o:75, clock_synthesizer.o:76, uart.o:77, touchpanel.o:80) and the link command (~line 107) has no --gc-sections (and CFLAGS no -ffunction-sections), so every object links fully into both images. Grep confirms zero PORT_1014D occurrences in statemachine.c, generator.c, PC_interface.c, uart.c, clock_synthesizer.c, font_1.c. Load-bearing cross-references verified: fnirsi_1013d_scope.c:279 calls ini_SysParam() unguarded in main (nearest guards end at line 227) -> PC_interface.c:57; scope_functions.c:99/120/158/179 (scope_adjust_timebase_and_voltdiv) and 2177-2178/2283 (scope_input_calibration) plus 3013+ (scope_do_auto_setup_new — both used on 1014D) call match_volt_per_div_settings -> statemachine.c:6579; usb_interface.c:1506-1516 calls usb_send_uint -> PC_interface.c:331; menu.c:9499-9501 (make_freq_to_text, unguarded — menu.c's PORT_1014D guards are all at/before line 2331) uses my_uint_to_str/my_strcpy/my_strcat -> PC_interface.c:510/409/425. Applying the repo's documented whole-file-guard remedy to statemachine.c or PC_interface.c per the table would therefore break the 1014D link — the wrong-action risk is real.

**Correction:** One evidence sub-item is inverted: usb_interface.c:1650 is the LIVE DEFINITION of usb_CDC_send_data; PC_interface.c's two copies (lines 196 and 235) are both inside /* */ comment blocks, so usb_interface.c does not call into PC_interface.c via that symbol. The same coupling exists anyway through usb_send_uint (usb_interface.c:1506-1516 -> PC_interface.c:331), so the conclusion is unaffected.

*(from code-gotchas)*

### uint16 time-cursor positions wrap: cursors jump to the opposite extreme

- **Where:** `fnirsi_1013d_scope/sm_1014d.c:292`
- **Severity:** medium

The time-cursor rotary handlers do `scopesettings.timecursor1position += speedvalue;` (line 292) and `timecursor2position += speedvalue;` (line 309) with wrap-then-clamp guards written for signed values. Both fields are uint16 in this tree (variables.h:804-805) but int16 in the pecostm32 source (reference variables.h:482). Left cursor at its minimum (6) with fast speed (-10) wraps to 65532, fails `< 6`, passes `>= timecursor2position`, and is set to timecursor2position-1 — it jumps rightward across the screen. Right cursor sitting just right of the left one wraps the same way, fails `<= timecursor1position` (65531 is not <=), passes `> 704`, and snaps to 704 — the far right edge. The volt-cursor handlers (326, 343) are safe because their minimum bounds (59) exceed the maximum step (10). Concrete failure: with time cursors enabled and fast move speed, dialing a cursor past its stop makes it leap to the opposite side instead of stopping. Port-introduced seam.

**Evidence:** sm_1014d.c:292-304 (left cursor: `< 6` then `>= timecursor2position` clamp to timecursor2position-1), 309-321 (right cursor: `<= timecursor1position` then `> 704` clamp to 704); variables.h:804-805 `uint16 timecursor1position; uint16 timecursor2position;`; pecostm32 reference variables.h:482 `int16 timecursor1position;`; speedvalue -10 via sm_1014d.c:121-122 with MOVE_SPEED_FAST=10.

**Suggested action:** Use a signed local for the adjusted value before clamping, or make the cursor position fields int16 as in pecostm32 (they are screen coordinates 6..704, well within int16).

**Verifier:** Reproduced from source: sm_1014d.c:292-304 (left cursor: '+= speedvalue', then '< 6' else '>= timecursor2position' -> set to timecursor2position-1) and 309-321 (right cursor: '<= timecursor1position' else '> 704' -> set to 704); both fields are uint16 here (variables.h:804-805) but int16 in the pecostm32 source (reference variables.h:482-483), and Atlan4's own uint16 (Atlan4 variables.h:633-634) shows the port kept Atlan4's types under pecostm32's signed-logic handlers. Left cursor at min 6 with speedvalue -10 wraps to 65532, fails <6, passes >=timecursor2position, and jumps to timecursor2position-1 (rightward across the screen). Right cursor wraps when both cursors sit near the left edge (timecursor2 in 7..9, reachable by dialing left cursor to 6 then right cursor down to 7): 7-10 wraps to 65533, fails <=timecursor1position, passes >704, snaps to 704. (The finding's example figure '65531' is off by 2 — the actual wrapped values are 65533..65535 — but the mechanism and failure are exactly as claimed.) The volt-cursor handlers at 326/343 are indeed wrap-safe: their reachable minimum values (>=59, and >=voltcursor1+1) minus the max step 10 never go below zero.

*(from code-gotchas)*

### USB CDC debug command 'f' sets totalsamples unbounded, causing a huge acquisition buffer overrun

- **Where:** `fnirsi_1013d_scope/usb_interface.c:1542`
- **Severity:** medium

The CDC debug parser accepts `f<number>` and assigns it raw: `case 'f': f = val; fpgasettings.totalsamples = f; break;`. totalsamples is uint16 (variables.h:844), so values up to 65535 stick. scope_functions.c:865-866 derive nofsamples = totalsamples/2, and fpga_read_adc_data (fpga_control.c:1072 `count = scopesettings.nofsamples;`) writes count samples at stride 2 (`settings->buffer += 2`, fpga_control.c:1389) starting at tracebuffer[1] then tracebuffer[0] (fpga_control.c:798/870) — touching totalsamples bytes of channelXtracebuffer, which is only 16380 bytes (uint32[4095], variables.c:207/212 with UINT32_SAMPLE_BUFFER_SIZE=16382/4). A host sending 'f60000' over the CDC port makes every subsequent acquisition frame write ~44KB past each channel buffer through adjacent .bss. Worse, totalsamples is saved in the checksummed config sector and restored on boot (scope_functions.c:8269), so the corruption persists across power cycles until a factory reset. Pre-existing: usb_interface.c is byte-identical to Atlan4-1.00o5 (diff verified), so this is Atlan4's debug back door, not a port regression — but the 1014D build carries it.

**Evidence:** usb_interface.c:1542 raw assignment inside the a..k debug parser (Slovak comments, Atlan4 origin; `diff -q usb_interface.c ../Atlan4-1.00o5/fnirsi_1013d_scope/usb_interface.c` reports identical); fpga_control.c:1072/1076/1156/1389 read loop writing *buffer with += 2 per sample; scope_functions.c:865-866 nofsamples/samplecount from totalsamples; variables.c:207 `uint32 channel1tracebuffer[UINT32_SAMPLE_BUFFER_SIZE]` = 16380 bytes; variables.h:844 `uint16 totalsamples;`; scope_functions.c:8269 restore from config sector.

**Suggested action:** Clamp the 'f' command (and the config-restore value at scope_functions.c:8269) to a safe maximum, e.g. totalsamples <= 16380 (or the FPGA-legal maximum), before it reaches nofsamples.

**Verifier:** Reproduced the full chain. usb_interface.c:1542 is verbatim `case 'f': f = val; fpgasettings.totalsamples = f; break;` with val from my_atoul and no bounds check, inside the a..k CDC parser that is wired to the EP2 RX interrupt path (usb_CDC_receive_callback called at usb_interface.c:1416); the file is byte-identical to Atlan4-1.00o5 (diff exit 0), confirming pre-existing Atlan4 origin. totalsamples is uint16 (variables.h:844) so 60000 sticks; scope_functions.c:865-866 recompute nofsamples = totalsamples/2 (uint32, variables.h:751) every acquisition; fpga_read_sample_data points settings->buffer (uint8*, variables.h:668-669) at tracebuffer[1] then [0] (fpga_control.c:798/870) and fpga_read_adc_data loops count = scopesettings.nofsamples (fpga_control.c:1072) storing one byte then 'settings->buffer += 2' per sample (~1386-1389) — two passes cover totalsamples bytes, i.e. 60000 bytes into channelXtracebuffer which is uint32[UINT32_SAMPLE_BUFFER_SIZE=4095] = 16380 bytes (variables.h:528-529, variables.c:207/212, tracebuffer pointers assigned at scope_functions.c:7396/7425) — ~43.6KB past each buffer. Persistence confirmed: saved at scope_functions.c:8052 inside scope_save_config_data (checksum at 8060-8068), restored at 8269 inside scope_restore_config_data after only a checksum+version check (8088) with no range validation; nothing else writes totalsamples except the default 3000 at 7828.

*(from code-gotchas)*

### MAX_SAMPLE_BUFFER_SIZE (16382) exceeds the 16380-byte uint32 trace arrays: 2-byte overruns

- **Where:** `fnirsi_1013d_scope/variables.h:529`
- **Severity:** medium

UINT32_SAMPLE_BUFFER_SIZE is defined as MAX_SAMPLE_BUFFER_SIZE/4 = 16382/4 = 4095 (floor), so every uint32[UINT32_SAMPLE_BUFFER_SIZE] trace array holds 16380 bytes, while byte-view loops and I/O run to 16382 (SAMPLE_COUNT == MAX_SAMPLE_BUFFER_SIZE). Verified out-of-bounds users: ref_and_math.c:145-155 compute_all_math_ops loops i < SAMPLE_COUNT reading A[i]/B[i] and WRITING math_tracebuffer[i] (points at math_channel_tracebuffer, 16380 bytes, scope_functions.c:7471) — a 2-byte write past the array every math frame; ref_and_math.c:102 `memcpy(dest->tracebuffer, src->tracebuffer, SAMPLE_COUNT)` reads and writes 2 bytes past both; ref_and_math.c:247-310 f_read of SAMPLE_COUNT bytes into ref1..8_tracebuffer (uint32[4095], variables.c:217-218) on reference-waveform load — 2 bytes past each. The overrun lands in the adjacent .bss buffer (2 bytes of sample data corrupted, no crash observed-class), but it is a real OOB write. Pre-existing: macros and ref_and_math.c are byte-identical to Atlan4-1.00o5 (diff verified).

**Evidence:** variables.h:528-529 `#define MAX_SAMPLE_BUFFER_SIZE 16382` / `#define UINT32_SAMPLE_BUFFER_SIZE (MAX_SAMPLE_BUFFER_SIZE / 4)` (integer floor -> 4095); variables.c:207/212/217-218/237 uint32[UINT32_SAMPLE_BUFFER_SIZE] declarations; ref_and_math.c:102, 145-155, 247-310 (SAMPLE_COUNT-sized byte accesses); Atlan4-1.00o5/fnirsi_1013d_scope/variables.h:383-384 identical; `diff ref_and_math.c ../Atlan4-1.00o5/.../ref_and_math.c` empty.

**Suggested action:** Define UINT32_SAMPLE_BUFFER_SIZE as ((MAX_SAMPLE_BUFFER_SIZE + 3) / 4) so the arrays round up to 16384 bytes; one-line fix, no behavior change for in-range data.

**Verifier:** Reproduced: variables.h:528-529 define MAX_SAMPLE_BUFFER_SIZE 16382 and UINT32_SAMPLE_BUFFER_SIZE (16382/4) which floors to 4095, so every uint32[UINT32_SAMPLE_BUFFER_SIZE] array holds 16380 bytes while SAMPLE_COUNT == 16382 (variables.h:531). Verified OOB users: ref_and_math.c copy_ch_to_ref does memcpy(dest->tracebuffer, src->tracebuffer, SAMPLE_COUNT) at line 102 (2-byte OOB read and write on 16380-byte buffers); compute_all_math_ops (ref_and_math.c:145-156) loops i < SAMPLE_COUNT writing scopesettings.math_tracebuffer[i], which points at math_channel_tracebuffer = uint32[4095] (scope_functions.c:7471, variables.c:237) — called from display_MATHx_data (ref_and_math.c:116) each math frame; the ref-waveform loads f_read(..., SAMPLE_COUNT, ...) into ref1..8_tracebuffer = uint32[4095] (ref_and_math.c:247-310, variables.c:217-226) — and the matching saves f_write SAMPLE_COUNT (416/431/446/461/475...) mean the files really do contain 16382 bytes, so the load writes the full 2 bytes past. Pre-existing confirmed: ref_and_math.c diffs empty against Atlan4-1.00o5, and Atlan4's variables.h:383-384 has the identical macros.

*(from doc-fpga-boot)*

### Command table still says 0x28 is dead — contradicts the doc's own SOLVED section and the code

- **Where:** `FPGA_NOTES.md:57`
- **Severity:** medium

The command-inventory row for 0x28 reads: '"short time base mode" — **dead**: Atlan4 comments it "0X28 not support in FPGA???" and never sends it'. That was true before 2026-07-12; the same document's SOLVED section (lines 190-204), PORT_AUDIT.md F25, and CLAUDE.md all record that 0x29/0x01 + 0x28/0x00 were restored on the stock fw_FPGA==1 path and bench-confirmed. The code sends 0x28 every conversion on the 1014D fw1 path. The table — the part a fresh session is most likely to consult as the protocol reference — was never updated, and 0x29 (dual-ADC mode) is missing from the inventory entirely.

**Evidence:** fnirsi_1013d_scope/fpga_control.c:631-641: `#if PORT_1014D / if(fpgasettings.fw_FPGA == 1) { fpga_write_cmd(0x29); fpga_write_byte(0x01); fpga_write_cmd(0x28); fpga_write_byte(0x00); }` inside fpga_do_conversion; only the roll-mode copy remains commented (line 599). FPGA_NOTES.md:190-204 (SOLVED section) says 'Restoring 0x29/0x01 + 0x28/0x00 ... eliminated the sawtooth'; commit 1a18ce8 (2026-07-12) 'restore FPGA 0x29/0x28 dual-ADC mode-select in do_conversion (F25)'.

**Suggested action:** Update the 0x28 table row to: sent every conversion on the fw1/1014D path since F25 (0x00 fast, 0x01 roll — roll copy still commented out), and add a 0x29 (dual-ADC mode) row.

**Verifier:** FPGA_NOTES.md:57 still reads '0x28 ... dead: Atlan4 comments it ... and never sends it', but fpga_control.c:631-641 sends 0x29/0x01 then 0x28/0x00 inside fpga_do_conversion on the PORT_1014D fw_FPGA==1 path (commit 1a18ce8, 2026-07-12, 'restore FPGA 0x29/0x28 dual-ADC mode-select in do_conversion (F25)'), with only the roll-mode copy still commented at lines 599-600. The doc's own SOLVED section (lines 190-204) records the restore, contradicting its own table. 0x29 appears nowhere in the command inventory (grep: sole mention is line 197 in the SOLVED prose).

*(from doc-fpga-boot)*

### 0x14 row documents dead code; trigger address is 13-bit, not 12-bit, in this tree

- **Where:** `FPGA_NOTES.md:53`
- **Severity:** medium

The 0x14 row says 'read trigger point (short & 0x0FFF, +2)'. In this tree that exact expression is commented out; the live code (inherited unchanged from pristine Atlan4, comment 'change 11.12.2025') is `data = fpga_read_short() & 0x1FFF` with no +2 — a 13-bit value. Line 104's '12-bit trigger address from 0x14' repeats the error: Atlan4's own protocol reference documents trigger_address[12:8]+[7:0] (13 bits) and 0x1F loading sample_read_offset[12:0]. The '& 0x0FFF, +2' form is pecostm32's official 1014D firmware, not this tree — but the doc's preamble (line 4) says everything was read out of this tree's fpga_control.c. Raw-readout/multi-sampling code written against the documented 12-bit/+2 contract would mis-mask bit 12 and be offset 2 samples versus the actual acqprobe/ringdump data.

**Evidence:** fnirsi_1013d_scope/fpga_control.c:741-746: `//data = (fpga_read_short() & 0x0FFF) + 2;` (commented) then live `data = (fpga_read_short() & 0x1FFF);  //change 11.12.2025`; identical in Atlan4-1.00o5/fnirsi_1013d_scope/fpga_control.c:694-707. 'Atlan4-FPGA/FPGA commands.txt': `8'h14 ... o_mcu_data <= trigger_address[12:8]` then `[7:0]`; `8'h1F ... sample_read_offset[12:8]`. pecostm32's variant: FNIRSI_1014D_Firmware/fnirsi_1014d_scope/fpga_control.c:555-556 `//Get the 12 bit location ... (fpga_read_short() & 0x0FFF) + 2`.

**Suggested action:** Correct the table row to `& 0x1FFF` (no +2) for this tree, note pecostm32's 0x0FFF+2 variant explicitly as reference-only, and change '12-bit trigger address' at line 104 to 13-bit register / mod-4096 ring behavior (the bench-confirmed wrap).

**Verifier:** fpga_control.c:743-744: the '(fpga_read_short() & 0x0FFF) + 2' form is commented out and the live code is '& 0x1FFF' with no +2 ('//change 11.12.2025'), identical in the Atlan4-1.00o5 reference (lines 701-706); the 0x0FFF/+2 form is live only in pecostm32's 1014D reference (fpga_control.c:555-558). Atlan4-FPGA/FPGA commands.txt — which documents the stock FPGA (its 0x06 returns 0x14/0x32 = version 0x1432) — gives trigger_address[12:8]+[7:0] and sample_read_offset[12:8], i.e. 13 bits. FPGA_NOTES.md:53 documents the dead 12-bit/+2 expression in a table headed 'as used in this tree' (and line 4 claims everything was read out of this tree), and line 104-105 repeats '12-bit trigger address from 0x14'.

*(from doc-fpga-boot)*

### Roll-mode 0x28/0x01 follow-up names the wrong function (also in PORT_AUDIT.md)

- **Where:** `FPGA_NOTES.md:201`
- **Severity:** medium

The SOLVED section's follow-up says 'restore roll-mode 0x28/0x01 in fpga_set_time_base (still commented)' (and line 193 similarly attributes the commented roll-mode write to fpga_set_time_base). The commented `0x28/0x01` actually lives in `fpga_set_long_timebase()`; `fpga_set_time_base()` contains no 0x28 at all, live or commented. PORT_AUDIT.md:643 repeats the misattribution ('the roll/long path (fpga_set_time_base, fpga_control.c:603)'). Since FPGA command writes are silicon-tied and must be placed exactly, a fresh session following the doc would grep fpga_set_time_base, find nothing, and either give up or patch the wrong function. The 'still commented' status itself is accurate.

**Evidence:** fnirsi_1013d_scope/fpga_control.c:557-572 = fpga_set_time_base (only 0x0E written); lines 576-601 = fpga_set_long_timebase containing `//fpga_write_cmd(0x28); //0X28 not support in FPGA??? / //fpga_write_byte(0x01);` at 599-600. PORT_AUDIT.md:643-644: 'the roll/long path (`fpga_set_time_base`, `fpga_control.c:603`) still has its `0x28/0x01` commented out'.

**Suggested action:** Change the function name to fpga_set_long_timebase() in both FPGA_NOTES.md (lines 193, 201-202) and PORT_AUDIT.md:643.

**Verifier:** fpga_control.c:557-572 = fpga_set_time_base, which writes only 0x0E (no 0x28, live or commented); the commented '//fpga_write_cmd(0x28); //0X28 not support in FPGA??? / //fpga_write_byte(0x01);' lives at lines 599-600 inside fpga_set_long_timebase (576-601), same placement as pristine Atlan4 (reference line 598, inside fpga_set_long_timebase). FPGA_NOTES.md:193 and 201-202 both attribute it to fpga_set_time_base, and PORT_AUDIT.md:643-644 repeats the misattribution ('the roll/long path (fpga_set_time_base, fpga_control.c:603)'). The 'still commented' status is accurate; only the function name is wrong.

*(from doc-fpga-boot)*

### Clock-search 'uncommitted' status labels are stale — committed 2026-07-10

- **Where:** `FPGA_NOTES.md:459`
- **Severity:** medium

Line 411 labels the auto clock search '(Grok Build 4.5, uncommitted)' and line 459 labels the decoupling restructure 'Restructured (uncommitted, hw-verify pending)'. Both were committed the same day the section is dated: commit 77af34b (2026-07-10, 'clock-search decoupling, GUI seam fixes F10-F17...') contains exactly this work, and the code is in the tree today. The doc was edited as recently as 2026-07-21 (ceeae17) without fixing the labels. A fresh session could hunt for uncommitted working-tree changes or wrongly fear the feature is unprotected.

**Evidence:** git log -S auto_detect_max_clean_sampling_clock → 77af34b (2026-07-10); commit message body: 'Base calibration no longer runs the Si5351 clock search; ... moved to Factory settings -> Sampling clock'. Code in tree: fnirsi_1013d_scope/scope_functions.c:1561 (auto_detect_max_clean_sampling_clock), sm_1014d.c:2701 (sm_open_clock_menu), sm_1014d.c:164 (NAV_CLOCK_MENU_HANDLING). FPGA_NOTES.md last committed in ceeae17 (2026-07-21).

**Suggested action:** Delete/replace both 'uncommitted' labels (lines 411 and 459) with 'committed 2026-07-10 (77af34b)'.

**Verifier:** FPGA_NOTES.md:411 says '(Grok Build 4.5, uncommitted)' and line 459 'Restructured (uncommitted, hw-verify pending)', but git log -S auto_detect_max_clean_sampling_clock shows the work landed in commit 77af34b (2026-07-10, 'clock-search decoupling, GUI seam fixes F10-F17...' — the commit message explicitly describes the Base-cal decoupling and Sampling clock menu), and the code is in the tree today (scope_functions.c:1561, sm_1014d.c:164/2701). The working tree has no uncommitted source changes (git status: only untracked fpga/ and tools/__pycache__/). FPGA_NOTES.md was last committed in ceeae17 (2026-07-21) with the stale labels intact.

*(from doc-fpga-boot)*

### samplerate==0 comp gate described as current — it was later removed (comp now unconditional)

- **Where:** `FPGA_NOTES.md:477`
- **Severity:** medium

The clock-search section presents 'Compensation apply gate in fpga_read_adc_data() corrected from timeperdiv >= 29 to samplerate == 0' as the fixed state, and the screenshot-analysis section (line 559) reasons from 'the samplerate==0 gate change is correct for measurements ... and neutral for display'. The gate no longer exists: per PORT_AUDIT F23 (commit b0daa6b, 'Apply comp at all rates (drop samplerate==0 gate)') the compensation in fpga_read_adc_data is now applied unconditionally at every rate, matching pecostm32. FPGA_NOTES never records the removal anywhere, so a session auditing interleave behavior at slow timebases would predict uncompensated data where the code compensates.

**Evidence:** fnirsi_1013d_scope/fpga_control.c:1087-1093: '//Compensate the value for ADC inequality ... Applied unconditionally at every rate, matching pecostm32's read path (... PORT_AUDIT.md F23). The old samplerate==0 gate left the sawtooth uncorrected on every timescale except the fastest'; `sample += settings->compensation;` with no rate condition. PORT_AUDIT.md F23 table row: 'Apply comp at all rates (drop samplerate==0 gate) | b0daa6b | no change (tested at rate 0)'. No `samplerate == 0` comp gate remains in fpga_control.c (grep).

**Suggested action:** Add a dated superseded-by note at lines 477-483 (and 555-560): the gate was subsequently dropped entirely (b0daa6b / F23); compensation now applies at all rates.

**Verifier:** fpga_control.c:1087-1093 applies 'sample += settings->compensation;' unconditionally, with a comment explicitly stating it is applied at every rate matching pecostm32 (PORT_AUDIT.md F23) and that 'the old samplerate==0 gate left the sawtooth uncorrected'; commit b0daa6b (2026-07-11) is 'apply interleave comp at all rates (drop samplerate==0 gate), match pecostm32', and PORT_AUDIT F23's table has the row 'Apply comp at all rates (drop samplerate==0 gate) | b0daa6b | no change (tested at rate 0)'. FPGA_NOTES.md mentions the gate only at lines 477-478 (as the corrected/current state) and 559 ('the samplerate==0 gate change is correct...'), and grep confirms it never records the removal (no F23/b0daa6b/'all rates' anywhere in the file).

*(from doc-fpga-boot)*

### 'sources tracked' for fpga/AL3_1014D/ is false — the entire fpga/ directory is untracked

- **Where:** `FPGA_NOTES.md:696`
- **Severity:** medium

The 1014D-retarget bullet says '(fpga/AL3_1014D/, sources tracked; artifacts + reports curated in out/)'. Nothing under fpga/ is tracked by git: `git ls-files fpga/` is empty and `git status` shows `?? fpga/`. The retarget sources (zaklad.v patch, zaklad_1014d.adc/.sdc, build.tcl/build.sh) and the built bitstream exist only as untracked local files — anyone relying on the doc would assume they are protected by git history when they are not (a real loss hazard for desk-derived pinout work).

**Evidence:** `git status --short -- fpga/` → `?? fpga/`; `git ls-files fpga/` → empty (only tools/acqprobe_analyze.py is tracked from the sibling claim). Files present on disk: fpga/AL3_1014D/{zaklad.v, zaklad_1014d.adc, zaklad_1014d.sdc, build.tcl, build.sh, out/FPGA_1014D.bin (282,898 B)}.

**Suggested action:** Either commit fpga/AL3_1014D sources (per the doc's stated intent) or correct the doc to 'untracked local' — and decide deliberately, since the .adc name-map is bespoke work.

**Verifier:** git status --short -- fpga/ returns '?? fpga/', git ls-files fpga/ is empty, and git log --all -- fpga/ is empty — nothing under fpga/ has ever been committed on any branch, so FPGA_NOTES.md:696's '(fpga/AL3_1014D/, sources tracked; artifacts + reports curated in out/)' is false. The files exist only on disk (fpga/AL3_1014D/{zaklad.v, zaklad_1014d.adc, zaklad_1014d.sdc, build.tcl, build.sh, out/FPGA_1014D.bin = 282,898 B}). The repo .gitignore even excludes only /fpga/AL3_1014D/build/ (line 27), showing intent to track the rest that was never executed with git add — the desk-derived pinout work is genuinely unprotected.

## Confirmed — low

*(from doc-claude-repo)*

### chmod gotcha is wrong on this branch: mksunxi/flashfilepacker are committed executable

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/CLAUDE.md:128`
- **Severity:** low

The gotcha 'Fresh checkout: chmod +x fnirsi_1013d_scope/mksunxi fnirsi_1013d_scope/flashfilepacker (committed non-executable)' is stale for atlan4-base: both tools have been committed mode 100755 since the Atlan4 vendor commit 9daa91f (2026-07-06), so a fresh checkout of this branch gets executable tools and the chmod is a no-op. The claim is only true on the pristine pecostm32 branches (main).

**Evidence:** git ls-files -s: '100755 … fnirsi_1013d_scope/flashfilepacker' and '100755 … fnirsi_1013d_scope/mksunxi'. git log -p --diff-filter=M on mksunxi: commit 9daa91f (2026-07-06) shows 'old mode 100644 / new mode 100755'; the only earlier commit is pecostm32's e1a3c00 (2022) initial upload.

**Suggested action:** Drop the gotcha or rescope it ('only needed on the pristine pecostm32 branches; atlan4-base commits them executable').

**Verifier:** git ls-files -s shows both fnirsi_1013d_scope/flashfilepacker and fnirsi_1013d_scope/mksunxi at mode 100755 in the current index. git log -p --follow on mksunxi shows the mode flip 'old mode 100644 / new mode 100755' in 9daa91f (2026-07-06, the Atlan4 vendor commit), with the only earlier touch being pecostm32's 2022 initial upload. So on atlan4-base a fresh checkout gets executable tools and the chmod is a no-op — the '(committed non-executable)' parenthetical was already wrong when the file was rewritten 2026-07-09, three days after 9daa91f. Correctly scoped as low: the stale advice is harmless (a redundant chmod), and the claim remains true only on the pristine pecostm32 branches.

*(from doc-claude-repo)*

### main() init-order description swapped in two places (SPI flash vs Si5351; USB vs config load)

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/CLAUDE.md:164`
- **Severity:** low

Lines 164–167 give the boot sequence as 'clock_synthesizer_setup() → SPI flash → fpga_init() → display → SD/FatFs → USB → scope_load_configuration_data()'. Actual order in main(): sys_spi_flash_init() comes BEFORE clock_synthesizer_setup(), and scope_load_configuration_data() comes BEFORE usb_device_init(). The load-bearing constraint stated (Si5351 must precede FPGA init) is correct in the code, so this is orientation noise, not a hazard — but a session reasoning about init dependencies (e.g. whether config load can use USB, or whether the clock-gen needs SPI flash) would start from a wrong sequence.

**Evidence:** fnirsi_1013d_scope/fnirsi_1013d_scope.c: sys_spi_flash_init() at line 75, clock_synthesizer_setup() at line 84 (inside #if PORT_1014D), fpga_init() at line 88, sys_init_display at line 97, f_mount at line 103, scope_load_configuration_data() at line 142, usb_device_init() at line 145.

**Suggested action:** Reorder the sequence to: SPI flash → clock_synthesizer_setup() (Si5351, must precede FPGA init) → fpga_init() → display → SD/FatFs → scope_load_configuration_data() → USB.

**Verifier:** fnirsi_1013d_scope.c main(): sys_spi_flash_init() at line 75 precedes clock_synthesizer_setup() at line 84 (inside #if PORT_1014D; the comment there explains the Si5351-before-FPGA constraint, which fpga_init() at line 88 honors); sys_init_display at 97, f_mount at 103, then scope_load_configuration_data() at 142 precedes usb_device_init() at 145. CLAUDE.md:164-166 states 'clock_synthesizer_setup() → SPI flash → … → USB → scope_load_configuration_data()' — both pairs swapped exactly as claimed. The load-bearing Si5351-before-fpga_init ordering is stated correctly, so low severity (orientation noise) is the right call.

*(from doc-claude-repo)*

### Key-documents blurb undersells PORT_AUDIT.md as 'findings F1–F9'; it now carries F1–F35 and three later fix passes

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/CLAUDE.md:87`
- **Severity:** low

The PORT_AUDIT.md bullet describes it as the '2026-07-09 full audit… findings F1–F9, the same-day fix pass (§5)'. The document is the living findings log: it now holds F1–F35 plus fix passes §5b (2026-07-10), §5c (2026-07-10 night), and the §5d GUI-glue audit. The Known-issues section does cite F12–F25 by number so a careful reader can infer more exists, but a session summarizing or searching the doc from this blurb would expect nine findings and one fix pass.

**Evidence:** CLAUDE.md:87–88 'findings F1–F9, the same-day fix pass (§5)'. PORT_AUDIT.md headings: line 267 '## 5b. Bench-feedback fix pass', line 297 '## 5c. Screenshot-driven fix pass', line 700 '## 5d. GUI-glue audit'; findings run through F35 at line 694.

**Suggested action:** Change the blurb to 'the living findings log (F1–F35 and counting) begun with the 2026-07-09 full audit; later fix passes §5b–§5d'.

**Verifier:** CLAUDE.md:87-88 describes PORT_AUDIT.md as 'findings F1–F9, the same-day fix pass (§5)'. PORT_AUDIT.md's actual heading structure: '## 5. Fix pass (2026-07-09…)' at line 240, '## 5b. Bench-feedback fix pass (2026-07-10…)' at line 267, '## 5c. Screenshot-driven fix pass (2026-07-10 night…)' at line 297, '## 5d. GUI-glue audit' at line 700, and findings running through F35 at line 694. The blurb undersells the document by ~26 findings and three fix passes. The finding fairly notes the partial mitigation (Known-issues cites F12–F25/§5c/§5d by number), so low severity is appropriate.

*(from doc-claude-repo)*

### 'Release is an unused host target' is wrong — it is an unused stale ARM/STM32 target

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/CLAUDE.md:108`
- **Severity:** low

The build section says 'config Debug is the ARM target (Release is an unused host target)'. Makefile-Release.mk actually uses the ARM cross-compiler (CC=arm-none-eabi-gcc) and links with a leftover STM32 linker script (stm32f103-64k.ld) — it is a stale, broken ARM target, not a host one. 'Unused' is the correct operative word, but a session wanting a host-side build (e.g. to unit-test table math) might reach for Release believing it targets the host.

**Evidence:** fnirsi_1013d_scope/nbproject/Makefile-Release.mk:17 'CC=arm-none-eabi-gcc'; line 100 links with '-T./stm32f103-64k.ld -nostdlib'.

**Suggested action:** Reword to 'Release is a stale, unusable leftover (ARM compiler + an STM32 linker script) — only Debug builds'.

**Verifier:** fnirsi_1013d_scope/nbproject/Makefile-Release.mk:17 sets CC=arm-none-eabi-gcc, and line 100 links with 'arm-none-eabi-gcc … -T./stm32f103-64k.ld -nostdlib' — an ARM cross-compile against a leftover STM32F103 linker script, not a host build (only the unused CCC/CXX=g++ entries are host compilers, irrelevant to this C project). CLAUDE.md:108's '(Release is an unused host target)' is therefore wrong about the target architecture while right about 'unused'. The failure scenario (reaching for Release expecting a host build for unit-testing table math) is plausible but minor; low severity fits.

*(from doc-memory)*

### dev-flash-workflow still labels the Factory-settings FEL entry 'hw-untested'

- **Where:** `/home/jsantala/.claude/projects/-home-jsantala-src-FNIRSI-1014D-Firmware/memory/dev-flash-workflow.md:27`
- **Severity:** low

The memory (last touched 2026-07-09) says FEL entry is 'Factory settings → FEL firmware update (direct BROM jump, added same day, hw-untested)'. The repo CLAUDE.md now states this route matter-of-factly as the way to enter FEL, and six weeks of subsequent bench cycles (F25 A/B work, acqprobe runs) relied on FEL loading; per the user's bench-report convention (unmentioned = working) the caveat has expired. Harm is mild (worst case: needless re-verification), but the stale caveat contradicts the checked-in doc.

**Evidence:** dev-flash-workflow.md:26-28 '…FEL firmware update** (direct BROM jump, added same day, hw-untested)'. Repo CLAUDE.md 'Loading onto the device': 'enter it via Factory settings → "FEL firmware update" (direct BROM jump from the running scope)' with no caveat; bench-report-convention.md records the unmentioned=fixed rule.

**Suggested action:** Drop 'added same day, hw-untested' from the memory.

**Verifier:** Citations verified: dev-flash-workflow.md:26-28 says 'Factory settings → FEL firmware update (direct BROM jump, added same day, hw-untested)'; repo CLAUDE.md:146 states the route with no caveat ('enter it via Factory settings → "FEL firmware update" (direct BROM jump from the running scope)'), and BOOT_NOTES.md states 'FEL entry today = Factory settings → FEL, or hold a key at power-on → F3' equally plainly; bench-report-convention.md records the unmentioned=fixed rule. Bench history shows the Factory settings menu heavily exercised on hardware after 2026-07-09 (PORT_AUDIT F27's 'Factory settings → Auto adjust → Base cal' retests, the Sampling-clock submenu bench passes, two acqprobe runs launched from Factory settings → Acquisition probe), with no failure report against the FEL item across six weeks. One nuance the finding missed, which does not overturn it: repo CLAUDE.md's Known-issues paragraph (line 198) — itself an explicitly dated 2026-07-09-state snapshot — also still lists 'Factory settings menu (defaults/reboot/FEL…)' under 'hardware verification pending', so the memory's caveat has a surviving in-repo counterpart and direct proof that the specific FEL menu item was exercised is nowhere recorded. That keeps this exactly at the finding's own assessment: low severity, worst case needless re-verification.

*(from doc-memory)*

### dev-flash-workflow duplicates repo CLAUDE.md's build/flash mechanics

- **Where:** `/home/jsantala/.claude/projects/-home-jsantala-src-FNIRSI-1014D-Firmware/memory/dev-flash-workflow.md:17`
- **Severity:** low

The 'How to apply' block (chmod +x mksunxi/flashfilepacker, Makefile variant/bootloader echoes, build-intended-variant-last, the FEL command with addresses, two-binary raw-vs-packed distinction, dd bs=1024 seek=8) restates repo CLAUDE.md's 'Building' and 'Loading onto the device' sections nearly line-for-line. Two owners of silicon-adjacent procedures will drift (the FEL-caveat drift above is an instance). The genuinely memory-worthy content is the laptop-side detail the repo doc doesn't carry: Windows/Cygwin-as-Administrator dd, /proc/partitions card ID, Zadig→WinUSB on VID 1f3a PID efe8, transfer via GitHub/CIFS.

**Evidence:** dev-flash-workflow.md:17-47 vs repo CLAUDE.md 'Building' ('chmod +x fnirsi_1013d_scope/mksunxi flashfilepacker', '[port_config.h variant: …]', '>>> BOOTLOADER: …', 'Never leave a wrong-variant build as the last artifact') and 'Loading onto the device' ('dd … bs=1024 seek=8', 'sunxi-fel -p write 0x7FFFFFE0 fnirsi_1013d_scope.bin exe 0x80000000').

**Suggested action:** Trim the memory to the Windows-laptop specifics plus a pointer to repo CLAUDE.md for the build/flash commands; repo CLAUDE.md owns those.

**Verifier:** Read both documents side by side. dev-flash-workflow.md:17-47 restates repo CLAUDE.md's Building section (chmod +x mksunxi/flashfilepacker committed non-executable; the '[port_config.h variant: …]' and '>>> BOOTLOADER: …' echoes; build-the-intended-variant-last) and Loading section (dd bs=1024 seek=8; `sunxi-fel -p write 0x7FFFFFE0 fnirsi_1013d_scope.bin exe 0x80000000`; the raw-vs-packed two-binary distinction via the dist/ artifacts paragraph) nearly line for line. The drift hazard is demonstrated by finding 4's FEL-caveat divergence between the two owners. The claimed uniquely-memory-worthy content is confirmed absent from the repo docs: grep for Zadig/Cygwin/WinUSB/1f3a/PhysicalDrive/proc-partitions across CLAUDE.md and BOOT_NOTES.md returns nothing, while dev-flash-workflow.md:33-47 carries all of it (Cygwin-as-Administrator dd, /proc/partitions card ID, Zadig→WinUSB on 1f3a:efe8, GitHub/CIFS transfer). Low severity duplication finding stands.

*(from doc-memory)*

### pecostm32-1014d-reference duplicates repo CLAUDE.md's reference-tree and import-mapping content

- **Where:** `/home/jsantala/.claude/projects/-home-jsantala-src-FNIRSI-1014D-Firmware/memory/pecostm32-1014d-reference.md:15`
- **Severity:** low

The vendored-checkout list, the not-imported FPGA/bootloader provenance ('md5-identical'/'hash-verified'), and the 24-vs-35-entry index-space contrast all restate repo CLAUDE.md ('Reference trees at repo root', the import table, and the timing-tables paragraph). Repo CLAUDE.md should own these (it does, and is committed). Unique memory-worthy bits worth keeping: the same-name nesting trip hazard (~/src/FNIRSI_1014D_Firmware/FNIRSI_1014D_Firmware/), the reference tree's inner git being dirty from the rebuild, and the .opencode/command style culture (rmslop/learn/commit — dir now also contains changelog.md, a trivial staleness).

**Evidence:** pecostm32-1014d-reference.md:15-31 vs repo CLAUDE.md 'Reference trees at repo root…' and 'pecostm32's 1014D used a 24-entry space without long timebases'. Verified in reference tree: FNIRSI_1014D_Firmware/fnirsi_1014d_scope/variables.c:246 'const char *time_div_texts[24]'. ls .opencode/command/ = changelog.md, commit.md, learn.md, rmslop.md.

**Suggested action:** Slim the memory to the unique operational hazards + pointer to repo CLAUDE.md; optionally add changelog to the command list.

**Verifier:** Verified all three legs. (1) Duplication: pecostm32-1014d-reference.md:15-31 restates repo CLAUDE.md's 'Reference trees at repo root' list, the bootloader provenance ('md5-identical' vs CLAUDE.md's 'hash-verified local rebuild'), the deliberately-not-imported FPGA note, and the 24-vs-35 index-space contrast (CLAUDE.md: 'pecostm32's 1014D used a 24-entry space without long timebases'). (2) The reference-tree fact checks out first-hand: FNIRSI_1014D_Firmware/fnirsi_1014d_scope/variables.c:246 is 'const char *time_div_texts[24]'. (3) The unique bits are genuinely absent from CLAUDE.md (the same-name nesting hazard ~/src/FNIRSI_1014D_Firmware/FNIRSI_1014D_Firmware/ and the dirty inner git are only in the memory), and ls .opencode/command/ = changelog.md, commit.md, learn.md, rmslop.md — the memory lists only rmslop/learn/commit, confirming the trivial staleness the finding noted. Low severity is appropriate.

*(from doc-memory)*

### MEMORY.md hook for user-terminal-misclicks omits the memory's own 'don't over-apply' quota-cutoff caveat

- **Where:** `/home/jsantala/.claude/projects/-home-jsantala-src-FNIRSI-1014D-Firmware/memory/MEMORY.md:7`
- **Severity:** low

The index line only says rejections may be accidental mis-clicks. The file's second half (added 2026-07-17) is the action-critical counterweight: 5-hour quota cutoffs look like a turn simply ending, are NOT mis-clicks or direction changes, and the rule is to only reason about mis-clicks when an actual denied tool call is in the transcript. An agent primed by the hook alone could misread a quota resume as a rejection-recovery situation — exactly what the caveat exists to prevent.

**Evidence:** MEMORY.md:7 'Terminal mis-clicks — tool rejections may be accidental (mouse capture on refocus); CLAUDE_CODE_DISABLE_MOUSE=1 suggested' vs user-terminal-misclicks.md:24-30 '**Don't over-apply (2026-07-17):** … Only reason about mis-clicks when there is an actual denied tool call in the transcript.'

**Suggested action:** Extend the hook: '…; but quota cutoffs mid-turn are NOT mis-clicks — only apply when a tool call was actually denied'.

**Verifier:** Both quotes verified verbatim. MEMORY.md:7 carries only the mis-click half ('tool rejections may be accidental (mouse capture on refocus); CLAUDE_CODE_DISABLE_MOUSE=1 suggested'). user-terminal-misclicks.md:24-30 contains the 'Don't over-apply (2026-07-17)' section: 5-hour quota cutoffs look like the turn simply ending (no denial result), are NOT mis-clicks or direction changes, and mis-click reasoning applies only when an actual denied tool call is in the transcript. The hook omits this counterweight entirely, and the failure mode is plausible: the hook alone primes 'rejections may be accidental' without the guard that bounds when to apply it. A one-line index can't carry everything, but the omitted half is specifically the part written to prevent misapplication of the first half — reasonable as low-severity index polish.

*(from doc-memory)*

### Stale pre-supersession PORT_A.md orphan at workspace root, unmentioned by workspace CLAUDE.md

- **Where:** `/home/jsantala/src/PORT_A.md:1`
- **Severity:** low

/home/jsantala/src/PORT_A.md (dated 2026-07-06) is an older divergent copy of the repo's PORT_A.md: it lacks the '⚠️ SUPERSEDED (2026-07-09)' banner the in-repo copy carries and still presents the deleted port_a.c/.h generation as the live effort ('so the effort can be picked back up'). The workspace CLAUDE.md was scrubbed of PORT_A-era content on 2026-07-09 but this sibling file was left behind and is not mentioned by it. An agent listing ~/src could read the un-superseded version and act on first-generation-port assumptions. The workspace CLAUDE.md's own claims all verify: FNIRSI_1014D_Firmware/ exists on branch atlan4-base; PORT_AUDIT/FPGA_NOTES/BOOT_NOTES/ROADMAP/AGENTS.md and the three named vendored checkouts all exist.

**Evidence:** md5sum differs (59e19b5… vs bef6c8e…); stat: 2026-07-06 05:06. diff of first 100 lines: repo copy adds the '⚠️ SUPERSEDED (2026-07-09)' block absent from the workspace copy. /home/jsantala/src/CLAUDE.md:12-13 mentions only the old CLAUDE.md duplication, not this file.

**Suggested action:** Delete ~/src/PORT_A.md (or prepend the same SUPERSEDED banner / note it in the workspace CLAUDE.md).

**Verifier:** Verified directly. /home/jsantala/src/PORT_A.md exists (14,794 B, mtime 2026-07-06 05:06), md5 59e19b58a6cd0a3efbfb32715fa61d5e vs the repo copy's bef6c8efb1dc91baae4dde10a75523ad — both hash prefixes match the finding's evidence. Head-to-head diff of the openings confirms the divergence: the in-repo copy carries the '⚠️ SUPERSEDED (2026-07-09)' banner block (pointing to atlan4-base, CLAUDE.md, PORT_AUDIT.md); the workspace copy lacks it and presents the deleted port_a.c/.h generation as resumable work ('so the effort can be picked back up'). The workspace CLAUDE.md (801 B, 2026-07-09) mentions only the replaced old CLAUDE.md duplication, never this sibling file, and its other claims (repo, branch, doc names, vendored trees) all check out. The file sits outside any read-only vendored tree, so it is in scope. An agent listing ~/src could plausibly read the un-superseded copy first. Low severity is right.

*(from doc-agents)*

### AGENTS.md chmod-on-fresh-checkout gotcha has a false premise: mksunxi/flashfilepacker are committed executable (100755) on this branch

- **Where:** `AGENTS.md:105`
- **Severity:** low

The gotcha says 'mksunxi / flashfilepacker Linux ELFs are committed mode 100644. On fresh checkout, chmod +x ...'. On atlan4-base both files have been committed mode 100755 since the Atlan4 vendor commit 9daa91f (2026-07-06), so a fresh checkout needs no chmod. The advice is harmless but the premise is wrong, and CLAUDE.md repeats the same stale claim ('committed non-executable') — worth fixing in both.

**Evidence:** git ls-files -s in fnirsi_1013d_scope/: '100755 ... flashfilepacker' and '100755 ... mksunxi'; git log -p for fnirsi_1013d_scope/mksunxi shows 'old mode 100644 / new mode 100755' in 9daa91f 'vendor: Atlan4 Fnirsi1013D v1.00o5'; on-disk: -rwxr-xr-x both. AGENTS.md:105 'committed mode 100644'.

**Suggested action:** Reword to: committed executable since 9daa91f — chmod only needed on the older branches (main/PORT_A); keep the rebuild-from-source fallback. Apply the same correction to CLAUDE.md's 'committed non-executable' line.

**Verifier:** git ls-files -s shows both fnirsi_1013d_scope/mksunxi and flashfilepacker committed at mode 100755 on this branch, and git show 9daa91f (2026-07-06, 'vendor: Atlan4 Fnirsi1013D v1.00o5') contains the 'old mode 100644 / new mode 100755' change for both files; on disk both are -rwxr-xr-x. AGENTS.md:105 claims 'committed mode 100644' requiring chmod +x on fresh checkout — false premise on atlan4-base (and CLAUDE.md's 'committed non-executable' repeats it). Harmless advice, wrong premise; low severity is right.

*(from doc-agents)*

### AGENTS.md Factory-settings and module-layout sections predate the 2026-07-10 additions (Sampling clock item, fonts_1014d.c)

- **Where:** `AGENTS.md:82`
- **Severity:** low

Two same-day (2026-07-10) changes never made it in: (1) lines 58 and 82 describe the Factory settings menu with three actions (Restore defaults / Reboot / FEL); a fourth item 'Sampling clock' (manual Si5351 CLK1 selection + auto clock search) was added and is documented in CLAUDE.md; (2) the Module layout section (lines 74-83) and the import discussion never mention fonts_1014d.c (P14 font_0/2/3/4, imported per F10 because the P14 layout needs P14 glyph metrics, with Atlan4's font_0/2/3/4.c guarded '#if !PORT_1014D') — a significant fact for anyone touching 1014D UI text.

**Evidence:** menu_1014d.c:5212 '"Sampling clock",' in the factory menu item list; sm_1014d.c:2698-2803 sampling-clock menu handlers (sm_set_sampling_clock, clock_menu_p1b, NAV_FACTORY_MENU_HANDLING return); PORT_AUDIT.md §5b F10 (fonts_1014d.c import, Atlan4 fonts guarded); AGENTS.md:58/82 list only three factory actions; grep 'fonts_1014d' AGENTS.md → no hits.

**Suggested action:** Add 'Sampling clock' to both factory-menu descriptions and a Module layout bullet for fonts_1014d.c/font_1.c (P14 glyph metrics; Atlan4 font_0/2/3/4.c compiled out on 1014D).

**Verifier:** Both omissions verified: (1) menu_1014d.c:5212 lists 'Sampling clock' as a factory-menu item and sm_1014d.c:2701-2843 implements the clock menu (sm_open_clock_menu, sm_set_sampling_clock, sm_handle_clock_menu_actions, NAV_CLOCK_MENU_HANDLING, clock_menu_p1b), while AGENTS.md:58 and :82 list only Restore defaults / Reboot / FEL (grep 'Sampling clock' in AGENTS.md: 0 hits); (2) fonts_1014d.c exists (added in 77af34b per its diffstat, 1152 lines, F10 in PORT_AUDIT.md:269), font_0.c carries '#if !PORT_1014D // the 1014D build uses the pecostm32 1014D fonts (fonts_1014d.c) instead', and grep 'fonts_1014d' in AGENTS.md returns 0 hits. Both changes are 2026-07-10, same day as AGENTS.md's last commit, which predates or missed them.

*(from doc-agents)*

### AGENTS.md names a vendored tree 'Atlan4/' that does not exist (actual: Atlan4-1.00o5/)

- **Where:** `AGENTS.md:113`
- **Severity:** low

The gotcha '`FNIRSI_1014D_Firmware/` and `Atlan4/` are vendored third-party trees' points at a nonexistent path: the Atlan4 reference checkout is Atlan4-1.00o5/ (and separately Atlan4-FPGA/). The list is also incomplete versus the current set of read-only reference trees (FNIRSI_1013D_Firmware/, pecostm32-RE/, 'Bootloader fw0.02...'), though CLAUDE.md covers those.

**Evidence:** ls of repo root: Atlan4-1.00o5/, Atlan4-FPGA/, FNIRSI_1013D_Firmware/, FNIRSI_1014D_Firmware/, pecostm32-RE/ — no Atlan4/. AGENTS.md:113 '**`Atlan4/`**'.

**Suggested action:** Rename to Atlan4-1.00o5/ and either enumerate the current reference trees or defer to CLAUDE.md's list.

**Verifier:** ls at repo root: Atlan4/ does not exist; the actual reference checkouts are Atlan4-1.00o5/ and Atlan4-FPGA/ (plus FNIRSI_1013D_Firmware/, FNIRSI_1014D_Firmware/, pecostm32-RE/, and 'Bootloader fw0.02 and fw0.03 and fw0.04/'). AGENTS.md:113 names '`Atlan4/`' as a vendored tree — a nonexistent path — and the list is incomplete versus the current reference set, as claimed.

*(from doc-agents)*

### AGENTS.md variables.c adjacency gotcha lists a stale declaration sequence

- **Where:** `AGENTS.md:119`
- **Severity:** low

The gotcha claims 'globaldisplaytext[50] → speedvalue (int8) → setvalue (int8) → navigationstate (uint8) → menuitem (int16) are declared consecutively'. In current variables.c six other state variables (fileviewstate, buttondialstate, enablesampling, sliderdata, onoffdata, onoffhighlighteditem) now sit between navigationstate and menuitem. The hazard itself (a globaldisplaytext overrun silently corrupts adjacent UI-state variables) is still real and arguably worse — more state now lives in the blast radius — but the specific sequence taught is no longer what the file shows.

**Evidence:** fnirsi_1013d_scope/variables.c:19-29: globaldisplaytext[50] (19), speedvalue (20), setvalue (21), navigationstate (22), fileviewstate (23), buttondialstate (24), enablesampling (25), sliderdata (26), onoffdata (27), onoffhighlighteditem (28), menuitem (29). AGENTS.md:119 lists menuitem as directly following navigationstate.

**Suggested action:** Update the sequence (or generalize: 'the 1014D UI state block at variables.c:19-34 sits directly after globaldisplaytext — any overrun corrupts it').

**Verifier:** variables.c:19-29 verified: globaldisplaytext[50] (19), speedvalue (20), setvalue (21), navigationstate (22), then fileviewstate (23), buttondialstate (24), enablesampling (25), sliderdata (26), onoffdata (27), onoffhighlighteditem (28), and only then menuitem (29) — six state variables now sit between navigationstate and menuitem, so AGENTS.md:119's taught sequence (menuitem directly after navigationstate) no longer matches the file. The underlying hazard (globaldisplaytext overrun corrupting adjacent UI state) remains plausible as described, so this is a stale-detail correction, not a retraction — low severity is right. (Pedantic caveat that doesn't affect the verdict: C declaration order doesn't strictly guarantee link-time adjacency, but the finding targets the doc's stated declaration sequence, which is verifiably stale.)

*(from doc-audit-roadmap)*

### PORT_AUDIT F7 names the wrong file: the implicit-decl wart was in menu.c, not PC_interface.c (contradicts its own §5 outcome row)

- **Where:** `PORT_AUDIT.md:183`
- **Severity:** low

F7's second bullet claims 'PC_interface.c calls usb_CDC_in_ep_callback() without including cdc_class.h — pre-existing upstream Atlan4 wart'. That is false: pristine Atlan4's PC_interface.c already includes cdc_class.h. The actual pre-existing implicit-declaration site was menu.c (calls usb_CDC_in_ep_callback at ~2202+ with no cdc_class.h include in Atlan4), which is exactly what the §5 outcome row says was fixed ('menu.c includes cdc_class.h'). So §2 and §5 give the same finding two different file attributions, and §2's is the wrong one. Also minor: §2's 'After fixing the first item, only the pre-existing one remains' is contradicted by §5's 'Zero implicit-declaration warnings remain' (both were fixed).

**Evidence:** PORT_AUDIT.md:180-188 (F7 text) vs PORT_AUDIT.md:250 (§5 row 'menu.c includes cdc_class.h (pre-existing upstream wart)'). Code: Atlan4-1.00o5/fnirsi_1013d_scope/PC_interface.c:25 already has '#include "cdc_class.h"'; Atlan4-1.00o5/fnirsi_1013d_scope/menu.c has no cdc_class include but working-tree fnirsi_1013d_scope/menu.c:22 adds it (calls at menu.c:2202, 2238, 2259, 2264); declaration at fnirsi_1013d_scope/cdc_class.h:52.

**Suggested action:** Correct F7's second bullet to name menu.c (and drop the now-false 'only the pre-existing one remains' sentence), so §2 and §5 agree.

**Verifier:** Fully reproduced. PORT_AUDIT.md:184-185 (F7 second bullet) claims PC_interface.c calls usb_CDC_in_ep_callback without including cdc_class.h as a 'pre-existing upstream Atlan4 wart' — but Atlan4-1.00o5/fnirsi_1013d_scope/PC_interface.c:25 already has #include "cdc_class.h" (and so did the working tree at the audited commit 4a5fa61, verified via git show). The real implicit-decl site was Atlan4's menu.c: no cdc_class include, 4 calls (2156/2192/2213/2218). The §5 outcome row (PORT_AUDIT.md:250) correctly names menu.c, which the working tree fixed (menu.c:22 include; calls at 2202/2238/2259/2264; declaration at cdc_class.h:52). §2's tail 'only the pre-existing one remains' (line 187-188) is also contradicted by §5's 'Zero implicit-declaration warnings remain'. §2 and §5 attribute the same wart to different files and §2's is wrong.

*(from doc-audit-roadmap)*

### ROADMAP multi-sampling background still calls the post-trigger-fill question 'open' while item 27 in the same section records it closed

- **Where:** `ROADMAP.md:159`
- **Severity:** low

The section-25-30 background paragraph says 'capture-geometry facts + open post-trigger-fill question in FPGA_NOTES §capture geometry', but item 27's own 2026-07-19 addendum a page later says the second acqprobe run 'closed the post-trigger fill boundary' (post-trigger fresh ≈2507, gap 680-1480 samples), item 29(b) uses the confirmed number, and FPGA_NOTES states 'The driven run closed the last open item — the post-trigger fill boundary'. Internal contradiction within one ROADMAP section.

**Evidence:** ROADMAP.md:159 ('open post-trigger-fill question') vs ROADMAP.md:197-203 ('Second run 2026-07-19 ... closed the post-trigger fill boundary ... post-trigger fresh ≈ 2507') and ROADMAP.md:228-230 ('confirmed ≈2500 post-trigger samples per ADC'); FPGA_NOTES.md:98 ('The driven run closed the last open item — the post-trigger fill boundary'); commit ceeae17 '1014d: post-trigger fill CLOSED'.

**Suggested action:** Change the background line to 'capture-geometry facts (post-trigger fill closed 2026-07-19) in FPGA_NOTES §capture geometry'.

**Verifier:** ROADMAP.md:159 says 'open post-trigger-fill question in FPGA_NOTES §capture geometry' while the same section's item 27 (ROADMAP.md:197-203) says the second 2026-07-19 run 'closed the post-trigger fill boundary' with post-trigger fresh ≈2507, and item 29(b) (ROADMAP.md:228-231) uses 'confirmed ≈2500 post-trigger samples per ADC'. FPGA_NOTES.md:98 states 'The driven run closed the last open item — the post-trigger fill boundary' (verified at exactly line 98). Commit ceeae17 '1014d: post-trigger fill CLOSED' exists. Internal contradiction within one ROADMAP section, exactly as claimed.

*(from doc-audit-roadmap)*

### PORT_AUDIT §5c header says 'F12–F15' but the section accreted F12–F35

- **Where:** `PORT_AUDIT.md:297`
- **Severity:** low

Section header '## 5c. Screenshot-driven fix pass (2026-07-10 night — F12–F15, both variants rebuilt clean)' now fronts a section containing F12 through F35 plus the 2026-07-16 sweep note and the 2026-07-17 generator note — entries dated up to 2026-07-17, well past the header's '2026-07-10 night' and 'F12–F15' scope. Anyone navigating by headers (or the CLAUDE.md pointer to 'findings F1–F34') gets a wrong map of where later findings live. Numbering itself is clean: F1..F35 continuous, no gaps or collisions.

**Evidence:** PORT_AUDIT.md:297 (header) vs F16 at :346, F25 at :617, F35 at :694, generator note at :696, sweep note dated 2026-07-16 at :698 — all inside §5c with no later section break until §5d at :700 (itself dated 2026-07-10).

**Suggested action:** Retitle §5c to a running bench-fix log (e.g. '5c. Bench-driven fix passes, 2026-07-10 → 2026-07-17 — F12–F35') or split the post-2026-07-12 entries into their own dated subsection.

**Verifier:** Core claim reproduced exactly: the §5c header at PORT_AUDIT.md:297 says '2026-07-10 night — F12–F15', yet the section runs through F16 (line 346), F25 (617), F35 (694), the 2026-07-17 generator note (696), and the 2026-07-16 sweep note (698), with no section break until §5d at line 700. Numbering F1..F35 is continuous with no gaps (F1-F9 in §2, F10/F11 in §5b, F12-F35 in §5c). One inaccuracy in the parenthetical, see correction.

**Correction:** The aside attributing 'findings F1–F34' to CLAUDE.md is wrong: the repo CLAUDE.md (line 88) says 'findings F1–F9' for PORT_AUDIT.md — itself stale in the other direction, but not the F1–F34 phrasing (that wording comes from the user's memory file / review prompt, not CLAUDE.md). The header-vs-content mismatch stands regardless.

*(from doc-audit-roadmap)*

### PORT_AUDIT §4 item 2 cites 'fix pass §6' for overlay compositing; §6 is the Reproduction appendix

- **Where:** `PORT_AUDIT.md:226`
- **Severity:** low

Checklist item 2 reads 'Overlay-menu compositing (fix pass §6)', but the compositing enhancement is documented at the end of §5 ('Enhancement — overlay-menu compositing (user request)'); §6 is '## 6. Reproduction appendix'. Broken internal cross-reference.

**Evidence:** PORT_AUDIT.md:226 vs PORT_AUDIT.md:255 ('**Enhancement — overlay-menu compositing (user request):**' inside §5) and PORT_AUDIT.md:716 ('## 6. Reproduction appendix').

**Suggested action:** Change '(fix pass §6)' to '(fix pass §5, Enhancement)'.

**Verifier:** PORT_AUDIT.md:225 (checklist item 2, spanning 225-227) reads 'Overlay-menu compositing (fix pass §6)'. §6 is '## 6. Reproduction appendix' (verified at line 716); the compositing enhancement actually lives at the end of §5 ('**Enhancement — overlay-menu compositing (user request):**', line 255). The internal cross-reference is broken as claimed (the cited line 226 is off by one — the '§6' text is on 225 — immaterial to the finding).

*(from doc-audit-roadmap)*

### F22's 'Fix (next session)' trim DC re-centre was never implemented and never explicitly retired

- **Where:** `PORT_AUDIT.md:482`
- **Severity:** low

F22 instructs: 'Fix (next session): mirror cal in the trim handler — when it changes adc1compensation by Δ, also add Δ × dcoffsetstep to dc_calibration_offset ... so the flattened trace re-centres on 0.' The current trim handler contains no dc_calibration_offset adjustment (it splits the difference symmetrically around a zero mean and its comment says the common mode 'is a DC shift the zero-level cal absorbs'). Unlike F22's cadence-pacing idea (explicitly superseded in the addendum) and the addendum's rate-0 revert (done, commit 3de7675), this instruction was neither built nor retired; F24 re-explains the same above-0 symptom and F30 now owns the DC-residual thread, but a reader of F22 cannot tell whether the re-centre is still planned.

**Evidence:** PORT_AUDIT.md:475-484 (the instruction) vs fnirsi_1013d_scope/sm_1014d.c:2833-2869 (sm_handle_trim_actions: only adc1compensation/adc2compensation touched, no dc_calibration_offset term); PORT_AUDIT.md:566-569 (F24 explanation of the lift) and :684 (F30 owns the DC residual, 'not yet root-caused').

**Suggested action:** Append one line to F22: the re-centre was superseded by the F25 fix (comps now ~0, lift negligible) and the residual-DC thread continues as F30 — or, if still wanted, move it to ROADMAP.

**Verifier:** PORT_AUDIT.md:482-484 instructs 'Fix (next session): mirror cal in the trim handler — when it changes adc1compensation by Δ, also add Δ × dcoffsetstep to dc_calibration_offset'. Verified never implemented: sm_handle_trim_actions (sm_1014d.c:2833-2869) touches only adc1compensation/adc2compensation with a symmetric split around zero, its comment (2853-2854) says the common mode 'is a DC shift the zero-level cal absorbs', and grep finds zero dc_calibration_offset references anywhere in sm_1014d.c. Verified never explicitly retired: the F22 addendum supersedes only the cadence-pacing experiment ('do not build it', line 499); F24 (566-569) re-explains the lift mechanism and F30 (684, 'not yet root-caused') owns the DC residual, but no text retires the re-centre instruction. The contrast with the addendum's handled items (rate-0 revert done in 3de7675, cadence explicitly killed) holds.

*(from doc-audit-roadmap)*

### ROADMAP 11 'Calibration track' unannotated although Base cal + per-ADC compensation substantially landed

- **Where:** `ROADMAP.md:60`
- **Severity:** low

Item 11 ('wire/verify the imported calibration UI ... per-ADC compensation on this unit's front end') carries no done-annotation, yet much of it shipped: Base calibration runs, persists its results (F19, commit ca45a7f 'restore Base calibration across power cycles'), and per-ADC interleave compensation exists with a manual trim UI (F20/F21, commits ee3fc97/87cf94b), using pecostm32's 2 MSa/s measurement (3de7675). Items 20/21/23 in the same file show the house style of in-place '*Done in code ...*' annotations; item 11 predates them and was never reconciled. The sector-708 framing is also known-moot on the 1014D (F19: the input-cal sector is 'written solely by the 1013D input-calibration procedure, never on a 1014D').

**Evidence:** ROADMAP.md:60-62 vs PORT_AUDIT.md:390-404 (F19), :410-418 (F20 trim), commits ca45a7f, ee3fc97, 87cf94b, 3de7675; PORT_AUDIT.md:396-397 (sector-708 never written on a 1014D).

**Suggested action:** Annotate item 11 with what landed (F19-F21 cal + trim, persistence) and narrow the remainder (e.g. any still-unwired CALIBRATION_STATE screens) or fold it into item 22.

**Verifier:** ROADMAP.md:60-62 (item 11) carries no done-annotation or strike, while items 20/21/23 in the same file (lines 129, 135-136, 142-143) show the in-place '*Done in code ...*' house style. The substance verifiably landed: F19 (PORT_AUDIT.md:390-404, commit ca45a7f) makes Base cal persist across power cycles; F20/F21 (PORT_AUDIT.md:406-418/:420+, commits ee3fc97/87cf94b) add per-ADC interleave compensation with a manual trim UI; 3de7675 adopts pecostm32's 2 MSa/s measurement. The sector-708 framing is moot on the 1014D per PORT_AUDIT.md:395-397 ('written solely by the 1013D input-calibration procedure, never on a 1014D'). All commits verified in git log.

*(from doc-audit-roadmap)*

### ROADMAP 12 roll-mode item omits F21's concrete verdict: the roll renderer is geometry-broken

- **Where:** `ROADMAP.md:63`
- **Severity:** low

Item 12 still reads as a fresh 'verify, then polish' task, but the verify step happened: the F21 bench session (2026-07-11) reached roll mode, retuned the roll/sweep boundary, and left a specific open defect — 'the roll renderer itself looks geometry-broken on the 1014D (trace crawling past the right edge + a trace-area shadow at the wrong location ...) — likely the same P14-vs-Atlan4 copy-rect geometry as F17, now only reachable at ≥500ms; needs its own pass'. The ROADMAP item is the natural home for that lead and doesn't carry it.

**Evidence:** ROADMAP.md:63-64 vs PORT_AUDIT.md:448-452 (F21 'Still open: the roll renderer ...').

**Suggested action:** Update item 12 with the F21 datapoint (boundary now ≤500 ms roll-only; renderer geometry defect suspected F17-class copy-rect) so the next roll session starts from the known state.

**Verifier:** ROADMAP.md:63-64 (item 12) still reads 'newly reachable from keys after the F1 fix; verify, then polish' with no mention of the F21 outcome. PORT_AUDIT.md:448-451 records the verify step's concrete verdict: 'Still open: the roll renderer itself looks geometry-broken on the 1014D (trace crawling past the right edge + a trace-area shadow at the wrong location at slow timebases) — likely the same P14-vs-Atlan4 copy-rect geometry as F17, now only reachable at ≥500ms; needs its own pass.' The ROADMAP item is the natural home for that lead and does not carry it — reproduced exactly.

*(from code-recent)*

### F28 decimal-resolution cap ignores the frozen-zoom rescale the value receives

- **Where:** `fnirsi_1013d_scope/menu_1014d.c:1797`
- **Severity:** low

Commit d2fa75c (F28): in ui_display_voltage the displayed value is rescaled by vertical_scaling_factors[displayvoltperdiv][samplevoltperdiv]/10000 when the two volt/div settings differ (stop-mode zoom, lines 1786-1790), but the new 'one ADC code' resolution at line 1797 is computed without that factor; ui_display_vavg has the same asymmetry (value rescaled at 1609-1613, resolution at 1619 not). The commit explicitly fixed this omission for the Vavg *value* ('caught in the pre-commit review') but not for the resolution. vertical_scaling_factors ratios span /100…*100, so under frozen zoom the decimal cap is off by the zoom ratio: zoomed-in readouts (display finer than sampled) get an over-large resolution and drop an honest decimal; zoomed-out readouts pass an under-sized resolution and print a decimal finer than one ADC code — the exact fabricated-precision class F28 set out to remove. Minor related imprecision: '(int32)signal_adjusters[...] >> VOLTAGE_SHIFTER' truncates 3.6-3.8M/2^21 = 1.72-1.81 to 1 (and 3.62 to 3), understating one ADC code by up to ~42% even unzoomed.

**Evidence:** menu_1014d.c:1782-1797: volts gets '(volts * vertical_scaling_factors[...])/10000' inside 'if(settings->displayvoltperdiv != settings->samplevoltperdiv)', then 'int32 resolution = ((int32)signal_adjusters[settings->samplevoltperdiv] >> VOLTAGE_SHIFTER) * vcd->mul_factor;' with no vsf term; same pattern at 1605-1619 in ui_display_vavg. variables.c:1071-1082 shows vertical_scaling_factors 100…50000 (ratios /100…*5 within shown rows); variables.c:991 signal_adjusters {3600000…7600000}, variables.h:297 VOLTAGE_SHIFTER 21.

**Suggested action:** Apply the same conditional rescale to resolution in both functions (resolution = resolution * vsf / 10000 when displayvoltperdiv != samplevoltperdiv), and optionally compute it as (signal_adjusters * mul_factor) >> VOLTAGE_SHIFTER to avoid the truncation. Only affects stop-mode zoomed readouts; live traces are unaffected.

**Verifier:** Asymmetry verified at the cited lines: menu_1014d.c:1786-1790 rescales the value by vertical_scaling_factors[displayvoltperdiv][samplevoltperdiv]/10000 when display != sample volt/div, while the resolution at :1797 is built from signal_adjusters[samplevoltperdiv]>>VOLTAGE_SHIFTER * vcd->mul_factor with no vsf term; identical pattern in ui_display_vavg (:1609-1613 vs :1619). Commit d2fa75c's message confirms the frozen-zoom vsf rescale was added to the Vavg value after being 'caught in the pre-commit review' — the resolution was not given the same treatment. ui_print_value:1883-1888 confirms the semantics (larger resolution drops more decimals), so an un-rescaled resolution is off by exactly the zoom ratio and one zoom direction fabricates sub-ADC-code decimals — the class F28 targeted. Table values verified: signal_adjusters {3600000..7600000} (variables.c:991), VOLTAGE_SHIFTER=21 (variables.h:297), 3600000>>21 = 1.716 -> 1 (~42 % understatement; 3.8M -> ~45 %), vsf ratios /100..*100 (variables.c:~1068+). One detail is inverted, see correction.

**Correction:** The finding's direction mapping is swapped. Verified by a physical-invariance check of the vsf indexing (sample 5V/div displayed at 1V/div -> vsf[display][sample] = 50000 = x5, which keeps the printed voltage constant): zoomed-IN (display finer than sampled) multiplies the value by vsf>1, so the un-rescaled resolution is UNDER-sized -> the cap loop under-caps and prints decimals finer than one ADC code (fabricated precision). Zoomed-OUT (display coarser) shrinks the value (vsf<1), so the passed resolution is OVER-large -> honest decimals get dropped. The finding states the opposite pairing; the defect, its magnitude (the zoom ratio), and the existence of both failure classes are all correct.

*(from code-recent)*

### Acqprobe phase B reads CH1 unconditionally; analyzer per-sample overhead assumes enabled channels only

- **Where:** `fnirsi_1013d_scope/scope_functions.c:2008`
- **Severity:** low

Commit 100f9ff: scope_do_acquisition_probe phase B calls fpga_read_sample_data(&scopesettings.channel1, data) unconditionally while channel 2 is gated on enable (lines 2010-2013) — unlike the standard path it claims to mirror (scope_acquire_trace_data gates CH1 at line 869). Commit 3de9c3d's analyzer computes the phase-B readout overhead per sample as overhead / (nch * samplecount) where nch counts enabled channels (tools/acqprobe_analyze.py:210-223). In the CH1-disabled + CH2-enabled configuration the firmware reads 2 channels (6000 samples) while the analyzer divides by 3000, overstating the µs/sample readout figure ~2x; with both channels disabled the firmware still reads CH1 while the analyzer counts 0 (guarded to print 0). Harmless in the bench runs so far (all CH1-on), but the derived stacking-budget numbers in FPGA_NOTES §capture geometry would be wrong if a CH1-off run is ever used.

**Evidence:** scope_functions.c:2008 'fpga_read_sample_data(&scopesettings.channel1, data);' with no enable guard vs :2010 'if(scopesettings.channel2.enable)'; contrast :869 'if(scopesettings.channel1.enable)' in scope_acquire_trace_data. tools/acqprobe_analyze.py:210-211 'nch = (1 if info.get("ch1_enable") else 0) + (1 if info.get("ch2_enable") else 0); rsamples = nch * info.get("samplecount", 3000)'.

**Suggested action:** Either gate the CH1 read on enable in the probe (matching the standard path being measured) or make the analyzer count CH1 as always-read (nch = 1 + ch2_enable); add a note that existing runs are unaffected (CH1 was enabled in all runs).

**Verifier:** All cited evidence reproduced: scope_functions.c:2008 calls fpga_read_sample_data(&scopesettings.channel1, data) with no enable guard while :2010-2013 gates channel 2 on enable, and the standard path gates CH1 at :869 (phase B's comment at :1995 explicitly claims to mirror scope_acquire_trace_data's translation, and FPGA_NOTES §Readout cost presents phase B as the standard-path cost). tools/acqprobe_analyze.py:210-211 computes nch from ch1_enable/ch2_enable and rsamples = nch*samplecount; :220-223 divides the phase-B overhead by rsamples (guarded 'if rsamples else 0'). So a CH1-off/CH2-on run reads 6000 samples while the analyzer divides by 3000, overstating us/sample ~2x, and a both-off run reads 3000 while the analyzer prints 0. Checked all six bench acqprobe_*.txt files: every run so far is ch1_enable=1 / ch2_enable=0, so the published 2.6-3.1 us/sample and stacking-budget numbers in FPGA_NOTES are unaffected — a latent hazard only, matching the low severity.

*(from code-seams)*

### Stale comment on sm_handle_trim_actions contradicts the symmetric-trim implementation

- **Where:** `fnirsi_1013d_scope/sm_1014d.c:2827`
- **Severity:** low

The header comment above sm_handle_trim_actions still describes the pre-F21(b) behavior: 'Only the ADC2 compensation moves, so the finest possible difference step is available; the half count of common mode this adds is invisible...'. The code below (lines 2855-2863) implements the F21(b) symmetric split documented in PORT_AUDIT.md ('The trim now moves the difference adc2-adc1 symmetrically ... instead of only bumping adc2'): adc1compensation = -(d/2); adc2compensation = d + adc1compensation. The inner comment at 2854 is correct; only the function header is stale. A future reader (or a cal/trim debugging session) acting on the header would mis-model what the trim writes.

**Evidence:** sm_1014d.c:2827-2830 comment 'Only the ADC2 compensation moves, so the finest possible difference step is available; the half count of common mode this adds is invisible next to the DC calibration offsets.' vs sm_1014d.c:2862-2863 'settings->adc1compensation = -(d / 2); settings->adc2compensation = d + settings->adc1compensation;'. PORT_AUDIT.md F21 item (b): 'The trim now moves the difference adc2-adc1 symmetrically (one count/detent, -5/+5 style) instead of only bumping adc2, matching cal.'

**Suggested action:** Rewrite the header comment to describe the symmetric split (difference adc2-adc1 adjusted one count per detent, split -(d/2)/+(d+adc1) so rows read like cal results), dropping the ADC2-only/common-mode sentences.

**Verifier:** Reproduced exactly. The function header comment at sm_1014d.c:2826-2831 still says 'Only the ADC2 compensation moves, so the finest possible difference step is available; the half count of common mode this adds is invisible next to the DC calibration offsets' — describing the pre-F21(b) behavior. The code at sm_1014d.c:2856-2863 implements the symmetric split: 'int32 d = settings->adc2compensation - settings->adc1compensation + setvalue; ... settings->adc1compensation = -(d / 2); settings->adc2compensation = d + settings->adc1compensation;' — both compensations move. The inner comment at 2853-2855 correctly describes the symmetric split ('keep it split symmetrically so the row reads like a cal result (-5/+5), matching what Base cal writes'), and PORT_AUDIT.md's F21 item (b) (lines 443-445: 'The trim now moves the difference adc2-adc1 symmetrically (one count/detent, -5/+5 style) instead of only bumping adc2, matching cal') confirms the header is stale rather than the code being wrong. Low severity is right: doc-only, but a reader debugging cal/trim off the header would mis-model what the trim writes.

*(from code-fpga)*

### Stale timeperdiv>=29 compensation gate in fpga_average_trace_data/_long is dead at every call site and contradicts the F23 doctrine

- **Where:** `fnirsi_1013d_scope/fpga_control.c:1456`
- **Severity:** low

Commit 77af34b added 'if (scopesettings.timeperdiv >= 29) datatmp += settings->adc1compensation;' to fpga_average_trace_data and fpga_average_trace_data_long, and its own commit message says the gate was 'corrected from timeperdiv>=29 to samplerate==0' — but only in fpga_read_adc_data; these two functions kept the stale >=29 form. Later (F23) the read-path comp became unconditional again ('applied unconditionally at every rate, matching pecostm32', fpga_control.c:1087-1092), leaving these functions triply inconsistent: Atlan4 applied comp unconditionally here, the F23 comment claims always-on, and the actual gate can never be true because fpga_average_trace_data is called only from the long-timebase path (test.c:745/751/952/957/1072-1089) where timeperdiv <= 10. Net effect: adc1compensation is silently never applied to roll-mode reads (possibly fine — 0x24/0x26 averages mix both ADCs — but that rationale is nowhere stated); fpga_average_trace_data_long has no callers at all.

**Evidence:** fpga_control.c:1455-1457 and 1502-1504: 'datatmp = FPGA_GET_DATA(); if (scopesettings.timeperdiv >= 29) datatmp += settings->adc1compensation;'. Atlan4-1.00o5/fpga_control.c (diff): 'datatmp = FPGA_GET_DATA() + settings->adc1compensation;' unconditional. git show 77af34b: 'Interleave compensation apply gate corrected from timeperdiv>=29 to samplerate==0'. Call sites all in test.c long-mode code; long mode is timeperdiv 0-10 (sm_1014d.c:1461).

**Suggested action:** Replace the dead condition with an explicit decision: either drop the comp add with a comment ('roll-mode 0x24/0x26 averages mix both ADCs, comp meaningless') or restore Atlan4's unconditional add; delete or mark fpga_average_trace_data_long as unused.

**Verifier:** fpga_control.c:1455-1457 and 1502-1504 carry the 'if (scopesettings.timeperdiv >= 29) datatmp += settings->adc1compensation;' gate in both average functions. git show 77af34b confirms the exact inconsistency: the commit message says 'Interleave compensation apply gate corrected from timeperdiv>=29 to samplerate==0', yet its diff applies samplerate==0 only in fpga_read_adc_data while adding the >=29 form to fpga_average_trace_data and _long; the pre-image (-) lines show Atlan4's unconditional 'FPGA_GET_DATA() + settings->adc1compensation'. The later F23 pass made the main read path unconditional again with a comment claiming comp is 'applied unconditionally at every rate, matching pecostm32' (fpga_control.c:1087-1093), leaving the triple inconsistency described. Dead-gate claim verified: fpga_average_trace_data's only callers are test.c:745/751/952/957 (scope_get_long_timebase_data/_1 read loops) and test.c:1072-1089 (scope_check_long_trigger), all reachable only via the long_mode route (fnirsi_1013d_scope.c:391-393), and long mode means timeperdiv < 11 (sm_1014d.c:1461-1463; roll boundary further tightened to <=6 at 1456-1457), so timeperdiv >= 29 is unreachable there and adc1compensation is silently never applied to roll reads. fpga_average_trace_data_long has zero callers (only the definition at fpga_control.c:1476 and the prototype at fpga_control.h:95).

*(from code-fpga)*

### fw_FPGA==1 never refreshes 0x0E per arm (pecostm32 did every arm); Base cal on the 1013D variant leaves 0x0E stale, and the 1014D build is only saved by a diagnostic-block side effect

- **Where:** `fnirsi_1013d_scope/fpga_control.c:688`
- **Severity:** low

Atlan4 moved the per-arm fpga_set_time_base out of scope_acquire_trace_data ('2.12 move to reset window', scope_functions.c:759) but the 'reset window' resend in fpga_do_conversion exists only under fw_FPGA > 1 (fpga_control.c:677-689), while pecostm32's 1014D re-sends 0x0E on every armed acquisition (his scope_functions.c:38). So on the stock FPGA the 0x0E auto-mode capture-window value is only correct as long as every code path that changes it restores it. scope_do_baseline_calibration ends by pushing only fpga_set_sample_rate (scope_functions.c:1145), leaving 0x0E at sample_rate_time_per_div[6] (the ~2MSa/s cal value) — on the 1014D build this is corrected only as a side effect of the PORT_1014D diagnostic block restoring saved_tp (scope_functions.c:1459-1463); on a 1013D-variant/stock-FPGA build (diagnostic compiled out, Atlan4-identical code) 0x0E stays stale after Base cal until the next timebase change, subtly changing auto-trigger free-run timing. Fragile: any future edit that removes or reorders the diagnostic block silently reintroduces the staleness on the 1014D too.

**Evidence:** scope_functions.c:759 '//fpga_set_time_base(scopesettings.timeperdiv); 2.12 move to reset window'; fpga_control.c:677-689 'if(fpgasettings.fw_FPGA > 1) { ... fpga_set_time_base(scopesettings.timeperdiv); }'; FNIRSI_1014D_Firmware/fnirsi_1014d_scope/scope_functions.c:38 'fpga_set_time_base(scopesettings.timeperdiv);' inside the per-arm block; scope_functions.c:1141-1145 cal-exit pushes (voltperdiv/offset x2 + sample rate, no time base), matching Atlan4-1.00o5/scope_functions.c:1091; the accidental restore at scope_functions.c:1446+1459-1463 (saved_tp = user timeperdiv, pushed back inside '#if PORT_1014D').

**Suggested action:** Add an explicit fpga_set_time_base(scopesettings.timeperdiv) beside the fpga_set_sample_rate at the end of scope_do_baseline_calibration (harmless on all paths), and note the fw1 no-per-arm-0x0E contract in FPGA_NOTES.

**Verifier:** Every link in the chain reproduced. (1) Per-arm 0x0E resend exists only under fw_FPGA>1: scope_functions.c:759 has Atlan4's '//fpga_set_time_base(...); 2.12 move to reset window' and the reset-window resend sits inside 'if(fpgasettings.fw_FPGA > 1)' (fpga_control.c:677-689; identical guard in Atlan4-1.00o5/fpga_control.c:643-654), while pecostm32's 1014D re-sends fpga_set_time_base(scopesettings.timeperdiv) inside every armed acquisition (his scope_functions.c ~38, verified in the arm block). (2) Base cal staleness: scope_do_channel_calibration's last unconditional 0x0E write is fpga_set_time_base(sample_rate_time_per_div[6]) at scope_functions.c:1366 (after the per-rate loop pushes at 1302-1305), and scope_do_baseline_calibration's exit restores voltperdiv/offset x2 + fpga_set_sample_rate only (scope_functions.c:1141-1145), byte-matching Atlan4-1.00o5/scope_functions.c:~1087-1091 — no time-base push. (3) The accidental save on 1014D: the diagnostic block at scope_functions.c:1445-1464 (saved_tp captured at 1446 while scopesettings.timeperdiv still holds the user value — cal never reassigns it, the reassignments at 1130-1135 are commented out) ends with fpga_set_time_base(saved_tp) at 1463, and that block is inside '#if PORT_1014D' (guard 1438, #endif 1465), so a 1013D-variant/stock-FPGA build compiles it out and leaves FPGA 0x0E at the cal value until the next timebase change. The fragility conclusion (any removal/reorder of the diagnostic block silently reintroduces the staleness on the 1014D) follows directly; low severity is right since the 1013D variant is untested on hardware and the 1014D is currently covered.

*(from code-guards)*

### 1014D get_fattime returns 0 — invalid FAT timestamp on every file the scope writes, diverging from the pecostm32 1014D reference

- **Where:** `fnirsi_1013d_scope/DS3231.c:379`
- **Severity:** low

The #else (PORT_1014D) branch of get_fattime returns 0. A packed FAT timestamp of 0 has month=0 and day=0, which is outside the valid FAT ranges (month 1-12, day 1-31), so every screenshot/waveform the 1014D saves gets an invalid directory timestamp that PCs render as a nonsense date. The behavior reference for anything 1014D (pecostm32's official firmware) instead returns a fixed valid timestamp (1349957149). FF_FS_NORTC is 0 (ffconf.h:240), so FatFs really calls this and stores the value verbatim.

**Evidence:** fnirsi_1013d_scope/DS3231.c:343-380: get_fattime body under #if !PORT_1014D with "#else return 0; #endif" (else at 378, return at 379). ffconf.h:240 "#define FF_FS_NORTC 0". Reference FNIRSI_1014D_Firmware/fnirsi_1014d_scope/diskio.c:197-201: "DWORD get_fattime (void) { //Some date and time value return(1349957149); }". Our diskio.c:197-203 copy is commented out (block comment), so DS3231.c's is the single live definition.

**Suggested action:** Return a fixed valid packed timestamp in the #else branch (e.g. the reference's 1349957149, or a build-date constant from build_time.h) instead of 0.

**Verifier:** All evidence reproduced: fnirsi_1013d_scope/DS3231.c:341-381 defines get_fattime with the body under #if !PORT_1014D and '#else return 0; #endif' (else at 378, return 0 at 379), so every 1014D build returns 0. ffconf.h:240 has FF_FS_NORTC 0 and line 11 FF_FS_READONLY 0, so FatFs (ff.c:298 GET_FATTIME() -> get_fattime()) really calls it on writes and stores the value in directory entries. A packed FAT value of 0 decodes to year 1980, month 0, day 0 — outside the valid FAT ranges (month 1-12, day 1-31), i.e. an invalid timestamp on every file the 1014D writes. The behavior reference FNIRSI_1014D_Firmware/fnirsi_1014d_scope/diskio.c:197-201 instead returns 1349957149, which decodes to the valid 2020-03-22 22:48:58. Our diskio.c copy (lines 197-203) is inside a /* */ block, so DS3231.c's is the single live definition (confirmed by grep: only DS3231.c:341 is live). Severity low is appropriate.

*(from code-guards)*

### Unguarded RTC display block in the 1014D main loop can paint over P14 chrome if onoffRTC is restored as 1, with no 1014D path to clear it

- **Where:** `fnirsi_1013d_scope/fnirsi_1013d_scope.c:454`
- **Severity:** low

The main-loop block "if (!(timerRTC)&&(onoffRTC)) { readtime(); ... display_fill_rect(TIME_TEXT_...); display_text(..., buffertime); }" has no variant guard. On the 1014D, readtime() is a compiled-out stub (DS3231.c:13), buffertime is never filled, and the block would black-fill the TIME_TEXT rect over the P14 layout every 500 ticks. It is dormant only because onoffRTC defaults to 0 (scope_functions.c:7838) — but onoffRTC is restored verbatim from the SD config sector (scope_functions.c:8171), whose format is shared with the Atlan4 1013D variant, and the only code that sets it to 1 is the 1013D touch toggle (statemachine.c:1479), unreachable on 1014D. So an SD card whose config sector was written by an Atlan4-1013D with RTC enabled leaves a 1014D permanently redrawing a black rectangle with empty text, with no on-device way to disable it short of a factory reset.

**Evidence:** fnirsi_1013d_scope.c:453-466: unguarded "//READ and show RTC time / if (!(timerRTC)&&(onoffRTC)) { timerRTC=500; readtime(); ... display_text(TIME_TEXT_XPOS, TIME_TEXT_YPOS, buffertime); }". DS3231.c:13-54: readtime() body entirely inside #if !PORT_1014D. scope_functions.c:8171 "onoffRTC = *ptr++;" (settings restore); scope_functions.c:7838 "onoffRTC = 0;" (preset). grep shows no onoffRTC writes in sm_1014d.c/menu_1014d.c; the only toggle is statemachine.c:1479 "onoffRTC ^= 1;" (1013D touch path).

**Suggested action:** Wrap the block in #if !PORT_1014D (matching the other RTC call sites' treatment), or force onoffRTC = 0 in the PORT_1014D settings-restore path in scope_functions.c.

**Verifier:** Structural claims all reproduced: fnirsi_1013d_scope.c:453-467 is outside the variant guard (its #endif is at line 449) — the RTC block runs in both variants' main loop; DS3231.c:11-55 shows readtime()'s entire body inside #if !PORT_1014D (empty stub on 1014D, so it links but never fills buffertime — variables.c:93 char buffertime[9], zero-init BSS); when onoffRTC==1 the block black-fills TIME_TEXT (123,3,48x13 per variables.h:383-386, inside the P14 top bar) every 500 ticks and draws an empty string. onoffRTC: preset 0 at scope_functions.c:7838, restored verbatim from the config sector at 8171 (scope_restore_config_data), saved at 7953 (so once 1 it persists), and grep over all sources confirms the ONLY code setting it to 1 is statemachine.c:1479 (touch config menu, unreachable on 1014D since touch_handler() is only called on the 1013D branch of the main loop). Factory 'Restore defaults' clears it via the preset — matching the finding's own 'short of a factory reset' qualifier.

**Correction:** The trigger scenario as worded is partially wrong: a config sector written by PRISTINE upstream Atlan4 1013D firmware (v1.00o5) would be REJECTED — scope_restore_config_data (scope_functions.c:8088) checks SETTING_SECTOR_VERSION, and Atlan4-1.00o5 uses VERSION_LOW 0x0015 while this tree uses 0x0016 (variables.h:123 vs Atlan4-1.00o5 variables.h:109), so onoffRTC would fall back to the preset 0. The scenario is live only for a sector written by THIS tree's own 1013D-variant build (PORT_1014D=0, same 0x0016 version, identical layout, toggle reachable via touch) whose SD card then boots a 1014D build. Latent hazard stands, but narrower than 'any Atlan4-1013D'.

*(from code-gotchas)*

### compute_all_math_ops int8 conversion is asymmetric: mul/div math modes produce sign-wrong results

- **Where:** `fnirsi_1013d_scope/ref_and_math.c:147`
- **Severity:** low

`int8 a = A[i];` wraps raw ADC bytes >=128 to negative (a mid-screen trace at ADC 128 becomes -128) while `int8 b = B[i]-128;` is properly centered. For add/sub (mathmode 1/2) the wrap cancels mod 256 when stored back into the uint8 math_tracebuffer, but for multiply (mode 3) and divide (mode 4) the operand sign matters: a trace sitting at/above mid-scale multiplies/divides as a large negative number, producing garbage. On the 1014D build mathmode is only reachable via restored configuration (no UI sets it; set-sites are in 1013D-only statemachine.c:2807-2925), so exposure is mainly the 1013D variant. Pre-existing: ref_and_math.c is byte-identical to Atlan4-1.00o5.

**Evidence:** ref_and_math.c:147-153 `int8 a = A[i]; int8 b = B[i]-128; ... result = (a * b); ... a / b`; A/B are uint8* into raw trace buffers (get_channel_data, ref_and_math.c:159-168); math_tracebuffer is uint8* (variables.h:748); empty diff vs Atlan4-1.00o5/fnirsi_1013d_scope/ref_and_math.c.

**Suggested action:** Center both operands the same way (int32 a = A[i]-128, b = B[i]-128), compute in int32, then offset/clamp into 0..255 when storing.

**Verifier:** Reproduced at ref_and_math.c:147-155: 'int8 a = A[i]; int8 b = B[i]-128;' with A/B being uint8* into raw trace buffers (get_channel_data at 159-168) and results stored to uint8 math_tracebuffer. int8 is signed char (types.h:11), so (int8)128 == -128 as claimed. The mod-256 analysis holds: for add, a+b === A + B - 128 (mod 256), which equals the properly-centered-and-recentered (A-128)+(B-128)+128; for sub likewise A-B+128 — so add/sub come out right despite the asymmetry. For multiply and divide the congruence does not carry through (mul differs by 128*b mod 256 for odd b vs centered operands, and integer division has no modular structure at all: e.g. A=192,b=10 gives -64/10=-6 vs centered 64/10=+6), so a trace at/above ADC 128 operates as a large negative operand — sign-wrong results as claimed. Exposure claim verified: mathmode is written only in statemachine.c:2807-2925 (1013D-only UI) and the config restore at scope_functions.c:8177; no sm_1014d.c/menu_1014d.c site sets it. Pre-existing confirmed: ref_and_math.c is byte-identical to Atlan4-1.00o5.

*(from code-gotchas)*

### X-Y mode indexes the trace buffer with disp_trigger_index-315 without a floor: negative-index OOB reads

- **Where:** `fnirsi_1013d_scope/scope_functions.c:3707`
- **Severity:** low

In X-Y display mode `uint32 index = disp_trigger_index - 315;` (and -317 in the thumbnail path at line 6963) underflows when the trigger is found early in the buffer (disp_trigger_index is simply the sample index where the trigger search matched, scope_functions.c:955, or fpgasettings.settriggerpoint at 3468). The wrapped uint32 converts back to a negative int32 in scope_get_x_sample/scope_get_y_sample (int32 index parameter, scope_functions.c:3801), which read `settings->tracebuffer[index]` with no bounds check — up to 315 bytes below the buffer. Outputs are clamped to the screen (3836-3848), so the effect is garbage pixels in the X-Y trace/thumbnail, not wild writes. Pre-existing: identical code in Atlan4-1.00o5 (scope_functions.c:2793 display path, 6014 thumbnail path).

**Evidence:** scope_functions.c:3707-3708, 6963-6964, 3801-3806 (unchecked tracebuffer[index]), 3836-3848 output clamps; Atlan4-1.00o5/fnirsi_1013d_scope/scope_functions.c:2793 and 6014 contain the same expressions.

**Suggested action:** Clamp index to >= 0 (or skip drawing) when disp_trigger_index < 315/317; cheap guard in both X-Y paths.

**Verifier:** The cited code is exactly as described and I verified it all: scope_functions.c:3707 'uint32 index = disp_trigger_index - 315;' (X-Y display), 6963 '- 317' (thumbnail), scope_get_x_sample/scope_get_y_sample take int32 index (3801/3853) and read settings->tracebuffer[index] with no bounds check (3806), outputs clamped to screen at 3836-3848 so the effect is bounded to garbage pixels from OOB reads; identical expressions exist in pristine Atlan4 (scope_functions.c:2793 and 6014), so pre-existing. The unchecked-underflow defect is real; only the stated activation path needs correcting (see correction), which does not change the low-severity latent-hazard conclusion.

**Correction:** The claimed trigger condition ('when the trigger is found early in the buffer') cannot occur in this tree or in pristine Atlan4: scope_process_trigger is called with fpgasettings.settriggerpoint (scope_functions.c:899) and scans only index = count-20 for 40 samples (938-940; same in Atlan4), so disp_trigger_index is always within +/-20 of settriggerpoint, whose only in-tree values are the default 1500 (7829), config restore (8270), and 1013D statemachine.c values samplecount/4 / /2 / -1000 (750/1500/2000 at default samplecount) — minimum ~730, far above 315. The underflow is reachable only through the unvalidated view-file/config restores (disp_trigger_index = ptr[index++] at scope_functions.c:5516 and menu_1014d.c:3317), i.e. a corrupt or hand-crafted file on the SD card, not normal acquisition.

*(from code-gotchas)*

### uart1_receive_data blocks forever on the key controller every main-loop iteration

- **Where:** `fnirsi_1013d_scope/uart.c:54`
- **Severity:** low

uart1_receive_data() spins on `(*UART1_LS_REG & UART_LSR_TEMT) == 0` (line 48) and then on `(*UART1_LS_REG & UART_LSR_DR) == 0` (line 54) with no timeout. It is called every main-loop pass via sm_handle_user_input -> uart1_get_user_input (sm_1014d.c:72, fnirsi_1013d_scope.c main loop), because the protocol is poll-response (write 0xFF, wait for the controller's answer, even when no key is pressed). If the front-panel key-controller MCU is absent, held in reset, or wedged, the DR wait never completes and the entire scope (acquisition + display, not just input) freezes with no recovery. uart1_wait_for_user_input (line 81) has the same property at its call sites (calibration prompts, screenshot viewer). Provenance: uart.c is byte-identical (whitespace only) to pecostm32's official 1014D firmware, and Atlan4 has no uart.c at all — so this is inherited reference behavior of the import source, not a port regression; CLAUDE.md already documents the blocking poll. Reported for completeness of the hazard class.

**Evidence:** uart.c:45-58 (both unbounded while loops), uart.c:78-82; sm_1014d.c:69-76 calls uart1_get_user_input each main-loop pass; `diff uart.c ../FNIRSI_1014D_Firmware/fnirsi_1014d_scope/uart.c` shows a single whitespace-only difference; no uart.c exists in Atlan4-1.00o5/fnirsi_1013d_scope/.

**Suggested action:** If robustness is wanted beyond pecostm32 parity, bound the DR wait with a counter timeout (like the FPGA ready waits at fpga_control.c:653-671) and return 0 (no key) on expiry; otherwise leave as documented reference behavior.

**Verifier:** Reproduced: uart1_receive_data (uart.c:45-58) spins unbounded on UART_LSR_TEMT (line 48), writes the 0xFF poll byte, then spins unbounded on UART_LSR_DR (line 54); uart1_wait_for_user_input (78-82) loops on uart1_receive_data. Called every main-loop pass: fnirsi_1013d_scope.c:441 sm_handle_user_input -> uart1_get_user_input (sm_1014d.c:69-76) -> uart1_receive_data, so an absent/wedged key-controller MCU stalls acquisition and display with no timeout or recovery; uart1_wait_for_user_input call sites in scope_functions.c:1190/1954/2117 and menu_1014d.c:204/5049/5097 have the same property. Provenance verified: diff against the pecostm32 reference uart.c shows exactly one whitespace-only difference (line 71), and Atlan4-1.00o5/fnirsi_1013d_scope/ has no uart.c — inherited reference behavior, already documented as a blocking poll in CLAUDE.md. The finding's own framing (low severity, hazard-class completeness, not a port regression) is accurate.

*(from code-gotchas)*

### display_character(int8) sign-extends codes >= 128: the degree-sign glyph (176) never renders

- **Where:** `fnirsi_1013d_scope/display_lib.c:1484`
- **Severity:** low

display_character takes `int8 text` and casts `(uint16)text` (lines 1494/1499): any character code >= 128 becomes 0xFF80+, so check_char_in_vw_font's range test (display_lib.c:1608-1615, first_char/last_char) fails and nothing is drawn. menu.c:6838 passes literal 176 (0xB0, degree sign) for the generator menu — int8 receives -80, lookup gets 0xFFB0, glyph silently dropped. display_text (line 1505) is immune because plain char is unsigned on this target (no -fsigned-char in CFLAGS, nbproject/Makefile-Debug.mk:86). menu.c is 1013D-variant-only code and the same signature/call exist in pristine Atlan4 (display_lib.c:1488, menu.c:6787), so this is pre-existing; it becomes visible only if/when the 1013D variant is hardware-tested.

**Evidence:** display_lib.c:1484-1500 (`int8 text`, `(uint16)text` casts), 1608-1615 range check; menu.c:6838 `display_character(..., 176)`; Makefile-Debug.mk:86 CFLAGS has neither -fsigned-char nor -funsigned-char (arm-none-eabi default: unsigned char); Atlan4-1.00o5 display_lib.c:1488 and menu.c:6787 identical.

**Suggested action:** Change the parameter to uint8 (or cast via uint8 before widening to uint16) so codes 128-255 reach the font lookup intact.

**Verifier:** Reproduced: display_character(uint32, uint32, int8 text) at display_lib.c:1484 casts '(uint16)text' at 1494/1499; int8 is signed char (types.h:11), so a literal 176 arrives as -80 and sign-extends to 0xFFB0. check_char_in_vw_font (display_lib.c:1607-1615) tests character >= first_char && <= last_char per range; font_3 — the font active at the call (display_set_font(&font_3) at menu.c:6811) — has ranges 0x20-0x7E and 0xA0-0xFF (font_3.c:412/416), so the 0xB0 degree glyph EXISTS in the font but 0xFFB0 fails both ranges and nothing is drawn: the defect is real, not moot. menu.c:6838 passes literal 176 exactly as cited (generator menu, 1013D-variant UI). display_text (1505) is immune as claimed: it dereferences plain char, CFLAGS (nbproject/Makefile-Debug.mk:86) has neither -fsigned-char nor -funsigned-char, and arm-none-eabi-gcc defaults char to unsigned. Pre-existing confirmed: Atlan4-1.00o5 has the identical display_character signature (display_lib.c:1488) and the identical call (menu.c:6787).

*(from doc-fpga-boot)*

### Stale path: fpga/zaklad_1014d.adc is actually fpga/AL3_1014D/zaklad_1014d.adc

- **Where:** `FPGA_NOTES.md:342`
- **Severity:** low

Line 342 ('The retarget pinout now exists: fpga/zaklad_1014d.adc + fpga/README.md') and line 672 ('the 1014D retarget via fpga/zaklad_1014d.adc') point at fpga/ root. The file lives at fpga/AL3_1014D/zaklad_1014d.adc; fpga/ root contains only README.md and the AL3_1014D/ directory. The doc's own later section (line 696) uses the correct directory.

**Evidence:** `ls fpga/` → AL3_1014D/, README.md only; `ls fpga/AL3_1014D/` → zaklad_1014d.adc, zaklad_1014d.sdc, zaklad.v, build.tcl, build.sh, out/. fpga/README.md:18 documents zaklad_1014d.adc inside AL3_1014D/.

**Suggested action:** Fix both path references to fpga/AL3_1014D/zaklad_1014d.adc.

**Verifier:** FPGA_NOTES.md:342 ('fpga/zaklad_1014d.adc + fpga/README.md') and 672 ('via fpga/zaklad_1014d.adc') point at fpga/ root, but ls fpga/ shows only AL3_1014D/ and README.md — the .adc lives at fpga/AL3_1014D/zaklad_1014d.adc, documented as such in fpga/README.md (project-layout section) and referenced with the correct directory by the doc's own line 696. The fpga/README.md half of line 342 is correct; only the .adc path is stale.

*(from doc-fpga-boot)*

### Acquisition-probe bullet still headed 'bench run pending' after the runs were analyzed

- **Where:** `FPGA_NOTES.md:165`
- **Severity:** low

The bullet opens '**Measurement probe exists (2026-07-17, bench run pending):**' while the same bullet (lines 170-179) records 'First bench run analyzed 2026-07-19' and the doc's capture-geometry section is built on two analyzed 2026-07-19 runs (commits 3de9c3d, ceeae17). The heading label was never updated — a status label that now contradicts the body it heads.

**Evidence:** FPGA_NOTES.md:165 vs 170-171; git log: 3de9c3d (2026-07-19) 'acqprobe run 1 analyzed', ceeae17 'post-trigger fill CLOSED -- driven hum run'; bench/acqprobe/ contains 2026-07-19-no-probes/ and 2026-07-19-trigger-probe/.

**Suggested action:** Change the parenthetical to '(added 2026-07-17; runs analyzed 2026-07-19)'.

**Verifier:** FPGA_NOTES.md:165 opens the bullet with 'Measurement probe exists (2026-07-17, bench run pending):' while line 171 in the same bullet says 'First bench run analyzed 2026-07-19' and the capture-geometry section above it is built on the two analyzed runs. git shows 3de9c3d (2026-07-19, 'acqprobe run 1 analyzed') and ceeae17 (2026-07-21, 'post-trigger fill CLOSED -- driven hum run'), and bench/acqprobe/ contains 2026-07-19-no-probes/ and 2026-07-19-trigger-probe/. The heading label contradicts the body it heads.

*(from doc-fpga-boot)*

### 0x3C is never sent by this tree — the 'init write' is fully commented out

- **Where:** `FPGA_NOTES.md:59`
- **Severity:** low

The command table (headed 'as used in this tree') lists 0x3C as a '"battery level"-ish init write (short 32431), purpose still unclear', and the special-IC section (lines 371-372) says 'our init's odd 0x3C write is this handshake's vestige' — both implying the write happens. The only writer, fpga_set_battery_level(), is wrapped in a block comment and its sole call site in main() is also commented out (same in pristine Atlan4), so this tree never sends 0x3C. Unlike the 0x28 row, no dead-code marker is present. Also, 'purpose still unclear' at line 59 is superseded by the doc's own special-IC decode (touch-panel-address loopback / anti-clone handshake).

**Evidence:** fnirsi_1013d_scope/fpga_control.c:1526-1535: `/* void fpga_set_battery_level(void) { fpga_write_cmd(0x3C); fpga_write_short(32431); } */`; fnirsi_1013d_scope.c:187-188 `//fpga_set_battery_level(); //Only called here and in hardware check`; fpga_control.h:97 prototype commented. grep for other 0x3C writes in fnirsi_1013d_scope/*.c finds none.

**Suggested action:** Mark the 0x3C row as dead/commented-out in this tree and cross-reference the special-IC section for its decoded purpose.

**Verifier:** The only 0x3C command writer, fpga_set_battery_level(), is wrapped in a /* */ block comment (fpga_control.c:1526-1535), its sole call site is commented (fnirsi_1013d_scope.c:188 '//fpga_set_battery_level();'), and the prototype is commented (fpga_control.h:97) — identical in pristine Atlan4 (reference fpga_control.c:1437-1446), and grep finds no other fpga_write_cmd(0x3C) in active source. So this tree never sends 0x3C, yet FPGA_NOTES.md:59 lists it in the 'as used in this tree' inventory with no dead-code marker (unlike the 0x28 row), and lines 369-370 say 'our init's odd 0x3C write is this handshake's vestige', implying the write happens.

*(from doc-fpga-boot)*

### 0x0B/0x0C table rows omit the /2 halving of the written values

- **Where:** `FPGA_NOTES.md:66`
- **Severity:** low

The Atlan4-additions table says 0x0B writes 'fpgasettings.settriggerpoint' and 0x0C 'fpgasettings.totalsamples'. The code writes settriggerpoint/2 and totalsamples/2 (per-ADC counts: 750 and 1500 for the 1500/3000 defaults). For a doc that serves as the fw2/fw3 protocol contract, the halving is load-bearing for anyone re-implementing or porting the path.

**Evidence:** fnirsi_1013d_scope/fpga_control.c:677-685: `if(fpgasettings.fw_FPGA > 1) { fpga_write_cmd(0x0B); fpga_write_short(fpgasettings.settriggerpoint/2); fpga_write_cmd(0x0C); fpga_write_short(fpgasettings.totalsamples/2); ... }`; defaults scope_functions.c:7828-7829 (totalsamples=3000 '(1500*2)', settriggerpoint=1500 '(750*2)').

**Suggested action:** Amend the two rows to note the /2 (values sent are per-ADC halves).

**Verifier:** fpga_control.c:677-685 (the only 0x0B/0x0C write sites, per grep) writes fpga_write_short(fpgasettings.settriggerpoint/2) and fpga_write_short(fpgasettings.totalsamples/2) under fw_FPGA > 1, while FPGA_NOTES.md:66-67 documents the unhalved 'fpgasettings.settriggerpoint' / 'fpgasettings.totalsamples'. Defaults at scope_functions.c:7828-7829 (totalsamples=3000 '(1500*2)', settriggerpoint=1500 '(750*2)') confirm the written values are per-ADC counts (1500/750) — the /2 is load-bearing for anyone re-implementing the fw2/fw3 path from the doc.

*(from doc-fpga-boot)*

### Sector-710 table row lists readers/writers that don't exist on the current 1014D chain

- **Where:** `BOOT_NOTES.md:60`
- **Severity:** low

The SD-sector-map row for 710 says 'both loaders + display_control.c read; scope_reset_config_data() and the sm boot-switch write'. On the tree this map claims to describe ('as used by this tree', default 1014D variant): pecostm32's 1014D loader never reads sector 710 from SD — its display_control.c only validates the DRAM mirror at 0x81BFFC00, which nothing populates (as the doc's own next paragraph states); the 'sm boot-switch' was removed 2026-07-09 (doc's own line 38); and scope_reset_config_data()'s sector-710 write is #if !PORT_1014D-guarded (doc's own lines 66-73). Each correction exists elsewhere in the doc, but the table — the quick-reference artifact — describes the 1013D chain / pre-2026-07-09 state without saying so.

**Evidence:** FNIRSI_1014D_Firmware/fnirsi_1014d_startup/: no sd_card_read of sector 710 anywhere (grep '710|DISPLAY_CONFIG|0x81BFFC' hits only display_control.c:12,51 = DRAM mirror validation); the sector-710 SD read exists only in the Atlan4 1013D-chain loader ('Bootloader fw0.02...'/fw0.04 fnirsi_1014d_startup.c:121). fnirsi_1013d_scope/scope_functions.c:7849-7855: `ptr[0] = 4;` then `#if !PORT_1014D ... sd_card_write(DISPLAY_CONFIG_SECTOR, 1, (uint8 *)0x81BFFC00); #endif`. No STARTUP_CONFIG writer remains in sm_1014d.c (grep).

**Suggested action:** Annotate the row per-chain: '1013D chain: loader reads into DRAM + scope reset writes; 1014D chain: nobody reads the sector from SD, write guarded out' — matching the body text.

**Verifier:** BOOT_NOTES.md:60's row says 'both loaders + display_control.c read; scope_reset_config_data() and the sm boot-switch write' under a header 'as used by this tree' (line 52), but: (a) pecostm32's 1014D startup (the committed 1014D-chain loader) has no sd_card_read of sector 710 anywhere — grep hits only display_control.c:12/51, the DRAM-mirror (0x81BFFC00) validation, which the doc's own lines 62-64 say nothing populates; the sector-710 SD read exists only in the 1013D-chain loaders (fw0.04's modified fnirsi_1014d_startup.c:121); (b) scope_reset_config_data()'s write is #if !PORT_1014D-guarded (scope_functions.c:7850-7855), so it never fires on the default variant; (c) the sm boot-switch was removed 2026-07-09 per the doc's own line 38, and grep finds no STARTUP_CONFIG writer in sm_1014d.c. The table row aggregates the 1013D-chain/pre-fix state without saying so, each correction living elsewhere in the same doc — exactly as the finding states.

## Refuted by adversarial verification (kept for the record)

- **Top-pointer bridge lost P14's clipping: negative-x 'H' pointer draw wraps to an out-of-bounds write** (code-seams, high) — The divergence evidence is real and I reproduced all of it: P14's display_top_pointer(int32 xpos, int8 id) clips via startpixel/lastpixel with early returns (reference FNIRSI_1014D_Firmware/fnirsi_1014d_scope/display_lib.c:1427-1459); ours is Atlan4's unclipped display_top_pointer(uint32,uint32,int8) (display_lib.c:1363-1373) over display_copy_icon_fg_color's raw 'ptr = screenbuffer + xpos + (ypos*pixels)' (display_lib.c:1206-1217); the call site menu_1014d.c:577-580 kept P14's partial-visibility band verbatim (HORIZONTAL_POINTER_LEFT=6, WIDTH=15 per variables.h:167/169, so positions -8..6 pass xpos -15..-1); Atlan4's own call sites pre-guard position<3 (Atlan4-1.00o5 menu.c:9356-9366, scope_functions.c:631-643); and the band is reachable (trigger_position_min = (TRACE_HORIZONTAL_END - disp_xrange)+5 goes deeply negative at fast timebases, scope_functions.c:3406/3416, with sm_set_trigger_position clamping only to min/max, sm_1014d.c:1314-1323; ui_draw_pointers runs per frame at scope_functions.c:3772). But the finding's conclusion — an out-of-bounds write up to ~30 bytes BEFORE displaybuffertmp corrupting whatever global precedes it — is arithmetically wrong. The 'H' call passes ypos = TRACE_VERTICAL_START = 59 (variables.h:144/152) and pixelsperline = SCREEN_WIDTH = 800 (display_lib.c:47-52, menu_1014d.c:24), so the destination pointer is screenbuffer + (xpos + ypos*pixels) where xpos+ypos*pixels = 0xFFFFFFF1 + 47200 = 47185 (mod 2^32): 47185 pixels INTO the 384000-pixel buffer, not before it. Every write of the 21-row icon stays in-bounds (max offset ~ 79*800+798 << 384000), and display_character's draw_vw_character (display_lib.c:1572) uses the same dominating '(ypos+y)*pixelsperline + xpos' sum. A write before the buffer would require ypos=0, which no caller passes. The finding's first-row-OOB claim silently dropped the +ypos*pixels term.
- **uint16 triggerverticalposition legally stores negative values: trigger level snaps to opposite rail** (code-gotchas, high) — The storage wrap is real (uint16 at variables.h:760 vs int16 in reference variables.h:459; negative positions down to ~traceposition-219 are storable wrapped, verified at sm_1014d.c:1339-1372), but the claimed failure does not occur. Both level computations multiply the position difference by 2^21 — fpga_control.c:512 uses '* 2097152' and sm_1014d.c:1356 uses '<< VOLTAGE_SHIFTER' (21) — and (x + 65536) * 2^21 == x * 2^21 (mod 2^32), so the mod-2^16 wrap is annihilated: the int32 result is bit-identical to what the int16 reference computes (the true difference is clamp-bounded to |diff| <= ~460 < 1024, so the true product always fits int32). I verified this empirically: a test compiling the exact expressions at -O2 gives identical levels for wrapped-uint16 vs int16 inputs (7==7, 1==1, -5==-5, -203==-203), and arm-none-eabi-gcc -O2 compiles fpga_set_trigger_level's multiply as 'lsl r0, r0, #21', which wraps mod 2^32. Therefore the immediate fpga_set_trigger_level() push computes the same small/negative level as the reference and clamps toward the BOTTOM rail (the direction the user dialed), not 255/top; the next detent likewise takes the level<0 branch and re-clamps to the lower limit exactly as the reference does — it never parks at traceposition+218. Since int16 and uint16 have identical 16-bit bit patterns, the += at line 1600 and the config save/restore (scope_functions.c:7924/8136, uint16* buffer, no clamping on restore) are also behavior-identical to the reference. Triggering does not stop and the level never snaps to the opposite rail.
- **F25 0x28/0x00 restoration is #if PORT_1014D-guarded, excluding the stock-FPGA 1013D variant that pecostm32's 1013D firmware also feeds 0x28** (code-fpga, low) — The code facts are accurate — fpga_control.c:631-642 wraps the restored 0x29/0x01 + 0x28/0x00 in '#if PORT_1014D' plus fw_FPGA==1, and FNIRSI_1013D_Firmware/fnirsi_1013d_scope/fpga_control.c:495-496 sends 0x28/0x00 unconditionally with no 0x29 anywhere in that file — but the finding's actual complaint, that 'neither the code comment nor PORT_AUDIT F25 records that the 1013D variant is intentionally left on the pre-F25 behavior', is factually wrong. PORT_AUDIT.md's F25 entry states explicitly: 'Fix applied (fpga_do_conversion, #if PORT_1014D + fw_FPGA==1 so the untestable 1013D and the PECO fw_FPGA>1 paths are untouched)'. That is precisely a record of deliberate conservatism, with its rationale (the 1013D variant has never been hardware-tested), in the exact document the finding says lacks it. The finding misses this sentence in the F25 text it otherwise cites.

## Coverage critic — what this review did NOT look at

The 10-agent review was broad and evidence-disciplined on its planned dimensions — the five doc groups were read end-to-end with claim-level verification, the imported-module seams and FPGA paths were diffed hunk-by-hunk, and the guard/gotcha sweeps were systematic. The misses concentrate in five places. (1) Verdict integrity: the three REFUTED verdicts include one that directly contradicts another agent's CONFIRMED finding on the same lines (the F25 0x28 restore IS '#if PORT_1014D'-guarded at fpga_control.c:629 while pecostm32's 1013D upstream sends 0x28/0x00 at its fpga_control.c:495 — the 'refuted' claim is factually true), and 2 of 3 REFUTEDs were the reviewers' high-severity findings, so the refutation reasoning deserves a spot-audit. (2) One false CLAUDE.md build claim nobody caught: the 'checked-in .c sources' for mksunxi/flashfilepacker do not exist anywhere in the tree — only the opaque committed binaries. (3) Whole infrastructure files were never opened: start.s, arm32.h, the linker script, interrupt.c (D-cache bit is set with no MMU anywhere, so the data side actually runs uncached; stacks are hard-wired with no guard), and the plan's stack/cache/alignment hazard classes were not executed. (4) IRQ-context concurrency was never hunted, and the USB mass-storage path — raw SD reads/writes from interrupt context against the same card the main loop uses via FatFs — went unaudited along with BOOT_NOTES' sector-0-exposure claim. (5) test.c, which is in the build for both variants and implements the roll-mode path the docs track as broken (F21/ROADMAP 12), was read only at one loop; the tracked sibling loader trees (129 files) were never even listed. The FatFs/SD/display_lib layer was covered only by byte-diff against Atlan4 — a defensible priority call, but it should be recorded as diffed-not-audited rather than reviewed. None of these gaps undermines the review's shipped findings; they define the next, much smaller pass.

- **Verdict integrity: cross-agent contradiction on the F25 1013D guard (REFUTED vs CONFIRMED on the same lines)** — code-guards CONFIRMED (medium) that the F25 0x28/0x29 restore is 1014D-only and the 1013D variant keeps the regression, while code-fpga's same-substance finding was REFUTED (low). I re-verified the facts: the restore in /home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/fpga_control.c is inside '#if PORT_1014D' (line 629, closing #endif line 641), and pecostm32's 1013D upstream sends 0x28/0x00 unconditionally in fpga_do_conversion (reference FNIRSI_1013D_Firmware/fnirsi_1013d_scope/fpga_control.c:494-496, 'Set the FPGA for short time base mode'). The factual claim in the REFUTED finding is true, so either the verify pass erred or it refuted on unstated grounds (e.g. 'Atlan4's shipped 1013D also omitted it'), which is exactly the F25 bug class. More broadly, 2 of the 3 REFUTED verdicts across the review were the reviewers' 'high' findings — the costliest place for a verify-pass error. Spot-check of the refuted triggerverticalposition claim: the level-rotary path IS clamped (sm_1014d.c:1362-1372), so that refutation looks plausibly right, but a second writer 'scopesettings.triggerverticalposition += delta' at sm_1014d.c:1600 is deliberately unclamped ('without worry about limit' comment) and was not discussed by either side.
  - Follow-up: Re-adjudicate the F25-guard pair against the two file locations above and record one verdict (the CONFIRMED one matches the code). Then have one agent audit the refutation reasoning of the two high REFUTEDs (menu_1014d.c top-pointer clipping; sm_1014d.c triggerverticalposition, including the unclamped writer at line 1600) — verify passes overturning top-severity claims deserve a second pair of eyes.
- **False CLAUDE.md build claim: mksunxi/flashfilepacker sources are NOT checked in** — CLAUDE.md line 107 says host gcc can 'rebuild mksunxi/flashfilepacker if needed' and line 128-129 says to 'rebuild from the checked-in .c sources'. No such sources exist anywhere: 'git ls-files | grep -i sunxi\|packer' returns only the ELF binaries (fnirsi_1013d_scope/mksunxi, fnirsi_1013d_scope/flashfilepacker, plus copies in the sibling loader trees), and 'find . -name mksunxi*.c -o -name *packer*.c' over the whole checkout (vendored trees included) returns nothing. doc-claude-repo verified the neighbouring chmod claim but never tested this one. Consequence: a user told to rebuild the tools hunts for nonexistent files, and every build's final image passes through two opaque committed host binaries with no in-tree source — an unauditable link in the flash chain that no agent examined (doc-claude-repo also explicitly skipped hash-verifying bootloader provenance).
  - Follow-up: Fix CLAUDE.md (drop or correct the 'checked-in .c sources' claim at /home/jsantala/src/FNIRSI_1014D_Firmware/CLAUDE.md lines 107 and 128-129), locate the actual upstream sources (pecostm32's repos/EEVblog) and either vendor them or document where they live; optionally hash-compare the committed binaries against a rebuild.
- **USB stack + mass-storage internals and the IRQ-vs-main-loop concurrency hazard class — never executed** — The only USB finding filed was the CDC 'f' debug command; code-gotchas explicitly skipped 'mass_storage_class.c/cdc_class.c protocol internals' and 'interrupt handling'. Facts I verified: USB service is interrupt-driven (setup_interrupt(USB_IRQ_NUM,...) at /home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/usb_interface.c:138/153/195/204), and mass_storage_class.c performs raw sd_card_read/sd_card_write in that IRQ context (lines 368, 481, 512, 589), giving the PC raw-sector write access to the same card the main loop uses via FatFs (screenshot/wave/config/acqprobe writes). Nobody verified that the 1014D USB view (sm_1014d.c:2978 sm_start_usb_export -> ui_setup_usb_screen) actually blocks all main-loop SD/FatFs activity while mounted, audited SCSI LBA/length bounds, or checked the BOOT_NOTES claim that USB MSC exposes sector 0 (doc-fpga-boot listed it as explicitly unverified). The SCSI buffer aliasing onto viewthumbnaildata (mass_storage_class.c:352/420/481) is also unexamined.
  - Follow-up: One targeted agent: read mass_storage_class.c (619 lines) + the usb_device_irq_handler dispatch, confirm ui_setup_usb_screen is a modal loop that serializes SD access (and that no timer/other ISR path touches SD concurrently), check scsi_start_lba/scsi_available_blocks bounds vs card capacity and the viewthumbnaildata aliasing, and settle the sector-0-exposure claim in BOOT_NOTES.
- **CPU-infrastructure layer never opened: start.s, arm32.h, fnirsi_1013d.ld, interrupt.c (stack sizing, cache coherency, alignment classes unexecuted)** — No agent names any of these files. Facts I verified: (1) main() enables I-cache and D-cache (fnirsi_1013d_scope.c:56-57, 'Instead of full memory management just the caches enabled') but nothing anywhere calls arm32_mmu_enable/arm32_ttb_set (grep over all first-party code) — on ARM926EJ-S the D-cache C-bit is inert with the MMU off, so the data side actually runs uncached: the comment is misleading, a large performance win is silently forfeited, and anyone later enabling the MMU with a flat cacheable map would break FPGA PE-bus reads, the DEBE framebuffer scanout, and USB — with no doc warning. (2) The linker script /home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/fnirsi_1013d.ld defines no stack or heap region at all; the six mode stacks are hard-wired in start.s lines ~139-150 (0x81FB5000..0x82000000, descending) with no overflow guard, and fixed-address data (display-config page 0x81BFFC00, the deliberately unaligned STARTUP_CONFIG_ADDRESS uint32* at 0x81BFFC1F, variables.h:108) sits below them unprotected. (3) interrupt.c (69 lines) was read by nobody. All four files are byte-identical to Atlan4-1.00o5 (I diffed), so this is inherited — but the review plan's 'stack sizing / cache coherency / ARM926 alignment' hazard classes were simply not run.
  - Follow-up: Short single pass: read interrupt.c + the IRQ frame in start.s, document the D-cache-inert-without-MMU fact in FPGA_NOTES or AGENTS.md (and add 'enable MMU + write-through/flush discipline' as a ROADMAP perf item), note the stack map and the buffers below it, and grep first-party code for misaligned uint32*/uint16* casts onto byte buffers beyond the already-documented 0x...1F quirk.
- **test.c is load-bearing production code (long-timebase/roll path) and essentially unread** — test.c is in the build (Makefile-Debug.mk:78) with no variant guard (compiled into BOTH variants) and contains the entire long-timebase/roll implementation the docs actively track as broken (F21 'roll renderer geometry-broken', ROADMAP 12): scope_get_long_timebase_data/-data1, scope_preset_long_mode, scope_check_long_trigger with a hard-coded '+25 compensation' magic (test.c:1063) that bypasses the per-channel input_calibration (the calibrated forms are commented out in favour of a global 'multiply'), plus soft-float DSP helpers (double/size_t sinc/FIR/EMA on ARM926) and scope_test_* diagnostics. Of ~1411 lines, only 'the roll-mode read loop' was read (code-fpga); code-seams and code-gotchas both scoped it out. The file most relevant to the open roll-mode defect fell between every dimension.
  - Follow-up: Targeted review of /home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/test.c: the roll acquisition/trigger path (signedness, bounds, the +25 magic and stale 'multiply' scaling vs the calibrated path, triggerverticalposition semantics), reachability of the scope_test_*/DSP helpers (dead-code candidates for ROADMAP 7), and whether soft-float in the roll loop matters for its known geometry problem.
- **Three tracked first-party sibling firmware trees never mentioned by any agent** — git ls-files shows tracked source trees at repo root outside fnirsi_1013d_scope/: fnirsi_1013d_startup_screen/ (55 files), fnirsi_1013d_startup_from_sd_card/ (38), fnirsi_1013d_sd_card_bootloader/ (36) — real buildable loader/startup code (each with Makefile, start.s, bl_fpga_control.c, dram_control.c, its own mksunxi copy). The review plan's framing ('all first-party/active code is in fnirsi_1013d_scope/') scoped them out and no agent even listed them. Unassessed: whether they are stale relative to the committed bootloader binaries actually flashed (bootloader_base.bin / bootloader_1014d_base.bin, whose provenance BOOT_NOTES ties to the untracked vendor trees instead), and whether any doc should label them live vs provenance-only. CLAUDE.md's module map does not mention them at all.
  - Follow-up: 30-minute pass: identify what each tree builds, whether output matches either committed bootloader (BOOT_NOTES cross-check), and add a one-line status for each to CLAUDE.md (e.g. 'upstream 1013D loader sources, provenance-only, superseded by bootloader_1014d_base.bin').
- **FatFs/SD/display_lib internals covered only by 'diff -q vs Atlan4'; malloc.c not linked vs CLAUDE.md's description** — ff.c (8756 lines), ffunicode.c, diskio.c (beyond get_fattime), sd_card_interface.c (1274 lines, known unbounded register waits, un-itemized), and display_lib.c's clipping internals (1914 lines; only display_character audited — ironically the one REFUTED-high finding turns on exactly the unaudited clipping behaviour) were verified byte-identical to Atlan4 but never read. I verified one concrete consequence chain: malloc.c is absent from the link (Makefile-Debug.mk object list), its only would-be consumer is FatFs ff_memalloc, and ffconf.h compiles those paths out (FF_USE_LFN=1 static, FF_USE_MKFS=0) — benign today, but CLAUDE.md's architecture line 'no libc (... tiny malloc.c)' implies a live allocator that does not exist in the image, and malloc.c joins font_6/7/8.c, icons_extract.c, strcpy.s as unlinked tree weight (ROADMAP 7 class).
  - Follow-up: Record the 'diffed-not-audited' status of the SD/FatFs/display_lib layer as an accepted-risk note in PORT_AUDIT (so future sessions don't assume it was reviewed); correct CLAUDE.md's malloc.c mention to 'present but not linked'; fold malloc.c into the ROADMAP 7 dead-weight list. Deep audit only if SD/file-path bugs surface.
- **Declared-but-never-closed doc claim classes (binary-only and shared-core §1 claims)** — Several agents correctly declared these skips, but no dimension owns them, so they will stay unverified forever unless assigned: (a) BOOT_NOTES' v0.8 loader dead-code claim (call-site-free FPGA-version wait) rests on one past disassembly, never re-derived; (b) bootloader_1014d_base.bin's 'hash-verified local rebuild' provenance never re-checked (both from doc-fpga-boot's and doc-claude-repo's skip lists); (c) PORT_AUDIT §1's 'verified good' byte-diff list was re-established by code-seams for the imported 1014D modules only — the shared-core files in §1 were sampled (code-gotchas diffed six) but not systematically re-verified; (d) tools/acqprobe_analyze.py got a smoke-run and table check but no code review, and bench-conclusion numbers in FPGA_NOTES rest on it. These are cheap, mechanical closures compared to the review effort already spent.
  - Follow-up: Batch them into one mechanical session: objdump the committed v0.8/1014D loader binaries to re-confirm the dead-code and provenance claims (scripted, no hardware), run a scripted diff of every PORT_AUDIT §1 shared-core file against its named reference, and give acqprobe_analyze.py a one-pass code review (its outputs are now load-bearing for FPGA_NOTES' capture-geometry conclusions).

## Appendix: per-agent coverage notes

### doc-claude-repo — repo CLAUDE.md (primary AI-instruction file)

Verified (personally read, current checkout): every import-table filename (menu_1014d.c/.h, sm_1014d.c, clock_synthesizer.c/.h, uart.c/.h, font_1.c, fonts_1014d.c present; font_0.c/font_2.c carry the claimed '#if !PORT_1014D' guard); table sizes at variables.c:421/518/794/890 (time_per_div_sample_rate[35], time_div_texts[35], sample_rate_settings[29], timebase_settings[35]; index 0 = 50s, long/short split at index 11); port_config.h PORT_1014D=1 default + PORT_A_KEYDEBUG; Makefile claims (variant $(info) at Makefile:62, VERSION_STRING v1.00o5-1014D at 52–55, bootloader selection/echo/offsets 0x8000/0x5BC00 at 124–130, variant-named artifact copies at 134–137, empty NetBeans test stubs at 176–190); CFLAGS in nbproject/Makefile-Debug.mk:86 (-O2, -fcommon, -Wno-error=implicit-*/int-conversion, arm926ej-s); both bootloader .bins tracked; main() sequence in fnirsi_1013d_scope.c:75–145 and the acquire/input/display loop at 412–444 incl. the enabletracedisplay||ui_menu_composite_active gate; ui_menu_composite_active/ui_redraw_active_menu (menu_1014d.c:5438/5457, called from scope_functions.c:3440/3776); UIC_BUTTON_OFF config save (sm_1014d.c:79–89); GD_KEY_* defined in uart.h and used in no .c file (documentation-only claim holds); uart 0xFF poll (uart.c:51); fw_FPGA mapping 0x1432/0x1532/0x1632→1/2/3 (fpga_control.c:205–207); FEL BROM jump 0xFFFF0020 (sm_1014d.c:2899); fpga_set_long_timebase, volt_div_texts_short, sm_set_time_base/scope_set_timebase, ui_update_measurements, fpga_set_trigger_level all exist; null 'Output browsing'/'Capture output' handlers (sm_1014d.c:25–26); the 49-ui_*-hooks claim (PORT_AUDIT.md:706); BOOT_NOTES.md consistency (sector map 16/80/708/709/710, dd seek=8 bs=1024, sunxi-fel 0x7FFFFFE0 command, sector-710 display config + legacy boot byte, sector-710-poke obsolete); all referenced FPGA_NOTES sections exist (§capture geometry:91, §clock search:409/448, §Screenshot analysis:497, §migration:240, §pecostm32-RE:287, §TD rebuild:628); all seven repo-root reference trees/files exist on disk incl. bench/.gitignore ('*'), tools/acqprobe_analyze.py with --csv, and the gitignored EEVBlog html; factory and clock menu item arrays (menu_1014d.c:5207–5301); branch/history claims (main, PORT_A, 9daa91f vendor commit). Staleness cross-check: full git log since 2026-07-12 (24 commits) diffed against CLAUDE.md's own change history (only 100f9ff and 3de9c3d touched it, neither touched Known issues) and against PORT_AUDIT F18–F35 status lines; PORT_AUDIT §4 checklist read to confirm the 'verify pending' labels CLAUDE.md carries are still accurate per the docs (they are — only item 4 is struck through — so individual 'verify pending' labels were NOT flagged). Skipped/not done: did not run make (forbidden) so echo behavior is verified from Makefile text only; did not hash-verify bootloader_1014d_base.bin provenance or diff branch 'main' against pecostm32's GitHub; did not verify glyph metrics of font_1 beyond structure (13-byte rows consistent with 13 px); did not audit the vendored reference trees' contents beyond existence (out of scope); the '49 hooks' and GUI-glue claims accepted on PORT_AUDIT's own record rather than re-counting hooks. No output caps hit.

### doc-memory — workspace CLAUDE.md + persistent AI memory

Read in full: /home/jsantala/src/CLAUDE.md and all 11 files in /home/jsantala/.claude/projects/-home-jsantala-src-FNIRSI-1014D-Firmware/memory/ (MEMORY.md + 10 memories). Index consistency: 10 memory files ↔ 10 MEMORY.md lines, exactly one line each; all [[links]] resolve except [[atlan4-merge-plan]] (reported). Git verification: all 16 commit hashes cited across the memories exist (4a5fa61, 2ab7296, 77af34b, 204201d, 3446773, 1a18ce8, b793048, bd188bb, 3de7675, 7453358, b0daa6b, d2fa75c, 1bbfce9, 3de9c3d, ceeae17, 9daa91f); `git log --since=2026-07-22 --all` is empty, so every 'hw-verify pending'/'open'/'never flashed' status is still the latest known state rather than invalidated by newer work; branch is 0 ahead of origin (the memories' 'nothing pushed' claims are the stale part, reported). Path/symbol verification by grep/ls against the checkout: port_config.h PORT_1014D=1 + PORT_A_KEYDEBUG; all imported 1014D modules incl. fonts_1014d.c; both bootloader .bins; measure_high_rate_artifact (scope_functions.h:54), fpga_dump_ring (fpga_control.c:1407), volt_div_texts_short, ui_menu_composite_active/ui_redraw_active_menu, UIC_BUTTON_OFF=200 (=0xC8=GD_OFF), SETTING_SECTOR_VERSION_LOW 0x0016, fw 0x1432 checks; tools/acqprobe_analyze.py with its fill-geometry section; bench/{screenshots,acqprobe/2026-07-19-*} + 'never commit' .gitignore; donwulff-notes.md and EEVBlog page gitignored as claimed; fpga/AL3_1014D full contents incl. out/FPGA_1014D.bin at exactly 282,898 B; FPGA_NOTES.md section headings (capture geometry, pecostm32-RE, special IC, TD rebuild, clock search ×2, screenshot analysis); PORT_AUDIT.md F29 and F35 text; PORT_A.md hardware facts (F1C100s/575 MHz/MS5351M/MXT2088); reference-tree time_div_texts[24]; ~/tools/anlogic-td/ contents (both .lic files, 5.0.3 install + GTK2 shim, license/Anlogic.lic) and license expiry 2026-08-31 confirmed inside the .lic (generated 2025-08-30 as claimed) — note: that is 11 days from today, worth surfacing to the user given fpga/ is untracked. NOT verified (out of reach from this checkout): bench-derived numbers (ring 911, caps/s budgets, comp values, 25% trigger width), binary-only claims (bootloader v0.8 dead-code disassembly, md5-identical startup rebuild), laptop-side facts (TD 5.0.4 install, Cygwin behavior), warning baselines 257/266 (no build run), Sipeed mirror workflow (no network calls made). Vendored reference trees used read-only for comparison only; no findings reported against them. No caps hit; nothing truncated.

### doc-agents — AGENTS.md + PORT_A.md

AGENTS.md (175 lines) read in full; PORT_AUDIT.md §3 read first (lines 208-218) plus §5/§5b/§5c/F25 context so §3-known errata were not re-reported (line 117 NAV_LEFT/RIGHT, line 78 #ifndef/F3, line 81 dispatch, line 101 committed-objects — all already reflect §3 and were not reported; lines 87/95 leftovers WERE reported). Verified against code, per claim: display buffers (variables.h:1384/1389, variables.c:179/185, SCREEN_SIZE usage), canonical draw sequence + clear/blit rects (scope_functions.c:3574-3796 — F17 divergence found), trace-display gating (fnirsi_1013d_scope.c:442, scope_functions.c:3439-3443 — accurate), ui_display_trigger_settings y=6/y=465 (menu_1014d.c:920-950, menu_1014d.h:27-31 — accurate), strcpy gotcha (definition scope_functions.c:5106-5117 returns terminator pointer; strcpy.s present but absent from Makefile-Debug.mk objects, strlen.o present; -fno-builtin-strcpy and -fcommon and -O2 in CFLAGS line 86 — accurate), guard-after-includes (sm_1014d.c:1-15 — accurate), int8 typedef (types.h:11 signed char — fix applied as documented), integer-promotion gotcha (ref_and_math.c:147 'int8 a = A[i]' — line reference exactly right; memset 128 confirmed scope_functions.c:275), UART blocking poll (uart.c:45-58 — accurate), variant guards (#if !PORT_1014D counts in touchpanel.c/DS3231.c/power_and_battery.c, zero #ifndef — accurate), file modes (git ls-files -s + mode-change commit 9daa91f — stale gotcha found), Makefile bootloader selection/offsets (fnirsi_1013d_scope/Makefile:123-130), factory-menu contents (sm_1014d.c:2605-2892, menu_1014d.c:5212), clock-before-FPGA init order (fnirsi_1013d_scope.c:84/88), PORT_A_KEYDEBUG (port_config.h:11), repo-root directory listing. PORT_A.md (300 lines) read in full: superseded banner present and strong; all referenced commits verified to exist (25a517c, 46b80c9, 18c597f, 9b37203, baf5005, 85d2ab8, 51f4872) and PORT_A branch exists; How_to_load_scope.txt absent from the current worktree but present in origin/PORT_A's tree, which is the branch that section documents — not reported. Skipped/not verifiable statically: hardware-behavior claims (UART divisor math, USB descriptor bugs, Si5351 register effects, FPGA silicon claims — cross-checked against repo docs only), external GitHub repo characterizations (Atlan4 upstream, froloffw7 — no network), the '(void)x is AI slop' style opinion (subjective, no code claim), 'Release config targets host' (took CLAUDE.md concurrence), and AGENTS.md's 'Working:' hardware-tested list (bench facts, consistent with CLAUDE.md/memory). No caps hit; nothing silently truncated. Note for the caller: the stale committed-non-executable claim also exists in CLAUDE.md (out of my assigned scope but flagged in the mksunxi finding).

### doc-audit-roadmap — PORT_AUDIT.md + ROADMAP.md

CHECKED: Full read of PORT_AUDIT.md (738 lines, in two pages — lines 1-557 then 558-737 due to the 25k-token read cap; no content skipped) and ROADMAP.md (252 lines, complete). Oracles: git log --oneline --since=2026-07-01 (all 41 commits on atlan4-base); FPGA_NOTES.md headings plus sections §capture geometry (91-115), §SOLVED sawtooth (186-205), §Auto clock search (409+), §decoupled clock search (448+), §Synthesis environment (575-628), §TD rebuild follow-on; CLAUDE.md 'Known issues' (from system context). Code spot-checks to verify doc claims: fnirsi_1013d_scope/fpga_control.c (0x28/0x29 restored in fpga_do_conversion :631-643, roll-path 0x28 still commented :599 — consistent with F25's stated open follow-up, F26 double ready-read kept :661-672), sm_1014d.c (timebase boundary 7→11/10→6 :1456-1457, sm_handle_trim_actions :2833-2869 has no DC re-centre, clock-menu presets :2799-2810, F1-F6 key cases handled), cdc_class.h include topology in working tree vs Atlan4-1.00o5 reference (PC_interface.c:25 vs menu.c), fpga/AL3_1014D/out artifacts and ~/tools/anlogic-td install on disk, ROADMAP-7 dead-weight files (all still present — item still valid, not stale).

F-STATUS TABLE (built during the pass; 'accurate' = doc status matches code/git/later docs): F1 fixed §5 ✓; F2 retracted ✓; F3 fixed (1013D hw-test still never done, §4.6 accurate) ✓; F4 fixed ✓; F5 resolved ✓; F6 accepted-as-is ✓; F7 fixed but §2/§5 disagree on the file — FINDING; F8 fixed ✓; F9 info (stray scope-1014D.bin still present, matches ROADMAP 7) ✓; F10-F15 fixed ✓ (F13 superseded by F17, explicitly noted); F16 fixed + bench-verified (noted in F17) ✓; F17 fixed ✓; F18 fixed + bench-confirmed (noted in F20) ✓; F19 fixed + bench-confirmed ✓; F20 fixed ✓; F21 experiment reverted, roll overlap fixed, roll-renderer defect correctly still open ✓ (ROADMAP 12 omission — FINDING); F22 superseded by F23-F25, cadence pacing explicitly retired, rate-0 revert done (3de7675), but the trim DC-re-centre 'next session' instruction never built nor retired — FINDING; F23 superseded, housekeeping recs acted on (bd188bb reverted, b0daa6b kept, documented) ✓; F24 explicitly marked superseded by F25, code reverted ✓; F25 CONFIRMED fixed on bench, roll-path follow-up correctly still open (code agrees) ✓; F26 applied, kept in code (F27 justifies via stock double-read) ✓; F27 heisenbug record, pecostm32 A/B correctly still PENDING (no later commit/doc closes it; last commit 2026-07-19) ✓; F28 implemented, hw-verify pending — accurate ✓; F29 guard implemented + bench-verified 2026-07-17 (commit 100f9ff 'bench-OK') ✓; F30 open, gated on A/B — accurate ✓; F31-F33 fixed in code, hw-verify pending (commit 1bbfce9) — accurate ✓; F34 fixed + bench-verified (100f9ff) ✓; F35 unreproduced, recorded ✓. NUMBERING: F1..F35 continuous, no gaps, no collisions; note the task brief said 'F1..F34' but the file contains F35 (2026-07-17) — plus the F22 'addendum' which is not a separate number. The §5c header's 'F12–F15' range vs actual F12–F35 content is reported as a finding.

ROADMAP sweep: items 1, 1b, 2-30 all read. Struck/annotated correctly: 2, 16 (first half), 14, 20, 21, 23, 27 (in-place addenda), 29(b). Stale items reported: 1b, 15, 16 (tail), 11, 12, plus the section-background 'open post-trigger-fill' contradiction. NOT flagged (judgment calls): item 1's remaining hardware-verify sub-items (AUTO, Position trim, compositing, version string) — CLAUDE.md still lists them as verify-pending so the docs agree with each other, and no doc entry explicitly closes them even though later bench sessions plausibly exercised several (per the user's bench-report convention); item 10 key coverage (F1-F6 cases exist in sm_1014d.c but I could not cheaply establish what remains unhandled); items 3-9, 13, 17-19, 22, 24-26, 28-30 checked against git log and docs — still open/valid. SKIPPED: deep read of BOOT_NOTES.md, PORT_A.md, AGENTS.md (only consulted where PORT_AUDIT/ROADMAP cite them); bench/ data files; re-verifying PORT_AUDIT §1 'verified good' claims (byte-diffs vs reference trees) — outside the staleness mandate; building either variant. No other caps hit.

### code-recent — post-2026-07-12 commit review

Reviewed all 7 listed commits with full `git show` diffs: ceeae17, 3de9c3d, 7378492, 100f9ff, 1bbfce9, 2497690, d2fa75c (the four 2026-07-12 commits b82c863/1a18ce8/b793048/c465d86 predate the task's list and were used only as context). Completeness checks done: every caller of the two changed-signature functions ui_print_value/ui_msm_print_value grep-verified updated (d2fa75c/100f9ff); sibling channel-2 blocks checked for every channel-1-adjacent edit (F31 coupling handler is channel-agnostic via currentsettings and reachable only from the normal-run state, matching Atlan4's live-only push; F29 dash guard applied identically in both ui_display_measurements and ui_update_measurements; averagesum set in the shared per-channel reader). Premises verified against vendored references: Atlan4-1.00o5 statemachine.c:1918-1950 coupling touch flow (F31 fix mirrors it exactly, incl. dcoffset zero on AC); pecostm32 P14 fpga_control.c:700-709 confirms the Q20 producer and our fpga_control.c:988-1006 the Q16 producer (F32 — all six >>20→>>16 sites converted, none remain, 1013D-side >>16 consumers consistent); time_calc_data value-identical between trees; F33 rescale placed before periodtime/frequency derivation, guarded no-op at scale==1.0, 1013D never leaves 1.0. F28 fixed-point pipeline re-derived end-to-end (signal_adjusters/VOLTAGE_SHIFTER/volt_calc_data), averagesum placement in fpga_read_sample_data confirmed pre-division; F34 geometry and the 5-item factory menu (highlight rows, 309+163=472 bottom edge, clock submenu) checked. Both variants built cleanly from the current tree (1013D first, then 1014D rebuilt last so the correct-variant artifact remains, per CLAUDE.md). tools/acqprobe_analyze.py smoke-run on both local bench run dirs; its TIME_DIV_TEXTS/SAMPLE_RATES tables match variables.c; doc-quoted fill numbers (911/910/2507/679/1481) reproduced, and an independent autocorrelation of the driven-hum dump (period 198-200 ring addresses) empirically pinned the ring-address time base, grounding finding 1. Doc cross-checks: F27-F35 entries in PORT_AUDIT.md vs code, 7378492's probe-comp purge grep-verified complete (only negated references remain). Limits: no hardware access — the commits' own 'hw-verify pending' items (coupling relay click, time readouts on a real signal, overclock freq) remain unverifiable here; the ±1 sample alignment of build_ring's ring fold is inherently ambiguous due to the stale-pipeline first byte, so doc figures like 910/2507 carry ±1 (docs claim 'to the sample'); ROADMAP items 25-30 prose reviewed only where tied to code; FatFs/USB interactions of the probe's SD writes not exercised.

### code-seams — imported-module divergence vs pecostm32 originals

Method: read PORT_AUDIT.md in full (738 lines, F1-F35 + fix passes 5/5b/5c/5d) to establish the documented-intentional set, then diffed every listed pair against FNIRSI_1014D_Firmware/fnirsi_1014d_scope/ and read every divergent hunk; for each bridge I checked the Atlan4 side (root tree + Atlan4-1.00o5/).

Per-file divergence counts and classification:
- clock_synthesizer.c: 4 hunks (variables.h include; MS1_P1B parameterized with 0x06 stock fallback; two new functions clock_synthesizer_set_sampling_clock/_apply_sampling_clock; EOF whitespace). All intentional (Sampling-clock feature, CLAUDE.md/FPGA_NOTES); the added function's PORTA pin config and all CS_* register values byte-match P14's own (CS_CLK1_CTRL 0x11 etc. verified in both headers). 0 findings.
- clock_synthesizer.h: 2 hunks (prototypes + EOF) — intentional.
- uart.c: 1 hunk, whitespace only. uart.h: 3 hunks (GD_KEY_* documentation table with the F5-corrected NAV labels, whitespace x2) — intentional per F5.
- font_1.c vs 1014D_fonts.c: font_1 metrics/info/FONTDATA plus all glyph data lines (every data line carries the font_1_ prefix, so the line-diff covered all glyph bytes): 193/193 lines identical. No PORT_1014D guard, documented safe in PORT_AUDIT §1 (nothing Atlan4-side references font_1). 0 findings.
- fonts_1014d.c vs 1014D_fonts.c: font_0 compared as a full section (295 lines incl. the anonymous 163x16 pixel array, translation table, extended/fixed info, FONTDATA) — identical; font_2 (193), font_3 (387), font_4 (193) via prefixed-line diff (covers every glyph array, metrics row, FONTDATA header values incl. the 32px font_4 height) — identical; font_structs.h byte-identical to P14, so the tables parse under the same layout. Guard after includes. 0 findings.
- menu_1014d.h: 5 hunks — factory/clock menu geometry, probe-translation externs, composite prototypes, F28 resolution args, guard rename. All documented; menu geometry arithmetic checked (5x31+8=163 to y=472; 7x31+8=225 to y=472).
- menu_1014d.c: ~25 hunks, all read. 24 classified intentional-documented: F2 strcpy removal, includes/guard, 5b/F17 version+build stamp, display_top_pointer 3-arg bridge (x2), F28 resolution plumbing (~10 call sites + both print functions; ui_display_vavg hi-res verified to mirror P14's vertical_scaling_factors zoom block, ref :1705), F32 Q16 shifts (6 sites), F14 probe-index translation (4 sites), F15 volt_div_texts_short, F29 dash guards (2), F12 source-buffer fix, F34 designator gap + zero alignment (3 sites), MEASUREMENTFUNCTION->UI_ rename, ui_unhighlight_main_menu_item fill-rect 174x25->177x28 (undocumented but verified correct: fully erases the 175x26 highlight box and stays inside the menu interior x6..185/y<456; part of the audited graft commit 4a5fa61), +333-line factory/clock/composite addition (buffer choreography and NAV-state coverage checked). 1 finding (top-pointer clipping loss, class iii).
- sm_1014d.c: ~20 hunks, all read. 18 classified intentional-documented: includes/guard, factory-menu table entry (P14's null 11th item), NAV dispatch, sm_init range-properties call, AUTO long_mode fix, F16 trigger-level pushes (4 sites), 50% bridge (scope_do_50_percent_trigger_setup verified consistent with the Atlan4 engine: it sets channelN.triggerverticalposition -> scope_calculate_trigger_vertical_position -> global position, and the F16 fpga_set_trigger_level push derives+stores scopesettings.triggerlevel from it — P14's direct triggerlevel write is subsumed), triggeronchannel pointer-compare bridges (3 sites, boolean-equivalent to P14's 1^ch/0^ch), F14 probe cycle, F31 coupling push (polarity verified against Atlan4 statemachine.c:1915-1950: coupling=1 is AC, dcoffset zeroed, both FPGA pushes; channel menu confirmed unreachable in wave view so Atlan4's !waveviewmode guard is not needed), F1/F21 timebase rebuild (RUN_STATE_RUNNING=1 verified consistent with Atlan4's truthy-running runstate), defensive trigger-position recompute, F19 cal-result save, +338-line factory/clock/trim/FEL block (trim symmetric split and ±200 clamp arithmetic checked; FEL jump address 0xFFFF0020 matches BOOT_NOTES). 2 findings (waveview samplerate clause; stale trim comment).

Bridges line-verified on the Atlan4 side: display_top_pointer, display_left/right_pointer (identical semantics both trees, call sites clamp — safe), scope_do_50_percent_trigger_setup, fpga_set_trigger_level, scope_calculate_trigger_vertical_position, scope_set_timebase, coupling flow, RUN_STATE defines, display_copy_icon_fg_color.

Skipped / not verified: statemachine.h vs P14 (shared header, outside the assigned file list; PORT_AUDIT covers it); on-hardware behavior of anything (static analysis only); the F32 claim that this tree's fpga_control produces Q16 (took PORT_AUDIT's word; only the six >>16 consumer sites were verified present); pixel-exact rendering of the new menus. Minor observations not filed: ui_display_vavg's decimal-cap 'resolution' is computed from samplevoltperdiv without the frozen-zoom rescale applied to the value (worst case one extra/missing decimal in stop-mode zoom — heuristic only); the new factory/clock menus scroll with '-setvalue' (direction bench-validated by extensive use per the repo logs). No tool caps were hit; both large diffs (949 + 687 lines) were read in full.

### code-fpga — FPGA command/path/push audit

Checked: (1) Full diff of fnirsi_1013d_scope/fpga_control.c and fpga_control.h against Atlan4-1.00o5 (205 diff lines, all read) — every changed byte accounted for: 0x29/0x01+0x28/0x00 trace to pecostm32's 1014D fpga_do_conversion (reference read verbatim) and pecostm32-RE bus captures; fpga_dump_ring's 0x1F/0x20-0x23 mirror fpga_read_sample_data and the calibration ADC commands; bounded ready-waits (F26), averagesum (F28), sampling_clock_scale rescale (F33, math verified consistent with the nominal-rate frequency division), comp-gate changes (F23 + the stale >=29 gate reported); no FPGA calls removed relative to Atlan4 (scope_functions.c diff contains zero '-fpga_' lines). Per-channel command bytes (0x02/0x03, 0x32-0x37, 0x20-0x23, 0x24/0x26) verified against pecostm32-RE 'FPGA explained.txt'; command-set comparison vs pecostm32 1014D done (his 0x18 fpga_had_trigger is unused here by design — Atlan4 does software trigger detection; our extra 0x0B/0x0C are fw>1, 0x50-0x52 reported). (2) fw_FPGA 1/2/3 asymmetries walked in fpga_do_conversion, fpga_set_time_base, read-pointer translation (3345/750, 7441, 5393 — unchanged from Atlan4) and interleave_runtime_triggerpoint (consistent). (3) long_mode transitions traced end to end: entry/exit via sm_set_time_base, AUTO (clears long_mode, 0x0F/0x28 self-heal via per-conversion writes), wave-view close (ui_close_view_screen verbatim pecostm32, pushes verified), factory reset (reboots), scope_preset_values (no FPGA writes); roll-mode read loop in test.c read; the missing per-cycle 0x0D/0x28-0x01 reported. (4) Missing-push enumeration: every writer of trigger mode/edge/channel/level/50%/origin, timebase, samplerate, v/div, position, enable, coupling, probe, dcoffset, interleave trim and sampling clock in sm_1014d.c, plus baseline/channel calibration, clock search (saves/restores verified line by line incl. 0x0F re-enable and backlight), auto-setup old+new, input calibration and acqprobe in scope_functions.c — each confirmed to push, be display-side, or self-heal at the next arm; the two genuine gaps (v/div->trigger level, enable->trigger swap) reported; channel-enable-push itself already documented as a decision (PORT_AUDIT F31 note/ROADMAP 24), not re-reported. (5) Init order verified in main(): clock_synthesizer_setup (line 84) before fpga_init (88); config load (142) before all FPGA setting pushes (153-181); long/short branch at boot correct (169-170). Also chased and CLEARED a div-by-zero suspicion: dc_shift_size/dc_shift_center used as divisors in fpga_set_trigger_level are loaded/defaulted at boot via scope_load_input_calibration_data (scope_functions.c:7613-7638, called from the config-restore path at 8253) — no finding; verified libgcc __divsi3 div-by-zero behavior by disassembling the shipped ELF while investigating. Skipped/limits: 1013D-only touch paths (menu.c, statemachine.c, generator.c, test.c UI internals) audited only where they intersect the shared FPGA layer or served as references; USB/storage and display pipelines not audited; no hardware verification possible (build-only repo, no automated tests); FPGA_NOTES' stale '0x14 mask 0x0FFF+2' table entry not reported separately (the 0x1FFF divergence is already recorded in PORT_AUDIT F27).

### code-guards — variant-guard static audit

Scope: every .c/.h/.s in fnirsi_1013d_scope/ (dist/, build/, and the vendored reference trees excluded; references used only for comparison). Enumerated all occurrences with grep -n: 97 PORT_1014D and 5 PORT_A_KEYDEBUG sites, across fpga_control.c, scope_functions.c(.h), fnirsi_1013d_scope.c, variables.c/.h, menu.c, menu_1014d.c, sm_1014d.c, fonts_1014d.c, font_0/2/3/4.c, touchpanel.c, DS3231.c, power_and_battery.c, port_config.h. No .s file contains either macro. Confirmed both variants compile the identical object list (nbproject/Makefile-Debug.mk:38-82); noted in passing that font_6/7/8.c, icons_extract.c, malloc.c, strcpy.s exist in the tree but are absent from the object list (not variant-related). Audit per task class: (1) guard-before-includes — checked the include order of every guard-bearing file individually; all whole-file guards (font_0/2/3/4.c:8 after port_config.h:6; fonts_1014d.c:11 after port_config.h:9; menu_1014d.c:15 and sm_1014d.c:15 after variables.h) and all per-body guards (DS3231, touchpanel, power_and_battery, menu, fpga_control, scope_functions, variables, fnirsi_1013d_scope) come after port_config.h/variables.h — zero instances of the F3/F4 bug class. (2) #ifdef/defined() misuse — grep for ifdef/ifndef/defined over PORT macros found only the PORT_CONFIG_H include guard; every variant test is a value-form #if — clean. (3) cross-variant symbols — scripted a preprocessor-nesting map over all guard-bearing files (all #if/#else/#endif pairs balanced, none unclosed) and a cross-reference scan of all 230 function/global definitions in menu_1014d.c+sm_1014d.c against every other file with guard-state tracking: no unguarded references anywhere (only harmless header declarations in statemachine.h/variables.h); the 1014D-only scope functions (measure_high_rate_artifact, auto_detect_max_clean_sampling_clock, scope_do_acquisition_probe at scope_functions.c:1492/1561/1933) sit inside the 1478-2119 guard with every caller guarded; font_0/2/3/4 dual definitions (font_*.c 1013D vs fonts_1014d.c 1014D) verified complementary; sampling_clock_scale is statically 1.0 (variables.c:40) so the unguarded F33 rescale at fpga_control.c:996 is safe on 1013D; PORT_A_KEYDEBUG=1 links on both variants (toprocesscommand, font_3/5, fpga_get_version all both-variant); verified single live get_fattime (diskio.c copy commented out) and single live ini_SysParam (PC_interface.c:136 copy inside a block comment). Also scanned for 1014D-visible references into the \"1013D-only\" files (finding 2). (4) asymmetric fixes — read every #if/#else PORT region in scope_functions.c, fnirsi_1013d_scope.c, menu.c, fpga_control.c, variables.c, touchpanel.c, DS3231.c and compared suspicious ones against Atlan4-1.00o5 and both pecostm32 references: all asymmetries except finding 1 are documented deliberate 1014D differences (trace-buffer-clear skip, chrome routing, copy-rect widths, restore-screen skip, boot-sector-write skip, composite-menu gate); coupling push (F31) verified symmetric (Atlan4's 1013D already had it at statemachine.c:1927/1947; sm_1014d.c:2492 added parity); trigger_position_min/max fix is unguarded shared code benefiting both variants. (5) inverted guard senses — none; menu.c:102-108 (generator-settings skip) is unreachable on 1014D because scope_setup_main_screen routes-and-returns at menu.c:42-46 (redundant but harmless). Skipped/limits: did not run make (per instructions — findings on 1013D-variant linkability are static analysis); did not audit non-variant correctness of shared code or the USB/CDC protocol behavior on 1014D (usb_CDC_receive_callback is live on both variants — same code, not a guard issue); regex-based symbol scans could in principle miss macro-generated identifier references, but this codebase does not use that style.

### code-gotchas — signedness/string/buffer/blocking-loop hazard hunt

Scope: first-party fnirsi_1013d_scope/ only; vendored trees used strictly as references. All cited lines read in this checkout.

(a) Signedness: Confirmed CFLAGS (nbproject/Makefile-Debug.mk:86) carry neither -fsigned-char nor -funsigned-char, so plain char is UNSIGNED (arm-none-eabi default); -fno-builtin-strcpy and -Wno-char-subscripts present. Audited types.h, all key/rotary handling (uart.c returns uint8; toprocesscommand is uint8; UIC_* codes are 0..~50 so no sign issues in dispatch), every speedvalue/setvalue consumer in sm_1014d.c (int8 deltas against uint16 fields -> findings 1-3; index/menuitem/newitem/probeindex/slider/adc-comp paths verified safe as int16/int32/clamped int8), int8 compensation fields (int16, safe), display_character int8 parameter (finding 9), compute_all_math_ops int8 wrap (finding 6). Grepped for char-vs-negative comparisons across all .c: only ff.c's internal prec<0 (int, fine).

(b) Asm routines: read all six in full. strlen, memset, memcmp are ISO-conformant (traced). memcpy and memmove are byte-identical direction-aware copies (overlap-safe both ways); quirk: src==dst returns NULL instead of dest (the 'len=0' comment is wrong) — verified NO first-party caller uses a memcpy/memmove/memset return value, so not reported as a finding. strcpy.s (returns dest+strlen+1, past the NUL) is NOT in the build object list (Makefile-Debug.mk links only memcmp/memcpy/memmove/memset/strlen); the live strcpy is the C one at scope_functions.c:5106 returning the terminator position, which the `buffer = strcpy(buffer, ...)` concat idiom in scope_functions.c/menu_1014d.c requires — all `= strcpy` call sites checked, all use the concat idiom. This whole area is already documented as PORT_AUDIT.md F2 (retracted, -fno-builtin-strcpy insurance), so not re-reported; note strcpy.s remains in the tree as a latent landmine if ever added to the build.

(c) Buffer geometry: verified the F17 705-wide 1014D copy rect vs 728 Atlan4 path (scope_functions.c:3785/3794 — correct, offscreen 728/730-wide fills are into the 800-wide buffer, safe); acqprobe ring dump correctly sized (5x4608 static vs count); fpga_read_sample_data/fpga_read_adc_data stride-2 writes bounded by totalsamples (default 3000, 1013D memory-deep max 15000, both < 16380) except the USB debug path (finding 4); the 16382-vs-16380 floor-division mismatch (finding 5); timebase tables: sm_set_time_base uses sizeof-derived bounds and clamps 0..34, time_per_div_sample_rate[35] values max 28 fit sample_rate[29]/freq_calc_data[29]/sample_rate_settings (fpga_control.c:388 also bounds-checks); screen_time_calc_data correctly extended to [35] here (pecostm32's was [24] — seam handled); volt_calc_data/volt_div_texts_short/vertical_scaling_factors [7][7] vs magnification {1,2,5} and voltperdiv 0..6 — in range; probe index wraps 0..2; measurement_names[12] == measurement_functions[12]; wave-file restore (menu_1014d.c:3309-3350) is gated by ID+version (0x01000002, distinct from pecostm32's 0x00000001) and a checksum before ui_restore_setup_from_file, so cross-firmware/corrupt index injection is blocked; config-sector restore is checksum+version gated (scope_functions.c:8088) but does not range-check totalsamples (folded into finding 4). X-Y negative-index reads = finding 7. The 16-bit truncated triggerhorizontalposition config save (scope_functions.c:7923 '&0xFFFF //int32!!!') is pre-existing Atlan4 (identical lines) and self-heals via the range clamp on next recalculation — not reported.

(d) Blocking loops: uart.c (finding 8, provenance: byte-identical to pecostm32, absent from Atlan4). clock_synthesizer.c I2C is write-only bit-bang with fixed delays, no ACK sampling, cannot hang. fpga_control.c: both conversion-ready waits are already timeout-bounded (2M iterations, lines ~653-671); fpga_check_ready (fpga_control.c:214-228) spins forever on version==0x1432 only (would hang with the 0x1532 Atlan4/rebuilt FPGA) but is DEAD CODE — no caller in this tree; same body in both references; worth deleting or fixing before the FPGA-swap project revives it. sd_card_interface.c, spi_control.c, timer.c, ccu_control.c, display_control.c, usb_interface.c verified byte-identical to Atlan4-1.00o5 (diff -q), so their unbounded register waits (e.g. SD0_CMDR start, SPI FIFO) are pre-existing Atlan4 behavior, working on hardware — not itemized. DS3231.c/touchpanel.c differ from Atlan4 only by the F3/F4 #if !PORT_1014D guard insertions (diff shows guards only); their waits are pre-existing and 1013D-only.

Skipped / not audited in depth: ff.c/ffunicode.c (FatFs internals), malloc.c, mass_storage_class.c/cdc_class.c protocol internals, icons/fonts data content, generator.c, PC_interface.c and menu.c/statemachine.c beyond the specific items above (1013D-only, never hardware-tested per docs), interrupt handling, and display_lib draw-clipping internals beyond the character path. No caps hit; no truncation.

### doc-fpga-boot — FPGA_NOTES.md + BOOT_NOTES.md

CHECKED (every claim personally verified against this checkout): FPGA_NOTES — bus/PE macros (fpga_control.h:14-38); fpga_get_version mapping 0x1432/0x1532/0x1632→fw_FPGA 1/2/3 (fpga_control.c:197-210); full command inventory vs code: 0x01, 0x04, 0x05 (both bounded 2,000,000-iteration waits, F26 double-read), 0x06, 0x0A, 0x0D (+0x07D0 comment), 0x0E fw1-vs-fw2/3 branch, 0x0F polarity, 0x14, 0x15/0x16/0x17, 0x1A, 0x1F, 0x28/0x29 (F25 restore + still-commented roll copy), 0x38, 0x3C, 0x0B/0x0C fw>1 guard, 0x50-0x52; cross-checked against 'Atlan4-FPGA/FPGA commands.txt' and pecostm32's 1014D fpga_control.c; long/short timebase (fpga_set_long_timebase gutted to 0x0F/0x01; scope_set_timebase 9/10 skip; scope_get_long_timebase_data exists; timebase_settings 411100 at 10ns incl. Slovak auto-timeout comment); capture geometry code claims (fw1 +3345/−750 in scope_functions.c:810-849 and acqprobe copy at 1996-2006; 0x1F rewind between ADC1/ADC2 reads at fpga_control.c:787/859; voltperdiv clamp to 5; nofsamples=totalsamples/2); fw2/3 constants (+7441/+5393); acquisition probe end-to-end (scope_do_acquisition_probe phases A/B/C, ACQP magic + six LE uint32 header + five command bytes 0x20/0x21/0x22/0x23/0x20 + 5×4608=23040 bytes, files acqprobe.txt/ringdump.bin) and tools/acqprobe_analyze.py (--csv, 'capture fill geometry' section present); clock search committed state (77af34b) and code presence (auto_detect..., sm_open_clock_menu, NAV_CLOCK_MENU_HANDLING); comp-apply gate current state (unconditional, F23/b0daa6b); MS5351M p1b arithmetic (800/(2·p1b+4) → 57.1/66.7/80); clock_synthesizer CLK0=200M/CLK1=50M/CLK2 powered down; sample_rate_settings first-18-entries identical Atlan4 vs pecostm32 (arrays 29 vs 18 long — doc's 'encodings identical' is fair); special-IC section vs pecostm32-RE ('FPGA explained.txt' 0x28 line 76, 0x41/0x8150 endless-loop line 95, 0x64-0x69 semantics lines 99-106; parameter_analysis.txt 0x0C/0x0D/0x16; readme.txt 100mS cutoff + 'really needed' quote; 0x64-0x6E absent from both firmwares by grep); Atlan4-FPGA layout + FPGA.bin=282,898 B; zaklad.v 0x06→0x15; fpga/ + fpga/AL3_1014D contents, FPGA_1014D.bin size byte-identical, id packet cc55aa33 f0000006 18006c31 @0x20 and ec f0 0433 marker verified by xxd; fpga/README.md do-not-flash gates; TD downloads/install/shim dirs in ~/tools/anlogic-td; bench/acqprobe run dirs + EEVBlog html + gitignore. BOOT_NOTES — Makefile variant/bootloader echo + overlay offsets 0x8000/0x5BC00; pecostm32 startup full flow (init order, live fpga_check_ready spin-until-0x1432 in bl_fpga_control.c:97-111, key idle 49/held 0, F1/F2/F3→choices, sector-80 eGON.EXE load to 0x80000000 header-skipped, SPI 0x27000 fallback, FEL 0xFFFF0020, backlight 0x78); sector defines 708/709/710 (variables.h:102-108); STARTUP_CONFIG_ADDRESS uint32* at 0x…1F quirk; scope_reset_config_data ptr[0]=4 + #if !PORT_1014D sector write guard; display-config magics AAAAAAAA/55555555/CCCCCCCC/33333333 + checksum word 6 + fallback timings 0x041E0044/0x041A0017 (scope and loader copies); fw0.04 loader (sector-710 read to 0x81BFFC00, uint8* address, choice>3 menu + &0x03, touch menu, live fpga_check_ready); v0.8 strings in committed bootloader_base.bin; sector-80 = image 0x8000 + 8192 dd offset arithmetic. Status labels audited against git log --since=2026-07-12 and actual fpga/-tools/ contents. SKIPPED / NOT VERIFIED: the v0.8 dead-code claim (zero call sites at ~0x1340/~0x125dc) — would need re-disassembly of the binary, taken on the doc's stated method; hash equivalence of bootloader_1014d_base.bin to a local rebuild; all bench-measured numbers (rates, fill depths 910/2507/679-1481, comp values, TD timing tables) — unverifiable from the repo, checked only for internal arithmetic consistency (911−750=161, 750/3000=25%, 23040 B, 282,898 B) and that the referenced data/scripts exist; pecostm32-RE pin-by-pin pinout-diff claims (P7/P23-P24/P33-34/P38-P50 spot-existence only via doc-internal consistency, not re-derived from the .adc files); GitHub-side claims (Atlan4 repo contents, bootloader v0.7.bin, Sipeed mirror mechanics); USB-mass-storage-exposes-sector-0 claim; PORT_A_KEYDEBUG overlay '1432' cosmetic claim. No caps hit; both docs read in full.


---

# Follow-up audits (2026-08-21, same day)

The coverage critic's five gaps, each closed by a dedicated agent (sequential run,
workflow `wf_7da5adae-958`), all findings adversarially verified: 28/28 CONFIRMED.
Fix disposition per finding is recorded in PORT_AUDIT.md §5e addendum; the roll-mode
patch below was applied the same day (hw-verify pending).

## test.c deep pass + roll-mode fix design

### F21 mechanism 1: in roll mode the 1014D main loop runs TWO renderers that fight, and roll poisons the standard renderer's geometry state

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/fnirsi_1013d_scope.c:442`
- **Severity:** high — **CONFIRMED**

The PORT_1014D main loop routes long_mode to scope_get_long_timebase_data() (fnirsi_1013d_scope.c:391-393), which draws the roll trace incrementally straight into maindisplaybuffer (test.c:997-1021), and then UNCONDITIONALLY also calls scope_display_trace_data() every pass (fnirsi_1013d_scope.c:440-445, gated only on enabletracedisplay, which defaults 1 — variables.c:13, sm_1014d.c:42). scope_display_trace_data has no long_mode early-return (scope_functions.c:3450-3453; long_mode only rescales disp_xpos_per_sample at 3490): it repaints the whole trace window into displaybuffertmp and blits (2,48,705,432) to screen (scope_functions.c:3796-3800), overwriting the roll renderer's pixels in x=2..706 every pass. Worse, the roll loop feeds it garbage: test.c:1035-1038 writes SAMPLE-INDEX values 375/1125/1875/2625 into scopesettings.triggerhorizontalposition, a SCREEN-coordinate field whose valid range is ~6..704 (variables.h:761, clamps at sm_1014d.c:1323-1332), and test.c:1031 forces disp_have_trigger=1 so disp_trigger_index keeps its stale value from the last short-mode capture (scope_functions.c:3472-3479). With triggerposition=1125..2625, disp_xstart=triggerposition-xrange lands beyond disp_xend after the 704 clamp (scope_functions.c:3518-3536), so the standard renderer draws little or nothing inside the grid — the roll trace is visible mainly as the junk the roll renderer leaves OUTSIDE the standard blit (x=705..726), which is exactly the bench-observed F21 symptom ('trace crawling past the right edge'). Atlan4's 1013D flow never has this fight: its main loop calls only touch_handler() (line 446-449), so scope_display_trace_data runs on touch events only. This per-pass call is a port addition; test.c itself is byte-identical to Atlan4's (diff verified).

**Evidence:** fnirsi_1013d_scope.c:440-445: '#if PORT_1014D sm_handle_user_input(); if(enabletracedisplay || ui_menu_composite_active()) { scope_display_trace_data(); }'. test.c:1031: 'disp_have_trigger=1;  //musi byt 1 aby nevlozilo sample/2 tj 1500'. test.c:1035-1038: 'if (scopesettings.count < 750) { disp_xstart = 0; disp_xend = 750; scopesettings.triggerhorizontalposition = 375;} ... else if (scopesettings.count < 3000) { ... scopesettings.triggerhorizontalposition = 2625;}'. scope_functions.c:3468 'int32 triggerposition = scopesettings.triggerhorizontalposition;' and 3533-3535 'if(disp_xend > 704) { disp_xend = 704; }'.

**Suggested action:** Make one renderer own roll mode: gate the main-loop scope_display_trace_data call on !scopesettings.long_mode (keeping the ui_menu_composite_active() leg if overlay menus must stay live during roll), or alternatively drop the roll renderer's screen drawing and teach the standard path to render the roll buffer. Also stop writing sample indices into triggerhorizontalposition from test.c (those values only make sense for the thumbnail/save path and should go through a separate variable). Coordinate with the geometry finding below — both are needed to close F21.

**Verifier:** Reproduced every element: fnirsi_1013d_scope.c:391-393 routes long_mode to scope_get_long_timebase_data(); lines 440-445 (#if PORT_1014D) call scope_display_trace_data() every pass gated only on enabletracedisplay (=1 at variables.c:13, re-set in sm_init at sm_1014d.c:42) || ui_menu_composite_active(). scope_display_trace_data has no long_mode early-return (scope_functions.c:3449-3453; long_mode only rescales disp_xpos_per_sample at 3490) and blits (2,48,705,432) at 3797, overwriting x=2..706 each pass. test.c:1031 forces disp_have_trigger=1 ('musi byt 1...'), and 1035-1038 write sample indices 375/1125/1875/2625 into triggerhorizontalposition, an int32 screen-coordinate field (variables.h:761) clamped to trigger_position_min/max (screen coords from disp_xrange) at sm_1014d.c:1323-1332. Standard renderer clamps 6..704 (scope_functions.c:3527-3535). test.c is byte-identical to Atlan4-1.00o5's (diff exit 0); Atlan4's own main loop calls only touch_handler() (its line 363) with scope_display_trace_data commented out at 216 — the per-pass call is a port addition. The 'draws little or nothing' sub-narrative varies with xrange (some segments draw full-width garbage instead of nothing), but the dual-renderer fight mechanism is exactly as stated.

### F21 mechanism 2: roll renderer uses Atlan4 1013D geometry — draws to x=726 and fills/blits (0,48,730,432) over the P14 sidebar and border

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/test.c:992`
- **Severity:** high — **CONFIRMED**

scope_get_long_timebase_data sweeps xpos 4..726 (reset at >726, test.c:960/985-986, line draw at 1006/1019 up to x=726), and on each sweep wrap black-fills and blits (0,48,730,432) (test.c:967/992). The 1014D P14 layout ends the grid at x=704 and the F17-corrected per-frame trace blit is (2,48,705,432) covering x=2..706 (variables.h:143-157: border x=5 w=701, TRACE_HORIZONTAL_END=705; scope_functions.c:3796-3800 comment: 'the trace window ends at x=706 (grid at 704) and the measurements sidebar starts right after it'). So the roll renderer (a) draws trace pixels 22 px past the grid end into the sidebar seam (x=705..726) where the standard renderer's 706-wide blit never erases them — the persistent 'crawl past the right edge'; and (b) once per sweep blits displaybuffertmp x=0..729 rows 48..479 over the sidebar columns x=707..729 and the left border x=0..1. displaybuffertmp's content there is the standard renderer's fill (2,48,728,432) i.e. black (scope_functions.c:3583-3588), so each sweep wrap paints a black band over the F17-protected sidebar chrome — the 'trace-area shadow at the wrong location'. test.c is byte-identical to vendored Atlan4-1.00o5/fnirsi_1013d_scope/test.c (diff verified), where these numbers are correct for the 1013D layout (blit clamp 727, scope_functions.c:3544-3547 #else branch); they were never ported to P14 geometry. The dead sibling scope_get_long_timebase_data1 has the same rects with height 403 (test.c:713/810).

**Evidence:** test.c:960-992: 'if ( scopesettings.xpos >726) { display_set_screen_buffer(displaybuffertmp); display_set_fg_color(BLACK_COLOR); display_fill_rect(0, 48, 730, 432); ... scopesettings.xpos = 4; scopesettings.lastx = 3; ... display_copy_rect_to_screen(0, 48, 730, 432); }' vs the P14 standard blit scope_functions.c:3800 'display_copy_rect_to_screen(2, 48, 705, 432);' and P14 clamps scope_functions.c:3527-3535 (6..704).

**Suggested action:** Under #if PORT_1014D use the P14 numbers in scope_get_long_timebase_data: sweep x within 6..704 (reset xpos=7/lastx=6, wrap test at >704), fill/copy (2,48,705,432); keep the 1013D numbers in the #else per the no-1013D-changes policy. Do this together with the single-renderer decision above.

**Verifier:** test.c:960-994 verified verbatim: xpos>726 reset, display_fill_rect(0,48,730,432) into displaybuffertmp, xpos=4/lastx=3, display_copy_rect_to_screen(0,48,730,432); line draws at 1006/1019 reach x=726. P14 geometry verified: variables.h:146-157 (border x=5 w=701, TRACE_HORIZONTAL_END=705), F17 blit (2,48,705,432) + comment at scope_functions.c:3792-3797. Standard renderer's fill (2,48,728,432) at 3588 leaves displaybuffertmp black in sidebar columns, so each sweep-wrap blit paints a black band over x=707..729 (and 0..1) rows 48-479. Dead sibling scope_get_long_timebase_data1 rects with height 403 confirmed at test.c:713/810. test.c byte-identical to Atlan4 (diff exit 0), where 1013D clamps are 3..727 (scope_functions.c:3538-3547 #else).

**Correction:** Trivial: the standard blit (2,48,705,432) covers x=2..706, so the never-erased roll pixels are x=707..726 (20 px), not x=705..726.

### Roll normal/single trigger permanently dead after uint16 triggerverticalposition wrap — the one consumer where the wrap is NOT annihilated

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/test.c:1062`
- **Severity:** high — **CONFIRMED**

scopesettings.triggerverticalposition is uint16 in this tree (variables.h:762) but int16 in pecostm32's official 1014D (reference FNIRSI_1014D_Firmware/fnirsi_1014d_scope/variables.h:459), whose handlers this tree imported. Two live writers can store a negative value that wraps to ~65xxx: (1) sm_1014d.c:1639 'scopesettings.triggerverticalposition += delta' — deliberately unclamped ('without worry about limit'), goes negative when the trace is moved down while the trigger pointer sits low; (2) sm_set_trigger_level's own lower clamp sm_1014d.c:1371 'triggerverticalposition = ((-127 * signal_adjusters[voltperdiv]) >> VOLTAGE_SHIFTER) + traceposition' assigns trp-218 (signal_adjusters 3.6M-3.8M, variables.c:991) or trp-460 at voltperdiv 6 (7.6M) — negative whenever traceposition<218 (autoset/50% setup set it to 200, scope_functions.c:2481/2494; always negative at voltperdiv 6 since traceposition<=399). REVIEW-2026-08-21 (line 1113) proved this wrap harmless for the FPGA paths because both multiply by 2^21, annihilating the mod-2^16 error mod 2^32. But scope_check_long_trigger does NOT multiply: test.c:1062 'level = scopesettings.triggerverticalposition+25' promotes the wrapped uint16 to ~65540 and compares it against sample1/sample2 <= 448 (test.c:1108-1110) — neither edge can ever fire. In roll normal/single mode (triggermode!=0 forces triggerlong=0, scope_functions.c:280; data acquisition is gated on triggerlong, test.c:835) the scope then waits forever with no trace, until the user happens to dial the level back into range (the level<0/level>255 re-clamp at sm_1014d.c:1368-1377 restores a sane value on the next detent). Failure recipe: roll timebase, normal mode, spin trigger-level rotary to the bottom stop with the trace at/below center — acquisition freezes on WAITING.

**Evidence:** variables.h:762 'uint16 triggerverticalposition;' vs reference int16; sm_1014d.c:1637-1639 '//Move the trigger pointer without worry about limit ... scopesettings.triggerverticalposition += delta;'; test.c:1062 'level = scopesettings.triggerverticalposition+25;   //+25 compensation'; test.c:1108-1110 compare sample1/sample2 (max (255*3689650)>>21 = 448) against level.

**Suggested action:** Either change the field to int16 to match the pecostm32 reference semantics (audit the config save/restore raw-buffer copies — bit patterns identical, so save format is unaffected), or compute the roll level in a signed local: 'int32 level = (int32)(int16)scopesettings.triggerverticalposition + 25;' plus treat sample comparisons as signed. The f0561ee signed-local pattern applies; this is the same class it fixed for traceposition and the cursors.

**Verifier:** All links re-derived: variables.h:762 uint16 triggerverticalposition vs reference FNIRSI_1014D_Firmware/fnirsi_1014d_scope/variables.h:459 int16. Writers: sm_1014d.c:1639 unclamped '+= delta' ('without worry about limit'); sm_1014d.c:1371 lower clamp (-127*signal_adjusters[vpd])>>21 + traceposition = trp-219 (3.6M) / trp-461 (7.6M at vpd 6, always negative since traceposition<=399); autoset sets traceposition=200 (scope_functions.c:2481/2494), so trp<219 wraps to ~65317. test.c:1062 'level = triggerverticalposition+25' promotes to ~65342; max sample = (255*3689650)>>21 = 448 (multiply=3689650 variables.c:994, VOLTAGE_SHIFTER=21 variables.h:297), so neither edge test at test.c:1108-1110 can fire. Gate verified: scope_functions.c:280 'if(triggermode) triggerlong=0 else 1' and test.c:831/835 gate acquisition on triggerlong. Freeze-until-redial scenario is real (uint16 wrap class matches f0561ee's other fixes, which did not touch this one).

**Correction:** Arithmetic nit: -127*3600000>>21 rounds to -219 (finding says trp-218) and 7.6M gives -461 (finding says -460); immaterial to the bug.

### Roll trigger's '+25 compensation'/global multiply is wrong on the calibrated 1014D: ignores input_calibration, v/div, trace position and DC offset

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/test.c:1080`
- **Severity:** medium — **CONFIRMED**

scope_check_long_trigger converts the raw ADC average to 'screen space' as sample = avg * multiply >> 21 with the global const multiply=3689650 (gain 1.759 px/count, variables.c:994), the per-channel calibrated forms being commented out (test.c:1077-1078/1094-1095), and compares against level = triggerverticalposition + 25. The correct screen mapping (scope_get_y_sample, scope_functions.c:3865-3896) is (avg-128)*input_calibration[samplevoltperdiv]>>21 + traceposition + dcoffset-term. The two agree ONLY at traceposition≈200 (128*1.759=225=200+25 — that is what the +25 magic encodes) AND input_calibration≈3689650. On the current calibrated 1014D input_calibration is tuned per channel per v/div in 3000000..4000000 (scope_functions.c:2326-2345), i.e. gain 1.43-1.91 — up to ~23% off the hard-coded 1.76; and the formula tracks the trace-position pointer with slope 0 instead of 1: move the trace to position 100 and put the trigger pointer on the visible trace, and the actual ADC threshold is (100+25)/1.759≈71 instead of 128 — the roll trigger fires ~2 divisions away from where the pointer says. DC offset (dcoffset/dc_shift_size, used by both scope_get_y_sample and fpga_set_trigger_level fpga_control.c:493-512) is ignored entirely. Answer to the audit question: yes, the magic is wrong on the calibrated unit except at the default center geometry. Note this is verbatim Atlan4 code (test.c byte-identical to the vendor tree), so the 1013D keeps it as-is per policy; a fix must be 1014D-guarded or clearly shared-safe.

**Evidence:** test.c:1062 'level = scopesettings.triggerverticalposition+25;   //+25 compensation'; test.c:1080-1081 'sample1 = ((sample1AVG) * multiply) >> VOLTAGE_SHIFTER;' with the calibrated forms commented at 1077-1078; variables.c:994 'const int32 multiply = 3689650;'; VOLTAGE_SHIFTER=21 (variables.h:297); calibration search bounds 3000000..4000000 at scope_functions.c:2326-2344.

**Suggested action:** Compare in ADC space instead of inventing a second screen mapping: fpga_set_trigger_level already computes the exact ADC-space level from triggerverticalposition, traceposition, dcoffset and signal_adjusters and stores it (fpga_control.c:512-516, scopesettings.triggerlevel) — have scope_check_long_trigger compare the raw averages (sample1AVG/sample2AVG) against that. Caveats: triggerlevel is uint16 and is stored BEFORE the 0..255 clamp (fpga_control.c:516-526), so either store the clamped value or clamp locally; and push fpga_set_trigger_level()/recompute on roll entry so the value is fresh.

**Verifier:** Verified: roll trigger uses sample = avg*3689650>>21 with calibrated forms commented out (test.c:1077-1081/1094-1098) and level = trp+25 (1062). Correct screen mapping confirmed at scope_get_y_sample (scope_functions.c:3869-3896): (raw-128)*input_calibration[samplevoltperdiv]>>21 + traceposition + (dcoffset*100)/dc_shift_size (doubled at vpd 6). Calibration search bounds 3000000..4000000 confirmed at scope_functions.c:2326-2344. fpga_set_trigger_level uses the dcoffset term (fpga_control.c:493-512). The math checks: 128*3689650>>21 = 225 = 200+25, so the +25 encodes traceposition 200 with the default gain; the roll formula tracks traceposition with slope 0 and ignores calibration and DC offset. test.c byte-identical to Atlan4, so 1013D-as-is policy note is apt.

### Roll wait loop blocks all key input with no abort on the 1014D — up to 1 s per sample at 50 s/div

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/test.c:895`
- **Severity:** medium — **CONFIRMED**

scope_get_long_timebase_data paces samples with 'while((havetouch == 0)) { curticks = timer0_get_ticks(); if(curticks >= timeout) goto skip_delay; tp_i2c_read_status(); }' (test.c:895-912) with timeout 10..1000 ms per sample by timeperdiv (1 ms tick — timer.c:15/38). On the 1013D a touch sets havetouch and aborts the wait; on the 1014D tp_i2c_read_status() is an empty stub (touchpanel.c:185-187, body '#if !PORT_1014D'-guarded; havetouch only ever set at touchpanel.c:234 inside the guard), so the loop can ONLY exit by timeout, and sm_handle_user_input (fnirsi_1013d_scope.c:441) — the only key poll — runs once per sample. At 50 s/div that is one key/rotary event processed per second; a rotary burst during roll is served at 1 detent/s while the blocking uart1 poll queue drains. No crash, but the scope feels hung at the slowest timebases and there is no in-loop UART equivalent of the touch abort. (Roll currently reachable for timeperdiv 0..6, i.e. 50s..500ms — sm_1014d.c:1462-1466.)

**Evidence:** test.c:895-912 wait loop; touchpanel.c:185-188 'void tp_i2c_read_status(void) { #if !PORT_1014D ...'; timer.c:15 '*TMR0_INTV_VALUE_REG = 24000;' + :36-38 1 ms tick; timeout table test.c:839-887 (case 0 -> 1000).

**Suggested action:** Inside the 1014D wait loop, poll the key controller (or at least check for a pending byte non-blockingly) and break out on input, mirroring the 1013D touch abort; alternatively shorten the wait granularity and run sm_handle_user_input between sub-waits. Needs care with uart.c's blocking write-0xFF/wait protocol (CLAUDE.md known constraint).

**Verifier:** test.c:895-912 wait loop exits only on timeout on 1014D: tp_i2c_read_status body is '#if !PORT_1014D'-guarded (touchpanel.c:185-187) and both havetouch writes (=1, =0) sit inside that guard (verified #endif placement). timer.c: 1 ms tick (TMR0_INTV=24000 of OSC24M, timer0ticks++ per IRQ). Timeout table test.c:839-887: case 0 -> 1000 ms; roll reachable only for timeperdiv 0..6 (sm_1014d.c dial remap: index 7 -> 11 sweep, 10 -> 6 roll), so effective per-sample waits are 10..1000 ms. sm_handle_user_input is called once per main-loop pass (fnirsi_1013d_scope.c:441), i.e. once per roll sample, so 1 key event/s at 50 s/div is correct.

### test.c dead code inventory: scrolling roll variant, DSP helpers with 16 KB stack array, CSV exporter — all uncalled but linked into both variants

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/test.c:583`
- **Severity:** low — **CONFIRMED**

Verified callers across the tree (grep, excluding vendor trees): scope_get_long_timebase_data1 (test.c:583, the alternate scrolling roll renderer with its own divergent 403-height rects and CH1-only support) has NO callers; scope_preset_long_mode exists only as a commented stub (test.c:565-579, prototype commented at test.h:43, sole reference commented at fnirsi_1013d_scope.c:260); fpga_average_trace_data_long (fpga_control.c:1480) has zero callers; moving_average, exponential_moving_average, fir_filter, filter_bit_changes, add_one_bit, average_filter, convert_waveforms_to_csv_excel, scope_load_REFx_trace_data and display_text_message are all uncalled (only commented references, e.g. menu.c:2319). The link has no -ffunction-sections/--gc-sections (nbproject/Makefile-Debug.mk:107), so all of it plus the double soft-float libgcc support pulled in by fir_filter/exponential_moving_average ships in the flash image of BOTH variants — pure code-size cost (ROADMAP 7 class). Latent hazards if ever wired up: fir_filter allocates uint8_t output[MAX_SAMPLE_BUFFER_SIZE] = 16382 bytes on the stack (test.c:485, variables.h:528) and moving_average a size-byte VLA (test.c:440) — either would smash the bare-metal stack for full-buffer sizes. Soft-float answer for the audit: the LIVE roll loop (scope_get_long_timebase_data + scope_check_long_trigger) is integer-only; the only per-frame double math in roll is scope_display_trace_data's disp_* computation, which short mode already pays every frame — soft-float is not a roll-specific cost today. Live test helpers: scope_test_trigerposition_value is reachable under dev_mode (fnirsi_1013d_scope.c:426, dev_mode default 0 at variables.c:161) and paints a 100x220 rect at x=600 over the P14 sidebar; scope_test_TP is live only in 1013D statemachine.c paths.

**Evidence:** grep across *.c/*.h: scope_get_long_timebase_data1 / fpga_average_trace_data_long / the DSP-helper names appear only at their definitions, in test.h prototypes, and in commented call sites; Makefile link line 'arm-none-eabi-gcc -o ... ${OBJECTFILES} ... -nostdlib -lgcc' with no --gc-sections; test.c:485 'uint8_t output[MAX_SAMPLE_BUFFER_SIZE];' with MAX_SAMPLE_BUFFER_SIZE 16382 (variables.h:528).

**Suggested action:** Delete (or #if 0 with a provenance comment) scope_get_long_timebase_data1, the DSP helpers and the CSV exporter, and drop fpga_average_trace_data_long, per ROADMAP 7; keep scope_load_REFx_trace_data only if the REF feature plan needs it. If any DSP helper is ever revived, replace the stack buffers with a static/heap buffer first. test.c is byte-identical to Atlan4's, so any cleanup is a deliberate divergence — document it in PORT_AUDIT.

**Verifier:** Grep across first-party *.c/*.h reproduces the inventory: scope_get_long_timebase_data1, fpga_average_trace_data_long, moving_average, exponential_moving_average, fir_filter, filter_bit_changes, add_one_bit, average_filter, convert_waveforms_to_csv_excel, scope_load_REFx_trace_data, display_text_message appear only at definitions/prototypes/commented call sites; scope_preset_long_mode stub commented everywhere. Link line has no --gc-sections/-ffunction-sections (Makefile-Debug.mk:107 'arm-none-eabi-gcc -o ... -nostdlib -lgcc'). fir_filter declares uint8_t output[MAX_SAMPLE_BUFFER_SIZE=16382] on stack (test.c:485, variables.h:528); moving_average has VLA uint8_t temp[size] (test.c:440). dev_mode=0 default (variables.c:161); scope_test_trigerposition_value live under dev_mode (fnirsi_1013d_scope.c:426) fills 100x220 at x=600,y=100 over the P14 sidebar (test.c:1298-1305); scope_test_TP live only at statemachine.c:1853/2982/3486 (1013D).

### Design proposal (test-c)

```
ROLL-MODE 0x28/0x01 FIX (F25's other half) — design only, not applied.

== Stock sequence derived from the captures (stock FPGA 0x1432; capture is from the stock 1013D firmware — same FPGA family/version as the 1014D's, flagged below) ==
Per read cycle, repeating (pecostm32-RE/'FPGA explained'/100mS_50S-div_signal_data_read_sequence.txt:63-82, four identical cycles):
  0x0D  write 4  data 0x00 0x00 0x07 0xD0     (rate; 0x7D0 for ALL nine roll timebases — FPGA explained.txt:205-214)
  0x28  write 1  data 0x01                     (mode select: roll — FPGA explained.txt:76)
  0x24  read 10                                (CH1 data)
  0x26  read 10                                (CH2 data)
Corroborated by FPGA explained.txt:202: '0x28 is written 0x01 after or before every read sequence. 0x0D is also written.'
Mode entry / after touch (100mS_50S:60-64 preceded by the special-IC dance; FPGA explained.txt:218-219): 0x0D <- 0x00 0x00 0x00 0x27, then 0x0D <- 0x00 0x00 0x07 0xD0 ('a kind of reset which is only done when time base is switched to 100mS and up'). No 0x0F appears anywhere in the roll capture; 0x0F/0x00 -> 0x28/0x00 -> ... -> 0x0F/0x01 is the SHORT-mode cycle (50mS capture lines 5-12).

== What our tree sends today (verified) ==
Entry (sm_1014d.c:1469-1475 -> fpga_set_long_timebase, fpga_control.c:576-601): only 0x0F/0x01; the 0x28/0x01 is dead-commented at :599-600 ('0X28 not support in FPGA???'). Then sm_1014d.c:1491-1496 -> fpga_set_sample_rate -> 0x0D + 0x00061A7F (sample_rate_settings[22]=399999, ~500 Sa/s — ALL roll indices map to it; variables.c:421-435/794-825) — a rate value that diverges from stock's 0x7D0 (~100 kSa/s). Per cycle (test.c:949-957 -> fpga_average_trace_data, fpga_control.c:1443): only the 0x24/0x26 averaged reads — no 0x0D, no 0x28. Net: the stock FPGA runs roll with the mode-select still at 0x28/0x00 (last written by fpga_do_conversion's F25 restore, fpga_control.c:642-643) and at an invented rate.

== Proposed change ==
(1) fpga_control.c — add a helper immediately AFTER fpga_set_long_timebase (ends at line 601), keeping all silicon-tied bytes in fpga_control.c per repo convention:

//----------------------------------------------------------------------------------------------------------------------------------
//Roll-mode per-read-cycle arm (F25's other half). The stock FPGA is fed 0x0D (rate) + 0x28/0x01
//before EVERY 0x24/0x26 read cycle, not just at mode entry: pecostm32-RE "FPGA explained".txt:202
//and 100mS_50S-div capture lines 63-82 (0x0D 0x000007D0 -> 0x28 0x01 -> 0x24 x10 -> 0x26 x10,
//repeating). Stock writes 0x000007D0 for ALL roll timebases (table lines 205-214) and paces the
//reads with a timer, exactly like scope_get_long_timebase_data's timeout loop does.
//Stock fw only: PECO FPGAs (fw_FPGA 2/3) have their own long-timebase handling. 1014D only:
//the 1013D variant stays at Atlan4's shipped behavior (no hardware to test — policy 2026-08-21).
void fpga_arm_long_timebase_cycle(void)
{
#if PORT_1014D
  if(fpgasettings.fw_FPGA == 1)
  {
    //ADC clock rate for roll mode: stock always 0x000007D0 here (fpga_write_int is MSB-first,
    //matching the capture byte order 0x00 0x00 0x07 0xD0)
    fpga_write_cmd(0x0D);
    fpga_write_int(0x000007D0);

    //Mode select: 0x01 = roll (counterpart of the 0x28/0x00 fast select in fpga_do_conversion)
    fpga_write_cmd(0x28);
    fpga_write_byte(0x01);
  }
#endif
}

Prototype in fpga_control.h next to fpga_set_long_timebase's declaration.

(2) fpga_set_long_timebase (fpga_control.c:576-601) — replace the dead lines 598-600 with the guarded stock entry sequence, keeping the existing 0x0F/0x01:

  //Disable trigger system???
  fpga_write_cmd(0x0F);
  fpga_write_byte(0x01);

#if PORT_1014D
  if(fpgasettings.fw_FPGA == 1)
  {
    //Stock roll-entry "reset": 0x0D <- 0x27 then the run rate + mode select
    //(pecostm32-RE FPGA explained.txt:218-219; seen at switch-to-roll and after touch).
    //The 0x27 write is the least-understood byte here -- first thing to drop on bench trouble.
    fpga_write_cmd(0x0D);
    fpga_write_int(0x00000027);

    fpga_arm_long_timebase_cycle();
  }
#endif

(3) test.c scope_get_long_timebase_data — one call per acquisition cycle, inserted right before line 949 ('//Check channel 1 is enabled', after the count>2999 redraw block ending at 947), so the 0x0D+0x28 pair brackets each 0x24/0x26 read group exactly like the capture (ONE pair covers both channels — do not call per channel):

    //Stock roll protocol: re-arm rate + mode select before every 0x24/0x26 read cycle
    //(no-op on 1013D / PECO FPGAs)
    fpga_arm_long_timebase_cycle();

(4) test.c scope_check_long_trigger — same single call at the top of the function (before line 1069's channel check): in normal/single mode this function performs the 0x24/0x26 reads INSTEAD of the main loop while waiting for the software trigger, and it is the first reader to run after mode entry, so without the arm its reads would still execute under the fast mode-select. Its two averaged reads are 5 ms apart; one arm covering both mirrors the capture's one-pair-per-read-group granularity (a per-read arm is the fallback if bench shows staleness).

Deliberately NOT changed: sm_1014d.c:1493-1496 keeps calling fpga_set_sample_rate (0x0D=0x00061A7F) at entry — scopesettings.samplerate must keep being set for the renderer/time math, and the per-cycle re-arm overwrites the FPGA-side rate to 0x7D0 before the first read anyway (self-healing ordering: entry-reset -> sample_rate write -> first cycle re-arm). The dead scope_get_long_timebase_data1 gets no call (leave dead / delete per the dead-code finding). 1013D and fw_FPGA>1 paths are untouched by construction of the guard.

== Open hardware questions (flag for the bench pass) ==
1. Does 0x28/0x01 change what 0x24/0x26 return on the stock 1014D FPGA (fresh rolling samples vs whatever the fast-mode select returns today)? A/B: current build vs patched, driven hum source, 500 ms/div, watch for trace actually tracking the input.
2. Rate semantics: stock 0x7D0 = ~100 kSa/s means the 10-byte average spans ~100 us; Atlan4's 399999 (~500 Sa/s) spans ~20 ms — more smoothing. If the stock rate makes roll visibly noisier, keeping Atlan4's value in fpga_arm_long_timebase_cycle (sample_rate_settings[scopesettings.samplerate] instead of 0x7D0) is a legitimate documented divergence — but start byte-exact to the reference per repo doctrine.
3. The 0x0D <- 0x00000027 entry write's meaning is unknown (possibly a park/stop divider); it is in the reference at entry/after-touch, so it is included, but drop it first if roll misbehaves after the patch.
4. 0x0F/0x01 at roll entry is Atlan4's, absent from the stock roll capture; kept because current roll demonstrably acquires data with it. If the patched sequence glitches, try removing it second.
5. Capture provenance: the pecostm32-RE roll captures were taken on a stock 1013D (GT911 0x41 read present at capture top); the 1014D's stock FPGA reports the same 0x1432 and PORT_AUDIT already validated the short-mode half of the same document on this unit's hardware (F25), so cross-applicability is likely but is itself a bench question.
6. This is the ACQUISITION half only — the F21 renderer findings (dual renderer + P14 geometry, reported above) must be fixed separately or roll will still look broken even with correct data.
```

**Coverage notes:** TASK 1: Read all 1412 lines of /home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/test.c and diffed it against the vendored Atlan4-1.00o5 copy (byte-identical — every internal defect is inherited; the 1014D-specific breakage is at the port seams). Traced the full roll path end-to-end: main-loop routing (fnirsi_1013d_scope.c:391-445), sm_set_time_base long branch + rate push (sm_1014d.c:1430-1517), fpga_set_long_timebase/fpga_set_sample_rate/fpga_average_trace_data(+_long) (fpga_control.c:380-395, 576-601, 1433-1525), scope_preset_values long branch (scope_functions.c:257-287), scope_display_trace_data geometry+blit (3450-3800), scope_get_y_sample (3865-3913), sm writers of triggerverticalposition/traceposition (sm_1014d.c:1340-1392, 1601-1645), P14 trace-window constants (variables.h:143-177), rate/cal tables (variables.c:421-435, 794-825, 991-994; scope_functions.c:2326-2345). uint16-wrap sweep over roll state: count/xpos/lastx are uint32 (variables.h:768-770, safe); averager accumulators bounded (10*255 and 100*255 < 65535); the one live wrap is triggerverticalposition (reported; REVIEW-2026-08-21's annihilation argument re-checked and confirmed inapplicable to the roll compare). Checked the timeout table (cases 7/8 now unreachable after the F21 boundary — dead cases only, not reported), tracebuffer bounds (max index 2999 vs 16384-byte buffers — safe), timer tick = 1 ms (timer.c:15), touch-stub behavior on 1014D (touchpanel.c:185-244), dev_mode reachability, and link flags (no --gc-sections). Cross-checked against REVIEW-2026-08-21.md and today's f0561ee/aea6f8a to avoid re-reporting fixed items (the FPGA_NOTES fpga_set_time_base misattribution is already corrected; the tvp unclamped writer was explicitly left open for this pass). TASK 2: personally read the >=100ms stock captures — pecostm32-RE/'FPGA explained'/100mS_50S-div_signal_data_read_sequence.txt (all 84 lines), FPGA explained.txt (full), 50mS-div capture header (short-mode 0x0F/0x28 cycle for contrast), Startup sequence 0x0D/0x0F/0x28 hits, parameter_analysis.txt, readme.txt — and verified fpga_write_int is MSB-first so 0x000007D0 matches the capture byte order, and averagecommand=0x24/0x26 (scope_functions.c:7389/7419). Not done: no build (make forbidden), no hardware verification (all roll behavior claims are code+capture-derived; F21 symptoms themselves are bench-recorded in PORT_AUDIT), no findings reported on vendored trees (used strictly as references), 1013D behavior left untouched per policy. The proposal below is a design only — nothing was applied.

## USB mass-storage / IRQ concurrency

### MSC WRITE_10 larger than the staging buffer silently drops one 512-byte packet per buffer flush

- **Where:** `fnirsi_1013d_scope/mass_storage_class.c:502`
- **Severity:** medium — **CONFIRMED**

In MSC_RECEIVE_DATA, the buffer-full branch (else at lines 502-543) flushes the accumulated data to the card with sd_card_write and repositions pointers, but never calls usb_read_from_fifo for the packet that triggered the flush. The IRQ dispatcher then unconditionally clears RX_PKT_READY (usb_interface.c:810 '*USBC_REG_RXCSR &= ~(USBC_BP_RXCSR_D_RX_PKT_READY | USBC_BP_RXCSR_D_OVERRUN);'), which discards that packet's 512 bytes from the double-buffered FIFO. scsi_byte_count is also never decremented for the dropped packet, so the transfer-complete test 'if(length >= scsi_byte_count)' (line 466) can never fire: no CSW is sent and the host times out and resets the device. The staging buffer is sizeof(viewthumbnaildata) = 1000 x 409 = 409,000 bytes (struct tagThumbnailData, variables.h:858-875: 33+12+182+182 all-byte fields; VIEW_ITEM_TRACE_POINTS=182 variables.h:46), so 798 x 512-byte packets (408,576 B) are accepted and packet 799 of a single CBW is dropped. Latent in practice because common host stacks cap one WRITE_10 at 64-128 KB, which is why bench USB writes work. Inherited verbatim: file is byte-identical to Atlan4-1.00o5 (empty diff) and this region is identical to pecostm32's official 1014D mass_storage_class.c (diff shows only descriptor/mounted_to_PC/READ_FORMAT_CAPACITY differences).

**Evidence:** mass_storage_class.c:455-461 accept path reads FIFO only when 'tptr < scsi_data_end_ptr'; lines 502-543 (else branch) contain sd_card_write/memcpy but no usb_read_from_fifo call; usb_interface.c:807-810 calls usb_mass_storage_out_ep_callback then clears RX_PKT_READY regardless; mass_storage_class.c:421 'scsi_data_end_ptr = scsi_data_in_ptr + sizeof(viewthumbnaildata);'

**Suggested action:** In the buffer-full branch, read the triggering packet from the FIFO after flushing (into the buffer start, after preserving the partial-sector remainder) and account for it in scsi_bytes_received/scsi_byte_count. Since the same code is in pecostm32's official 1014D, treat as an upstream-inherited latent bug; at minimum document it in PORT_AUDIT/ROADMAP so a large-transfer host isn't debugged as an SD failure.

**Verifier:** Reproduced structurally: MSC_RECEIVE_DATA accept path reads the FIFO only when tptr < scsi_data_end_ptr (mass_storage_class.c:455-461) and decrements scsi_byte_count only there (496); the buffer-full else (502-543) flushes via sd_card_write and repositions but contains no usb_read_from_fifo, and the dispatcher clears RX_PKT_READY unconditionally after the callback (usb_interface.c:803-810 '&= ~(RX_PKT_READY|OVERRUN)'), discarding the packet. Buffer math verified: tagThumbnailData = 33+12+2*182 = 409 all-byte fields (variables.h:857-875, no padding), VIEW_MAX_ITEMS=1000 -> 409,000 B; 798 packets (408,576 B) fit, packet 799 hits the else. scsi_check_read10_write10 (161-210) imposes no size cap, and WRITE_10's 16-bit block count allows up to 32 MB per CBW, so the path is host-reachable. File byte-identical to Atlan4 (empty diff); diff vs pecostm32 official 1014D shows hunks only at lines 36-155/281-333 (descriptors, mounted_to_PC, READ_FORMAT_CAPACITY) — the 452-543 region is inherited verbatim.

### 1014D USB screen exits on any key or rotary event, ignoring the host's PREVENT_MEDIUM_REMOVAL (mounted_to_PC never checked)

- **Where:** `fnirsi_1013d_scope/menu_1014d.c:204`
- **Severity:** medium — **CONFIRMED**

ui_setup_usb_screen() is: draw, usb_device_enable() (line 201), uart1_wait_for_user_input() (line 204), usb_device_disable() (line 207). uart1_wait_for_user_input (uart.c:78-82) returns on ANY nonzero key/rotary code, so a knob bump or key press while the host is mid-write soft-disconnects USB instantly, corrupting whatever the host had buffered (FAT metadata included). The MSC code maintains mounted_to_PC exactly for this (mass_storage_class.c:287-288, comment 'Use this command to block the return to scope mode if the host says so!!!'), and Atlan4's 1013D touch UI honors it (menu.c:329 'if (mounted_to_PC)' shows an error and refuses to exit), but the 1014D path never reads it — grep shows the only consumer is menu.c:329, unreachable on 1014D (touch_handler is only called in the #else branch, fnirsi_1013d_scope.c:445-448). pecostm32's official 1014D has the identical ungated wait (FNIRSI_1014D_Firmware/fnirsi_1014d_scope/user_interface_functions.c:197-203), so this is reference parity, not a port regression — but this tree already carries the flag and the 1013D precedent for the fix.

**Evidence:** menu_1014d.c:201-213: usb_device_enable(); uart1_wait_for_user_input(); usb_device_disable(); ... ui_sync_thumbnail_files(); — no mounted_to_PC test. uart.c:80-82: 'while((lastreceivedcommand = uart1_receive_data()) == 0);'. mass_storage_class.c:288: 'if(scsi_cbw.command[4] & 0x01) mounted_to_PC=1; else mounted_to_PC=0;'. menu.c:329 is the only reader.

**Suggested action:** Mirror the 1013D gate: after uart1_wait_for_user_input returns, if mounted_to_PC is set, show an eject-first message and loop back to waiting instead of disabling USB. Small, reference-backed (Atlan4 menu.c:329 pattern), 1014D-only change.

**Verifier:** menu_1014d.c:200-213 verified: usb_device_enable(); uart1_wait_for_user_input(); usb_device_disable(); ... ui_sync_thumbnail_files() — no mounted_to_PC test. uart.c:78-82 returns on any nonzero key/rotary byte. Grep confirms the only reader of mounted_to_PC is menu.c:329 (touch USB screen, shows error and refuses exit when set), and touch_handler is only called in the #else (1013D) branch of the main loop (fnirsi_1013d_scope.c:446-449), so it is unreachable on the 1014D. The setter exists at mass_storage_class.c:288 with the quoted comment at 287. pecostm32's official user_interface_functions.c:196-209 has the identical ungated enable/wait/disable — reference parity, not a regression, exactly as stated.

### READ_10 SD-error path sends the error CSW and then keeps sending data, desynchronizing the bulk-only transport

- **Where:** `fnirsi_1013d_scope/mass_storage_class.c:368`
- **Severity:** medium — **CONFIRMED**

In SCSI_CMD_READ_10, when sd_card_read fails (line 368) the code writes the FAIL CSW to EP1 (line 377) but does NOT break out: it falls through to 'usb_write_ep1_data((void *)scsi_data_in_ptr, cardsectorsize)' (line 381) and sets msc_state = MSC_SEND_DATA (line 396), after which the IN-endpoint callback streams stale buffer contents and eventually a second CSW. The host, expecting data first, interprets the 13-byte CSW as data; the transport is desynced until a BOT reset. This is deterministically host-triggerable: a buggy/malicious host issuing READ_10 with LBA past capacity makes sd_card_read return SD_ERROR_SECTOR_OUT_OF_RANGE (sd_card_interface.c:322-323), hitting exactly this path. Memory-safe (buffer pointers stay inside viewthumbnaildata) but a robustness defect. Identical in Atlan4-1.00o5 and pecostm32's official 1014D (inherited). The same missing-abort pattern exists in the IN-callback refill (lines 589-596: on error it sets the CSW status but still advances and keeps sending).

**Evidence:** mass_storage_class.c:368-378: 'if(sd_card_read(...) != SD_OK) { scsi_csw.status = MSC_CSW_STATUS_FAIL; ... usb_write_ep1_data((void *)&scsi_csw, MSC_CSW_LENGTH); }' with no break/return; line 381 then sends a data block; line 396 'msc_state = MSC_SEND_DATA;'

**Suggested action:** After sending the error CSW, set msc_state = MSC_WAIT_COMMAND and break (and stall the IN endpoint per BOT spec if being thorough). Same for the refill error at line 589. Low-risk, protocol-level fix; note as inherited-from-upstream in PORT_AUDIT.

**Verifier:** mass_storage_class.c:368-397 verified: on sd_card_read failure the FAIL CSW is written at 377 with no break/return; execution falls to usb_write_ep1_data(scsi_data_in_ptr, cardsectorsize) at 381 and msc_state = MSC_SEND_DATA at 396, so the host receives CSW-then-data — transport desync. Host-triggerable: scsi_check_read10_write10 (161-210) checks only direction/byte-count consistency, never LBA range, so an out-of-range LBA reaches sd_card_read which returns SD_ERROR_SECTOR_OUT_OF_RANGE (sd_card_interface.c:322-323). Same pattern in the IN-callback refill at 589-602 (sets CSW status, still advances counters and keeps streaming). Region identical to Atlan4 (empty file diff) and to pecostm32 official (no diff hunks beyond line 333).

### SD sector-range guard off-by-one (allows one sector past the end) plus uint32 sum wrap; flagged by its own comment

- **Where:** `fnirsi_1013d_scope/sd_card_interface.c:322`
- **Severity:** low — **CONFIRMED**

sd_card_read (line 322) and sd_card_write (line 393) both use 'if((sector + blocks - 1) > cardsectors) return(SD_ERROR_SECTOR_OUT_OF_RANGE);'. Valid LBAs are 0..cardsectors-1 (READ_CAPACITY_10 itself reports cardsectors-1 as the last LBA, mass_storage_class.c:300-306), so the guard admits sector+blocks-1 == cardsectors, one past the end; the card then rejects with an address error, feeding the READ_10 error-path desync above. Additionally 'sector + blocks - 1' can wrap for sector near 2^32 and pass the guard, but I traced the downstream consequences: SDHC gets cmdarg = huge sector (card rejects); SDSC byte-address wrap cannot reach a valid low sector because the guard's cardsectors ceiling (<= ~4M for SDSC) already excludes every sector large enough to wrap. So no wrong-sector access is host-reachable — only spurious accept-then-card-error. The code's own comment at line 320 says 'This might be wrong. Need testing with last sector!!!!!'. Inherited from upstream.

**Evidence:** sd_card_interface.c:320-323: '//This might be wrong. Need testing with last sector!!!!!\n  //Check if last bytes to read in range of the card sectors\n  if((sector + blocks - 1) > cardsectors)'; same at 393; mass_storage_class.c:300 'uint32 sectors = cardsectors - 1;' for READ_CAPACITY.

**Suggested action:** Change the guard to reject when blocks == 0 or sector >= cardsectors or blocks > cardsectors - sector (wrap-free form), and drop the stale comment. Applies to both variants (shared core).

**Verifier:** sd_card_interface.c:319-323 and 392-394 verified: '(sector + blocks - 1) > cardsectors' admits sector+blocks-1 == cardsectors, one past the last valid LBA, while READ_CAPACITY_10 reports cardsectors-1 as last LBA (mass_storage_class.c:300-306); the code's own comment 'This might be wrong. Need testing with last sector!!!!!' is at 319. Re-derived the wrap analysis: guard passes for sector near 2^32, SDHC passes the huge sector as cmdarg (card rejects); for SDSC the wrap-passing sectors give byte addresses (sector mod 2^23)*512 >= 2^32 - 65535*512 =~ 0xFE000200, far beyond any <=2 GB SDSC capacity, so no wrong-sector access is reachable — only accept-then-card-error, matching the finding.

### READ_FORMAT_CAPACITY answers with raw, possibly-zero READ_CAPACITY bytes in the wrong response format

- **Where:** `fnirsi_1013d_scope/mass_storage_class.c:327`
- **Severity:** low — **CONFIRMED**

SCSI_CMD_READ_FORMAT_CAPACITY (lines 327-334) replies with the 8-byte scsi_capacity buffer, which is only populated when the host has previously issued READ_CAPACITY_10 (lines 303-311); a host that probes READ FORMAT CAPACITIES first (Windows commonly does) receives 8 bytes of zeros from .bss. The SCSI response format is also wrong (spec wants a 4-byte capacity-list header plus 8-byte descriptor(s), 12 bytes minimum). Hosts tolerate a malformed/failed response here, so impact is cosmetic. This branch is an Atlan4 addition — pecostm32's official 1014D instead groups READ_FORMAT_CAPACITY with MODE_SENSE (diff vs FNIRSI_1014D_Firmware/fnirsi_1014d_scope/mass_storage_class.c shows the case moved and reshaped).

**Evidence:** mass_storage_class.c:327-334: 'case SCSI_CMD_READ_FORMAT_CAPACITY: { usb_write_ep1_data((void *)scsi_capacity, sizeof(scsi_capacity)); ... msc_state = MSC_SEND_STATUS; }'; scsi_capacity is a zero-initialized global (line 150) written only in READ_CAPACITY_10.

**Suggested action:** Either populate scsi_capacity from cardsectors/cardsectorsize inside this case too (cheap), or reply with a proper 12-byte format-capacities structure; alternatively fail the command like the default case. Polish only.

**Verifier:** Core defect verified: this tree's SCSI_CMD_READ_FORMAT_CAPACITY (mass_storage_class.c:327-334) sends the raw 8-byte scsi_capacity buffer, a zero-initialized global (line 150) populated only by READ_CAPACITY_10 (303-311), so a host probing READ FORMAT CAPACITIES first gets 8 zero bytes; the SCSI response format (capacity-list header + descriptor, >=12 B) is indeed not followed. This branch shape is Atlan4's (file byte-identical to Atlan4; diff hunk 325,333d318 vs pecostm32).

**Correction:** One comparison detail is wrong: pecostm32's official 1014D groups SCSI_CMD_READ_FORMAT_CAPACITY with SCSI_CMD_READ_CAPACITY_10 (reference mass_storage_class.c:291-292, so its buffer is freshly populated before sending), not with MODE_SENSE; MODE_SENSE_6 is a separate case at its line 314.

### IRQ-context raw SD I/O is safe only by an unwritten invariant (main loop parked while USB enabled); ~400 KB bursts also stall the 1 ms tick

- **Where:** `fnirsi_1013d_scope/mass_storage_class.c:481`
- **Severity:** low — **CONFIRMED**

All MSC SD traffic runs inside usb_device_irq_handler (usb_interface.c:807/834 -> usb_mass_storage_out_ep_callback / usb_mass_storage_in_ep_callback), performing sd_card_read/sd_card_write of up to SCSI_MAX_BLOCK_COUNT = 400000/512 = 781 blocks (399,872 B) per call (lines 368, 481, 512, 589; mass_storage_class.h:55). This never collides with FatFs or the sd_command/sd_data globals today ONLY because every usb_device_enable() site immediately parks the main context: menu_1014d.c:201-204 (busy-poll in uart1_wait_for_user_input, no SD side effects — uart.c:44-58 is pure UART register I/O) and the 1013D menu.c:315/344 touch loop (tp_i2c only); the FatFs resync (ui_sync_thumbnail_files, menu_1014d.c:213) runs only after usb_device_disable() at line 207. Nothing in the code states or enforces this invariant — a future caller enabling USB outside a modal screen (background MSC has been floated on the ROADMAP) would race main-loop FatFs on the shared SD driver state and clobber viewthumbnaildata (the SCSI staging buffer, lines 352/420/481). Secondary: interrupt.c's flat non-nesting dispatcher (irq_handler, interrupt.c:31-67) means a multi-hundred-block SD burst in the USB IRQ blocks timer0's 1 ms tick; multiple elapsed ticks collapse into one pend, so timer0ticks/timer0_delay undercount during transfers (harmless while parked, but real).

**Evidence:** usb_interface.c:153 'setup_interrupt(USB_IRQ_NUM, usb_device_irq_handler, 2);' (registered from main() via usb_device_init, fnirsi_1013d_scope.c:145, USB_CH340 defaults 0 at variables.c:160); usb_interface.c:807/834 callback dispatch; mass_storage_class.c:368/481/512/589 sd_card_* calls; interrupt.c irq_handler loop calls handlers to completion with no nesting.

**Suggested action:** Add a comment block at the top of mass_storage_class.c and a paragraph in PORT_AUDIT or FPGA_NOTES-adjacent docs stating the invariant: 'USB may only be enabled while the main loop is parked in a modal wait; all SD/FatFs access is otherwise unserialized against this IRQ path.' No code change needed now.

**Verifier:** All factual anchors verified: MSC SD traffic runs in usb_device_irq_handler via the EP callbacks (usb_interface.c:803-834); SCSI_MAX_BLOCK_COUNT = VIEW_THUMBNAIL_DATA_SIZE/512 = 400000/512 = 781 blocks = 399,872 B (mass_storage_class.h:55, variables.h:53) at call sites 368/481/512/589; registration setup_interrupt(USB_IRQ_NUM, usb_device_irq_handler, 2) at usb_interface.c:153/204. Parking invariant verified at both live enable sites: menu_1014d.c:201-207 busy-polls uart1_wait_for_user_input (uart.c:44-58 is pure UART1 register I/O) and menu.c:315/344 (1013D touch loop); ui_sync_thumbnail_files runs only after usb_device_disable() (menu_1014d.c:207/213). Nothing documents or enforces the invariant. interrupt.c's dispatcher (31-67) calls handlers to completion, no nesting, so long SD bursts in the USB IRQ do stall timer0 ticks (multiple elapsed periods collapse to one pend).

### 1013D-only: power-loss ISR calls scope_save_configuration_data() from IRQ context, able to preempt an in-flight main-loop SD operation

- **Where:** `fnirsi_1013d_scope/power_and_battery.c:113`
- **Severity:** low — **CONFIRMED**

On the 1013D variant (#if !PORT_1014D, guard verified inside both power_interrupt_init and power_interrupt_handler), the Port-E power interrupt (priority 3, registered at line 94) calls scope_save_configuration_data() at line 113 and then hangs in while(1). Because IRQs preempt main context, a power-off arriving while the main loop is mid FatFs/SD operation (screenshot save, view-file write) re-enters the SD driver's shared sd_command/sd_data state mid-command — the config save can fail or write garbage during the dying-gasp window. It cannot preempt the USB MSC IRQ itself (flat non-nesting dispatcher), so the USB-mounted case only interleaves between complete USB interrupts. Compiled out on the 1014D (no PORTE power IRQ registered), so this tree's active variant is unaffected. Inherited from Atlan4. Per repo policy (no 1013D behavior changes without hardware), this is a documentation finding only.

**Evidence:** power_and_battery.c:94 'setup_interrupt(PORTE_EINT_IRQ, power_interrupt_handler, 3);' inside '#if !PORT_1014D'; lines 100-118: handler body guarded '#if !PORT_1014D', calls scope_save_configuration_data() at 113 when viewactive == VIEW_NOT_ACTIVE, then 'while(1);'

**Suggested action:** Do not change code (1013D untestable here). Add a note in PORT_AUDIT.md's 1013D-variant section: power-loss config save runs in IRQ context and is unsafe if power fails during a main-loop SD write.

**Verifier:** power_and_battery.c verified: setup_interrupt(PORTE_EINT_IRQ, power_interrupt_handler, 3) at line 94 inside '#if !PORT_1014D' (guard confirmed in both power_interrupt_init and the handler); handler calls scope_save_configuration_data() when viewactive == VIEW_NOT_ACTIVE then while(1). scope_save_configuration_data -> sd_card_write(SETTINGS_SECTOR,...) (scope_functions.c:7494-7505), so an IRQ arriving mid main-loop FatFs/SD operation re-enters the shared SD driver state. Compiled out on 1014D; doc-only per policy — as the finding itself frames it.

**Coverage notes:** Read in full: mass_storage_class.c (619 lines), usb_interface.c lines 1-1436 (MSC IRQ handler complete; CDC handler skimmed — its 'f' debug-command bug is already REVIEW-2026-08-21 finding at usb_interface.c:1542, not duplicated), interrupt.c (complete), timer.c ISR, uart.c poll/wait functions, sd_card_read/sd_card_write in sd_card_interface.c, power_and_battery.c ISR, ui_setup_usb_screen (menu_1014d.c:164-214), sm_start_usb_export (sm_1014d.c:3025-3041), scope_setup_usb_screen (menu.c:267-360), main-loop dispatch guard (fnirsi_1013d_scope.c:430-455). Diffed mass_storage_class.c against Atlan4-1.00o5 (byte-identical) and pecostm32's official 1014D (only descriptors, mounted_to_PC, READ_FORMAT_CAPACITY differ; all READ_10/WRITE_10/state-machine logic identical), and checked the official 1014D's ui_setup_usb_screen (same ungated wait). TASK ANSWERS: (1) USB service IS fully interrupt-driven — setup_interrupt(USB_IRQ_NUM, usb_device_irq_handler, 2) at usb_interface.c:153 (MSC is the boot default: USB_CH340=0, variables.c:160; CDC registration at :138 only if USB_CH340==1), and mass_storage_class.c performs raw sd_card_read (368, 589) and sd_card_write (481, 512) from IRQ context, up to 781 blocks (~400 KB) per interrupt. (2) YES, the 1014D USB view is a full serializer: sm_start_usb_export runs in main-loop context (dispatched from sm_handle_user_input at fnirsi_1013d_scope.c:441) and blocks inside ui_setup_usb_screen -> uart1_wait_for_user_input (pure UART 0xFF-poll busy-wait, no SD side effects) until a key arrives; usb_device_disable() precedes the FatFs resync (ui_sync_thumbnail_files), so no FatFs op can interleave with host sector writes. Other SD touchers while mounted: none on 1014D — timer0 ISR only increments ticks, only three IRQs exist (timer0, USB, PORTE-power), and the PORTE power ISR is compiled out on 1014D (1013D-only hazard reported as finding 7). Screenshot/config/acqprobe paths are unreachable while the wait loop holds. (3) SCSI bounds: no check in mass_storage_class.c itself; sd_card_read/write guard against cardsectors with an off-by-one and a wrap-tolerant sum (finding 4), but I traced that no host-supplied LBA can reach a wrong real sector or overflow memory — worst case is a card-level error return, which then triggers the READ_10 error-path protocol desync (finding 3). Read bursts are capped at SCSI_MAX_BLOCK_COUNT=781 (399,872 B); writes are capped by scsi_data_end_ptr = buffer + sizeof(viewthumbnaildata) with a pre-read check. (4) Aliased buffer sized: viewthumbnaildata = THUMBNAILDATA[1000], sizeof = 1000 x 409 B = 409,000 B (struct is all byte fields: 33+12+182+182, VIEW_ITEM_TRACE_POINTS=182); read bursts (399,872 B) and the write bound (409,000 B) both fit; no overlap with live UI while the USB screen draws, because all drawing completes before usb_device_enable() and the thumbnail catalog is rebuilt from FatFs after disable — safety is implicit, hence finding 6. (5) BOOT_NOTES.md:82 claim CONFIRMED from the LBA math: READ_10/WRITE_10 pass the host LBA unmodified to sd_card_read/write (mass_storage_class.c:339/368, 403/481) and READ_CAPACITY_10 reports cardsectors-1 over the whole raw card (line 300), so MSC exposes the raw device from LBA 0 — a PC can read/rewrite the MBR, the boot image at 8 KB (sector 16), and config sectors 708-710; the documented 'dd of=<usb-disk> bs=1024 seek=8' reflash route is real. Variant guards on this path verified: mass_storage_class.c/usb_interface.c are unguarded shared core (correct, both variants use MSC); menu_1014d.c/sm_1014d.c carry whole-file #if PORT_1014D guards after includes (lines 15 / 5537 and 15 / 3044); the 1013D touch USB screen (menu.c:267, with its mounted_to_PC gate and CDC re-enable on exit) is reachable only via statemachine.c:1179 whose entry point touch_handler() is called solely in the #else branch at fnirsi_1013d_scope.c:448 — unreachable dead code on 1014D (the compiled-but-unreachable variant-table issue is already a REVIEW-2026-08-21 finding, not duplicated). Not audited: sd_card_send_command internals/DMA, the CDC/PC_interface protocol beyond confirming zero SD/FatFs access in that path (grep for f_open/f_read/f_write/sd_card_* over cdc_class.c and PC_interface.c is empty), USB PHY/enumeration correctness, and usb_interface.c lines 1437-1757 (CDC EP2 TX tail) beyond the dispatch structure.

## CPU infrastructure layer (start.s / arm32.h / .ld / interrupt.c)

### D-cache enable is a silent no-op: MMU is never enabled, so ALL data accesses run uncached and unbuffered

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/fnirsi_1013d_scope.c:57`
- **Severity:** medium — **CONFIRMED**

main() calls arm32_icache_enable(); arm32_dcache_enable() under the comment 'Instead of full memory management just the caches enabled' (lines 55-57). arm32_dcache_enable() only sets the CP15 c1 C bit (arm32.h:71-75). Grep over fnirsi_1013d_scope/ confirms arm32_mmu_enable/arm32_ttb_set/arm32_domain_set/arm32_tlb_invalidate are defined in arm32.h (lines 59, 108, 117, 126) but never called anywhere; start.s never touches the M bit either. On ARM926EJ-S, cacheability/bufferability of DATA accesses comes solely from the page-table C/B bits, so with the MMU off every data access is treated as noncacheable AND nonbufferable — the C bit is latently set but ineffective. The I-cache, by contrast, does work with the MMU off on ARM926 (flat mapping, all instruction fetches cacheable), so code runs cached. Net effect: every load/store — trace buffers, the six ~750 KB display buffers, FatFs sector copies, USB FIFO PIO — is a single-beat DDR transaction, and every store stalls (no write buffer). Rough cost (estimate, not measured; CPU=600 MHz, AHB=300 MHz per ccu_control.c:14-18): memcpy.s uses 8-word LDM/STM so bulk copies still get AHB bursts (maybe 2-4x slower than cached), but per-pixel uint16 rendering in display_lib and all pointer-chasing pay full single-access latency, plausibly 5-15x slower than with a working D-cache. The per-frame trace-window copy alone ((2,48,705,432) = ~609 KB read + ~609 KB written) is >1.2 MB of uncached traffic per displayed frame — this is very likely the firmware's frame-rate ceiling and the single biggest untapped performance lever in the whole tree. Inherited byte-identical from Atlan4 (and same in pecostm32 upstream).

**Evidence:** fnirsi_1013d_scope.c:55-57 '//Instead of full memory management just the caches enabled / arm32_icache_enable(); / arm32_dcache_enable();'. Grep for arm32_mmu_enable|arm32_ttb_set|arm32_domain_set|arm32_tlb_invalidate over fnirsi_1013d_scope/*.c/h: only the arm32.h definitions match — zero call sites. start.s touches CP15 c1 only to clear the V bit (lines 52-54). diff vs Atlan4-1.00o5: start.s, arm32.h, interrupt.c/h, fnirsi_1013d.ld all IDENTICAL.

**Suggested action:** Three homes: (1) ROADMAP.md perf item — 'enable MMU + real D-cache' with the quantification above and the uncacheable-region map from the companion finding; flag it as the largest single perf win available. (2) AGENTS.md gotcha — 'the D-cache is NOT actually on; do not reason about cache coherency, and do not expect dcache_enable to speed anything up'. (3) Fix the misleading comment at fnirsi_1013d_scope.c:55 to say the D-cache enable is ineffective without the MMU (code change is one comment line, zero behavior risk).

**Verifier:** Verified: fnirsi_1013d_scope.c:55-57 comment + arm32_icache_enable()/arm32_dcache_enable(); arm32_dcache_enable sets only CP15 c1 C-bit (arm32.h:71-75); grep over all first-party .c/.h/.s finds zero call sites for arm32_mmu_enable/arm32_ttb_set/arm32_domain_set/arm32_tlb_invalidate (definitions only, arm32.h:59/108/117/126); start.s touches CP15 c1 only to clear the V bit (lines 52-54) and copy vectors — the M bit is never set. The ARM926EJ-S architectural claim is correct (TRM: with MMU disabled all data accesses are noncachable/nonbufferable; instruction fetches remain cachable with I-cache on), so the D-cache enable is an ineffective latent bit. start.s/arm32.h/interrupt.c/h/fnirsi_1013d.ld all diff-identical to Atlan4. Performance figures are explicitly labeled estimates; the structural claim stands.

### Hazard map for any future MMU enablement: regions that must stay uncacheable (FPGA PE bus, all 0x01Cxxxxx/0x01Exxxxx peripherals, DEBE-scanned framebuffer)

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/display_control.h:31`
- **Severity:** medium — **CONFIRMED**

If someone later enables the MMU with a naive flat cacheable+bufferable 1 MB-section map to get the D-cache win, these break: (1) the entire sunxi peripheral space — Port E PIO at 0x01C20800 (fpga_control.c bit-bangs the FPGA parallel bus through PORT_E_* registers; cached/buffered access would batch, reorder, or satisfy reads from cache, destroying the strobe protocol), INTC 0x01C20400 (interrupt.h:13-36), CCU, SD0, USB MUSB FIFO/regs (usb_interface.h:80-81, FIFO word reads at usb_interface.c:271/304), TCON 0x01C0C000, DEBE 0x01E60800 — all must be mapped device/NCNB. (2) maindisplaybuffer (BSS, at 0x8026BC1C in the current ELF, handed to sys_init_display at fnirsi_1013d_scope.c:97): DEBE scans it from DRAM continuously; with write-back caching the rendered pixels sit in dirty lines and the panel shows stale content — must be uncacheable or write-through (or every render must clean lines). Importantly, DEBE scanout is the ONLY DRAM-side DMA agent in this firmware: I verified SD is PIO (sd_card_interface.c:969-979 'Send or receive the data using the cpu' — the DMA of the original code was removed; SD0_GCTL DMA bits only appear in reset masks) and USB is PIO FIFO (usb_read_from_fifo/usb_write_to_fifo, alignment-checked CPU copies), so no other buffer needs coherency management. (3) The vector page copied to 0x00000000 (SRAM, start.s:56-65) must stay mapped+executable. (4) Both BROM jumps — sm_enter_fel_mode's 0xFFFF0020 and the loader path — would need clean+disable first. None of this is a bug today (D-cache is effectively off), but the FPGA PE-bus item is exactly the kind of silent corruption that would look like an FPGA protocol heisenbug.

**Evidence:** fpga_control accesses PORT E data/config registers in 0x01C208xx space; interrupt.h:13-36 INTC at 0x01C204xx; display_control.h:8-37 TCON 0x01C0Cxxx and DEBE 0x01E608xx; sys_init_display(SCREEN_WIDTH, SCREEN_HEIGHT, (uint16 *)maindisplaybuffer) at fnirsi_1013d_scope.c:97; sd_card_interface.c:972-975 '//For larger blobs of data it uses DMA / //Send or receive the data using the cpu'; usb_interface.c:264-293 alignment-checked PIO FIFO copy.

**Suggested action:** Attach to the same ROADMAP.md MMU/D-cache item as its prerequisite checklist (flat map with: DRAM cacheable-WB, maindisplaybuffer's section write-through or uncached, 0x01C00000-0x01FFFFFF + 0x01E60000 device, SRAM page at 0 mapped, cache clean+disable before any BROM jump). Add one cross-reference line in FPGA_NOTES.md near the PE-bus protocol description: 'PE bus correctness currently depends on the D-cache being de-facto off; any MMU work must map 0x01C2xxxx device/uncacheable'.

**Verifier:** All cited facts verified: FPGA parallel bus registers at 0x01C20890/94/A0 in Port E PIO space (fpga_control.h:14-16); INTC 0x01C204xx (interrupt.h); TCON 0x01C0C000 and DEBE 0x01E60800 (display_control.h:7-37); sys_init_display(SCREEN_WIDTH, SCREEN_HEIGHT, maindisplaybuffer) at fnirsi_1013d_scope.c:96-97 with DEBE scanning DRAM; SD is CPU PIO with the 'original code used DMA' comment (sd_card_interface.c:970-975); USB FIFO copies are alignment-checked CPU PIO (usb_interface.c:264-293); vector page copied to 0x00000000 (start.s:56-65); FEL BROM jump 0xFFFF0020 (sm_1014d.c:2946-2964). maindisplaybuffer = 0x8026bc1c in the current ELF (nm). Advisory hazard map, factually sound.

### sm_enter_fel_mode jumps to BROM with the VIVT I-cache still enabled and never invalidated — stale-code risk for the FEL reflash workflow

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/sm_1014d.c:2963`
- **Severity:** medium — **CONFIRMED**

The Factory-settings FEL path (sm_enter_fel_mode, sm_1014d.c:2944-2965) saves config, paints the FEL message, calls arm32_interrupt_disable(), and jumps straight to 0xFFFF0020. The ARM926 I-cache — enabled since main() and VIVT — is left on and full of lines from the RUNNING firmware image at 0x80000000+. The standard workflow then has sunxi-fel write a NEW build over those same addresses (data writes, which bypass the I-cache) and 'exe 0x80000000'. Unless BROM invalidates the I-cache on its own (not guaranteed by anything we control), stale lines from the old build can be executed after the jump — the classic 'flashed the new build but observed old behavior' failure, which this project is specifically sensitive to (intermittent-symptom discipline, F26/F27 history). I am NOT claiming this caused any past symptom — only that the hole exists and is closable for two instructions. Note arm32.h has enable/disable helpers only; there is no invalidate helper (ICIALLU equivalent: mcr p15, 0, Rd, c7, c5, 0). The bootloader's own F3 FEL path is a separate binary and out of scope.

**Evidence:** sm_1014d.c:2960-2964: 'arm32_interrupt_disable(); ... __asm__ __volatile__ ("mov pc, %0\n" ...)' with address = 0xFFFF0020; no cache operation between them. arm32.h:83-93 defines only arm32_icache_enable/disable; grep confirms no c7,c5,0 invalidate anywhere in first-party code.

**Suggested action:** Small code fix, low risk, hardware-verifiable via the existing FEL workflow: add an arm32_icache_invalidate() (and/or disable) helper to arm32.h and call it in sm_enter_fel_mode after arm32_interrupt_disable(), before the BROM jump. Also add a line to BOOT_NOTES.md's FEL section documenting the ordering requirement. Per the ask-vs-act calibration this is reference-backed and reversible, but since it touches the recovery path, flag it for the user rather than treating it as a silent cleanup.

**Verifier:** sm_1014d.c:2944-2964 verified: save config, paint message, arm32_interrupt_disable(), 'mov pc, %0' to 0xFFFF0020 — no cache maintenance anywhere between. I-cache is enabled from main() (fnirsi_1013d_scope.c:56) and stays on. arm32.h provides only arm32_icache_enable/disable (lines 83-93); grep finds no ICIALLU (c7,c5,0) invalidate anywhere in first-party code (arm32_tlb_invalidate is c7,c10,4 drain + c8,c7,0 TLB only). The stale-VIVT-line concern over sunxi-fel write+exe of a new build at the same addresses is technically valid and the finding is appropriately hedged about BROM behavior.

### IRQ infrastructure verified correct, but the discipline is implicit: SP reset on every IRQ entry permanently bans nesting, and USB mass-storage does multi-sector SD PIO with IRQs masked

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/start.s:118`
- **Severity:** medium — **CONFIRMED**

Dispatch correctness (verified): interrupt.c's setup_interrupt bit math is right for all three registered sources — timer0 IRQ13/prio0 (timer.c:21), USB IRQ26/prio2 (usb_interface.c:138/153/195/204), Port E EINT IRQ39/prio3 (power_and_battery.c:94): PRIO word index (irq&0x30)>>4 and enable word irq>>5 are word-scaled pointer arithmetic on uint32* (correct: 13->PRIO0 bit26, 26->PRIO1 bit20, 39->PRIO2 bit14; EN0 bit13/bit26, EN1 bit7). irq_handler walks both pending registers, restarting numbering at 32 correctly; handler table is 64 entries and irq maxes at 63. The start.s frame is the u-boot pt_regs layout (72 bytes: r0-r12, user sp/lr via STM^, lr_irq, SPSR, old_r0) with the required one-instruction gap (mov r0,r0 at line 130) after LDM^ before touching banked registers, and returns via subs pc, lr, #4 — all correct ARMv5 form. Three implicit contracts worth writing down: (a) start.s:118 'ldr sp, _stack_irq_end' RE-INITIALIZES the IRQ SP to 0x81FDA800 on EVERY entry — safe only because CPSR.I stays set for the whole handler (hardware sets it, nobody clears it); if any future handler re-enables IRQ, the nested entry overwrites the live frame at fixed 0x81FDA7B8 and the outer return goes to garbage. (b) Because of that all-masked discipline, USB mass storage runs its whole SCSI data path in IRQ context — mass_storage_class.c:368/481/512/589 call sd_card_read/sd_card_write (multi-sector, PIO) from the endpoint callbacks invoked by usb_device_irq_handler — so during USB file transfers, IRQs are masked for milliseconds at a time and timer0ticks stalls (timekeeping jitter while mounted; inherited, works). (c) interrupt.c:44 'INTC_PEND_REG0[i] = 0': writing 0 to a pending register is a no-op if the register is write-1-to-clear (the likely F1C100s semantics), and would DROP interrupts arriving between the read and the write if it were write-value; it is safe today only because all three sources latch at the peripheral until acked there and so re-assert. Also for completeness: FIQ is permanently dead (F bit set in every mode word start.s:15-49, never cleared — arm32_interrupt_enable clears only bit 7; fiq: handler is 'b .'), and undefined/prefetch/data-abort handlers are silent 'b .' hangs (start.s:102-115) — a crash shows as a hard freeze with no diagnostic.

**Evidence:** start.s:117-133 (irq entry, sp reload, 72-byte frame, subs pc,lr,#4); start.s:15-49 (mode words 0xdb/0xd7/0xd2/0xd1/0xdf/0xd3, all with I+F set); arm32.h:33-44 (enable clears only bit 7); interrupt.c:41-44 (pending read then '= 0'); mass_storage_class.c:368 'if(sd_card_read(scsi_start_lba, scsi_available_blocks, (uint8 *)scsi_data_in_ptr) != SD_OK)' reached from usb_device_irq_handler (usb_interface.c:368+) via the ep callbacks.

**Suggested action:** AGENTS.md gotcha section, three bullets: 'never re-enable IRQs inside an interrupt handler (start.s resets the IRQ SP on every entry — nesting corrupts the frame)'; 'USB mass-storage file I/O runs entirely in IRQ context with IRQs masked — do not add timing-sensitive code that assumes timer ticks advance during transfers'; 'the INTC pending clear-write is a no-op/W1C — real acking happens at each peripheral; any future edge-style source must be latched at the source'. No code change proposed.

**Verifier:** Independently re-derived all of it. Bit math: (irq&0x30)>>4 word-index and enable irq>>5 on uint32* are word-scaled (interrupt.c:20-25); irq 13 -> PRIO0 bits 26-27/EN0 bit 13; 26 -> PRIO1 bit 20/EN0 bit 26; 39 -> PRIO2 bit 14/EN1 bit 7 — all as claimed, and exactly three sources registered (timer.c:21, usb_interface.c:153/204, power_and_battery.c:94). start.s: mode words 0xdb/0xd7/0xd2/0xd1/0xdf/0xd3 all with I+F set (15-49); IRQ entry reloads sp from _stack_irq_end (=0x81FDA800) on every entry (117-118), 72-byte u-boot-style frame, mov r0,r0 gap after ldmia^ (129-130), subs pc,lr,#4 return — so nesting would overwrite the fixed frame at 0x81FDA7B8. interrupt.c:41-44 reads pending then writes 0 (the W1C concern is correctly conditional). MSC SD calls run from the IRQ dispatcher with I set throughout (usb_interface.c EP callbacks -> mass_storage_class.c:368/481/512/589). FIQ: F set in every mode word, arm32_interrupt_enable clears only bit 7 (arm32.h:33-44), fiq: is 'b .' (start.s:135-136); undef/prefetch/data-abort are silent 'b .' (102-112).

### Stack map documented: six fixed mode stacks in the top 300 KB of DRAM; main() runs on the 50 KB SVC stack with the unused FIQ region as a de-facto guard; no plausible overflow threat

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/start.s:139`
- **Severity:** low — **CONFIRMED**

start.s:139-150 hard-codes six full-descending stack tops; the sizes are just the gaps between consecutive literals (nothing enforces them): UND top 0x81FB5000 (region below is open DRAM); ABT 0x81FB5000-0x81FC1800 = 50 KB; IRQ 0x81FC1800-0x81FDA800 = 100 KB; FIQ 0x81FDA800-0x81FE7000 = 50 KB; SVC 0x81FE7000-0x81FF3800 = 50 KB; SYS 0x81FF3800-0x82000000 = 50 KB (0x82000000 is the exact end of the 32 MB DRAM; first push lands at 0x81FFFFFC, in bounds). The mode dance ends in SVC (0xd3, start.s:47-49), so main() and the whole main loop run on the 50 KB SVC stack; SYS mode is never entered (its 50 KB is dead), FIQ is never taken (F masked forever, handler 'b .'), ABT/UND stacks are set but their handlers hang without pushing. What sits below: NOT the framebuffers — all display/trace buffers are linker-placed BSS ending at BSS_END = 0x8058A0E1 (from the built ELF; displaybuffer2 at 0x8040E4E0, channel1tracebuffer at 0x805860E0, maindisplaybuffer at 0x8026BC1C). Below the UND floor lie ~3.66 MB of unallocated DRAM, then the 512-byte SD-sector-710 staging page at 0x81BFFC00-0x81BFFDFF (display config + legacy boot byte + touch config), then a ~23.5 MB gap down to BSS_END. Threat assessment: an SVC overflow first consumes the never-used FIQ 50 KB (silent), then the live IRQ stack at 50-150 KB depth. Worst-case main-loop depth is nowhere near that: FATFS/FIL/FILINFO are globals (variables.c:196/318/320), FatFs LFN buffer is static (FF_USE_LFN=1), and the largest stack locals found are uint16 buffer[256] = 512 B (scope_functions.c:7513/7661) — inspected paths stay in low single-digit KB. The IRQ side uses one 72-byte frame + handler depth (USB mass-storage chain is the deepest) against 100 KB. Conclusion: no guard exists and none of the linker, which knows nothing about these addresses (fnirsi_1013d.ld allocates from 0x7FFFFFE0 upward only, no overlap check), would catch a collision — but with ~5.8 MB used of 32 MB the margins are enormous. Related ld quirk worth one doc line: MEMORY org 0x7FFFFFE0 places the 32-byte eGON header below the documented DRAM base (works on hardware; entry branch 0xea000006 jumps to _reset at exactly 0x80000000).

**Evidence:** start.s:139-150 literals (0x81FB5000/0x81FC1800/0x81FDA800/0x81FE7000/0x82000000/0x81FF3800); start.s:45-49 final mode 0xd3 = SVC with sp = _stack_srv_end; arm-none-eabi-nm on dist/Debug/GNU_ARM-Linux/fnirsi_1013d_scope.elf: BSS_END = 0x8058a0e1; display_control.c:13 DISPLAY_CONFIG_ADDRESS 0x81BFFC00; variables.c:196/318 'FATFS fs; ... FIL viewfp;'.

**Suggested action:** Add as an AGENTS.md memory-map subsection (the table above plus the three rules: main lives on the 50 KB SVC stack; FIQ/SYS regions are dead weight usable as expansion/guard space; the stack addresses exist ONLY in start.s — the linker cannot detect a BSS/stack collision, so re-check nm output if BSS ever grows by tens of MB). No code change.

**Verifier:** Stack literals verified in start.s:139-150 and gap arithmetic recomputed: ABT/FIQ/SVC/SYS regions 0xC800 = 50 KB each, IRQ 0x19000 = 100 KB, SYS top exactly 0x82000000; final mode 0xd3 = SVC with sp=_stack_srv_end (45-49), SYS never entered, so main runs on the 50 KB SVC stack with the dead FIQ region directly below it. nm on the current ELF reproduces BSS_END=0x8058a0e1, channel1tracebuffer=0x805860e0, displaybuffer2=0x8040e4e0, maindisplaybuffer=0x8026bc1c — all below the stack region. DISPLAY_CONFIG_ADDRESS 0x81BFFC00 (display_control.c:13); FATFS fs/FIL viewfp/FILINFO globals (variables.c:196/318/320); FF_USE_LFN=1 (ffconf.h:116); uint16 buffer[256] at scope_functions.c:7513/7661; ld MEMORY org 0x7FFFFFE0 (fnirsi_1013d.ld:3) with the 0xea000006 header branch landing _reset at 0x80000000 (32-byte header verified in start.s:5-11).

### Alignment audit: BOOT_NOTES' STARTUP_CONFIG_ADDRESS quirk confirmed and sharpened (the word-store also stomps the touch-config byte at 0x1C); no other misaligned multi-byte casts in first-party code; 1014D build never even emits the access

- **Where:** `/home/jsantala/src/FNIRSI_1014D_Firmware/fnirsi_1013d_scope/variables.h:108`
- **Severity:** low — **CONFIRMED**

Verified ARM926 semantics for the deliberately unaligned '(uint32*)0x81BFFC1F' (variables.h:108): start.s never sets the CP15 A bit, so ARMv5 legacy behavior applies — no alignment fault ever. LDR from ...1F loads the aligned word at ...1C rotated right 24, putting byte 0x1F in the result's LSB, so menu.c:7082 'boot_menu_start = ptr[0] & 0x07' genuinely reads the intended byte (works by accident, as BOOT_NOTES.md already documents). STR to ...1F ignores addr[1:0] and stores the WHOLE WORD at 0x81BFFC1C — so the 1013D-chain writer statemachine.c:5686 'ptr[0] = boot_menu_start' puts the boot choice's LSB into byte 0x1C, which is TOUCH_CONFIG_ADDRESS (touchpanel.c:19, read as xswap when the config is valid, touchpanel.c:70/149), and zeroes bytes 0x1D-0x1F — including the 0x1F byte that BOTH readers (the rotated LDR above and Atlan4's fw0.04 loader, which reads it as uint8) actually consume. BOOT_NOTES.md line ~76 says 'read and write paths are asymmetric at machine level' — the sharper statement is: the write clobbers the touch-config word and the saved choice can never round-trip through byte 0x1F. 1013D-only path, no hardware here, doc-only per policy. On the 1014D build this is moot at machine level: objdump of the current ELF finds exactly one 0x81BFFCxx literal (display_control.c's 0x81BFFC00 validation) — neither the LDR nor the STR to ...1F/...1C is emitted (users are variant-guarded out). Hunt for OTHER misaligned casts came back clean: scope_functions.c:7613/7624/7670/7680 cast (uint32*)&buffer[8]/[40] on 'uint16 buffer[256]' — byte offsets 16/80, aligned provided the local array base is word-aligned (the type only guarantees 2-byte alignment; de-facto fine in this build since cal data round-trips on hardware, but worth a one-line comment if ever touched); diskio.c:177 '*(uint32*)buff' writes FatFs' aligned LBA_t pointer; usb_interface.c:264-293/297-326 FIFO copiers explicitly test 'buffer & 3' and drop to halfword/byte paths; all other uint32* casts target naturally aligned peripheral registers.

**Evidence:** variables.h:108; menu.c:7081-7082; statemachine.c:5641/5686; touchpanel.c:19 '#define TOUCH_CONFIG_ADDRESS (uint8 *)0x81BFFC1C' and :149 'xswap = ptr[0]'; start.s has no write to CP15 c1 except the V-bit clear (lines 52-54), so A stays 0; objdump -d fnirsi_1013d_scope.elf | grep -ci '81bffc00|81bffc1c|81bffc1f' == 1; scope_functions.c:7613 'ptr32 = (uint32*)&buffer[INPUT_CALIBRATION_SETTING_OFFSET]' with 'uint16 buffer[256]' at 7513.

**Suggested action:** Tighten the existing BOOT_NOTES.md quirk paragraph with the two verified specifics: (1) the store lands on the word at 0x1C and overwrites the touch X-swap config byte while zeroing 0x1F, so the 1013D boot choice cannot round-trip; (2) the 1014D binary contains no access to the unaligned address at all (confirmed by disassembly). Keep the existing prescription (redeclare uint8* if the byte contract is ever adopted). Optionally add an AGENTS.md gotcha: 'CP15 A bit is never set — misaligned word accesses silently rotate (loads) or drop low address bits (stores) instead of faulting'.

**Verifier:** All anchors verified: STARTUP_CONFIG_ADDRESS (uint32*)0x81BFFC1F (variables.h:107); reader menu.c:7081-7082 'ptr[0] & 0x07'; writer statemachine.c:5686 'ptr[0] = boot_menu_start' then sd_card_write of 0x81BFFC00; TOUCH_CONFIG_ADDRESS (uint8*)0x81BFFC1C (touchpanel.c:19) read as xswap/yswap at 149-150. ARMv5 semantics with A=0 (start.s only clears the V bit, never sets A): LDR from ...1F returns the ...1C word ROR 24, putting byte 0x1F in the LSB (read works); STR ignores addr[1:0] and stores the whole word at 0x1C, putting the choice's LSB in the touch-config xswap byte and zeroing 0x1D-0x1F — the sharpened 'never round-trips through byte 0x1F' statement is correct. BOOT_NOTES.md:77 carries the 'asymmetric' sentence. objdump of the 1014D ELF finds exactly 1 occurrence of 0x81BFFCxx literals (the 0x81BFFC00 validation) — the unaligned access is not emitted in this variant. scope_functions.c:7513/7613 cast claims and usb_interface.c alignment-checked copiers verified.

**Coverage notes:** Read in full: fnirsi_1013d_scope/start.s (151 lines), arm32.h (143), interrupt.c (69) + interrupt.h, fnirsi_1013d.ld (32), and main()'s init sequence in fnirsi_1013d_scope.c:47-120. All five infrastructure files diff-confirmed byte-identical to Atlan4-1.00o5/ (findings describe inherited code; no findings reported on vendor trees). Verified with greps that arm32_mmu_enable/ttb_set/domain_set/tlb_invalidate have zero call sites. Supporting evidence pulled from the built ELF (dist/Debug/GNU_ARM-Linux/fnirsi_1013d_scope.elf): nm for BSS_END=0x8058a0e1 and buffer/symbol placement, objdump literal-pool scan proving the 1014D binary contains no access to 0x81BFFC1C/1F. Audited DMA agents for the MMU-hazard map: SD path read (sd_card_interface.c:960-1035 — PIO confirmed), USB FIFO copiers (usb_interface.c:264-326 — alignment-checked PIO), DEBE scanout of maindisplaybuffer — the only DRAM DMA. Traced the IRQ registration set (timer0/USB/PortE-EINT) and the USB-mass-storage-in-IRQ call chain into mass_storage_class.c. Checked interrupt masking discipline (I/F bits through all mode words and arm32_interrupt_enable). Alignment hunt: pattern greps for (uint32*)&/(uint16*)&/(int16*)&/*(uint32*) across fnirsi_1013d_scope/*.c/h plus manual triage of every hit; STARTUP_CONFIG semantics cross-checked against BOOT_NOTES.md (found already-accurate; sharpened). CPU/AHB clocks from ccu_control.c. Caps/limits: performance figures are order-of-magnitude engineering estimates (no measurements possible; no automated tests, make not run per ground rules); F1C100s INTC pending-register write semantics (W1C vs write-value) not silicon-verified — finding states both cases and why current sources are safe either way; did not audit the deep display_lib/scope_functions render paths beyond sampling access widths; bootloader binaries and the loader-side FEL path out of scope; 1013D-chain items reported doc-only per policy. Worst-case stack depth assessed by inspection of large locals + FatFs object placement, not by static call-graph analysis.

## tracked repo-root loader trees

### Building fnirsi_1013d_startup_screen/ silently clobbers the active build's flashable SD image

- **Where:** `fnirsi_1013d_startup_screen/Makefile:70`
- **Severity:** medium — **CONFIRMED**

The tree's post-build step packages its loader-only image and then runs 'cp dist/Debug/GNU_ARM-Linux/fnirsi_1013d.bin ../fnirsi_1013d_scope/dist/Debug/GNU_ARM-Linux/fnirsi_1013d.bin' — the exact filename the active scope build produces as its bootable SD image (fnirsi_1013d_scope/Makefile:129-130). The copied image is the upstream 1013D loader chain only (SPL at image 0, splash at 0x4000/sector 48, 38472 bytes total) whose splash stage expects the scope program at sector 92 — which the current packaging never populates (1014D packs at 0x8000/sector 80, 1013D-Atlan4 at 0x5BC00). Companion step: fnirsi_1013d_sd_card_bootloader/Makefile:67 copies its SPL into ../fnirsi_1013d_startup_screen/dist/.../fnirsi_1013d.bin as the seed of that chain. Failure scenario: user builds the 1014D scope image, then (following the repo-root README's provenance text, which describes building these three projects as the normal flow) runs make in fnirsi_1013d_startup_screen/; the last artifact at the flash path is now a scope-less loader image; dd it to SD and the scope boots to the PECO splash then halts on the magenta/cyan eGON-check error screen (recoverable by re-dd, but it is exactly the 'wrong artifact left as last build' class CLAUDE.md warns about, arriving from an undocumented direction). Mitigating factor: the next scope 'make' fully re-seeds fnirsi_1013d.bin from the committed base blob, so the clobber only bites if these trees are built after the scope build and before flashing.

**Evidence:** fnirsi_1013d_startup_screen/Makefile:67-70: './flashfilepacker -o dist/Debug/GNU_ARM-Linux/fnirsi_1013d.bin -i dist/Debug/GNU_ARM-Linux/fnirsi_1013d_startup_screen.bin -l 0x4000' then '# Copy to the scope directory' / 'cp dist/Debug/GNU_ARM-Linux/fnirsi_1013d.bin ../fnirsi_1013d_scope/dist/Debug/GNU_ARM-Linux/fnirsi_1013d.bin'. fnirsi_1013d_sd_card_bootloader/Makefile:67: 'cp dist/Debug/GNU_ARM-Linux/fnirsi_1013d_sd_card_bootloader.bin ../fnirsi_1013d_startup_screen/dist/Debug/GNU_ARM-Linux/fnirsi_1013d.bin'. Splash stage expects scope at sector 92 (fnirsi_1013d_startup_screen/fnirsi_1013d_startup_screen.c:25 '#define PROGRAM_START_SECTOR 92') while current packaging overlays at 0x8000/0x5BC00 (fnirsi_1013d_scope/Makefile:126-130).

**Suggested action:** Doc-first (these are tracked upstream-heritage sources, better left byte-identical to upstream for provenance): add a warning line to CLAUDE.md's building section — 'never run make in the three sibling loader trees; fnirsi_1013d_startup_screen's post-build overwrites the scope dist's fnirsi_1013d.bin; if it happens, rebuild the intended scope variant last'. Only if the user prefers active protection, comment out the cp at fnirsi_1013d_startup_screen/Makefile:70 with a note that it diverges from upstream.

**Verifier:** Reproduced from the Makefiles: fnirsi_1013d_startup_screen/Makefile .build-post packs its loader at 0x4000 and then 'cp dist/Debug/GNU_ARM-Linux/fnirsi_1013d.bin ../fnirsi_1013d_scope/dist/Debug/GNU_ARM-Linux/fnirsi_1013d.bin' — the active build's flashable SD image name; fnirsi_1013d_sd_card_bootloader/Makefile seeds the chain into startup_screen's dist with the same name. PROGRAM_START_SECTOR 92 confirmed (fnirsi_1013d_startup_screen.c:25, sd_card_read at 94/131), while current packaging overlays the scope at 0x8000 (1014D) or 0x5BC00 (1013D) per fnirsi_1013d_scope/Makefile:120-131 — sector 92 is never populated with an eGON image (it falls mid-scope-blob on 1014D builds). The checked-in 38472-byte fnirsi_1013d.bin exists in startup_screen/dist. Mitigation accurate: the scope make re-seeds from the committed base blob each run.

### Three tracked loader/startup trees absent from CLAUDE.md and BOOT_NOTES.md (status now established: pristine upstream, provenance-only)

- **Where:** `CLAUDE.md:1`
- **Severity:** low — **CONFIRMED**

fnirsi_1013d_sd_card_bootloader/, fnirsi_1013d_startup_screen/, and fnirsi_1013d_startup_from_sd_card/ are tracked at repo root but appear in no working doc: CLAUDE.md's module map omits them, BOOT_NOTES.md's chain description never names them (grep over repo .md files finds only PORT_A.md:38 — historical — and the provenance-marked upstream README text). Verified status: all three are byte-identical to the vendored pecostm32 1013D upstream (recursive diff clean, including dist/ binaries and nbproject/), every commit is pecostm32's own (2022-01-15..2024-01-07: a68e48c/43b0b64/0b74b68, e61ae88/8aaaafb, 07e993b), working tree clean, zero local modifications or orphaned fixes. They implement pecostm32's ORIGINAL 1013D boot chain (SPL sector 16 -> splash sector 48 -> scope sector 92), which predates and is incompatible with both shipped chains. None of their four dist binaries (14848/16384/22016/38472 bytes) matches or is embedded in either committed loader blob (bootloader_base.bin 375808 = 0x5BC00 exactly, its own multi-stage eGON layout; bootloader_1014d_base.bin 27136, md5 a281ab709ab305694b526af08cb3d41d = exact match to the vendored FNIRSI_1014D_Firmware/fnirsi_1014d_startup dist binary, confirming BOOT_NOTES.md:3-6). Nothing in the active build references them (fnirsi_1013d_scope/Makefile seeds solely from the committed base blob, lines 126-130).

**Evidence:** diff -rq fnirsi_1013d_{sd_card_bootloader,startup_from_sd_card,startup_screen} FNIRSI_1013D_Firmware/<same> -> empty; git log shows only pecostm32 commits; md5 scan of all dist binaries vs both committed blobs -> no match, no embedded 512-byte prefix; grep 'startup_screen|startup_from_sd|sd_card_bootloader' over repo .md files -> only README.md:34-39, PORT_A.md:38, REVIEW-2026-08-21.md:1130.

**Suggested action:** Add the three one-liner verdicts (see proposal) to CLAUDE.md's repository-layout section and a one-sentence cross-reference in BOOT_NOTES.md ('the tracked repo-root loader trees are the upstream 1013D chain (16/48/92), provenance-only — neither shipped loader binary is built from them'). Optionally strike the item from REVIEW-2026-08-21.md's open list.

**Verifier:** Reproduced: diff -rq of all three trees vs FNIRSI_1013D_Firmware upstream is empty; git log for the trees shows only pecostm32 commits (a68e48c/43b0b64/0b74b68/e61ae88/8aaaafb/07e993b, 2022-01-15..2024-01-07). dist sizes 14848/16384/22016/38472 confirmed. bootloader_base.bin is exactly 375808 = 0x5BC00; bootloader_1014d_base.bin 27136 with md5 a281ab709ab305694b526af08cb3d41d matching the vendored fnirsi_1014d_startup dist rebuild. Containment scan (whole-file and 512-byte prefix) of all four dist binaries against both committed blobs: no matches. Doc grep: the trees appear only in README.md:34-39 (upstream provenance text), PORT_A.md:38 (historical), REVIEW-2026-08-21.md:1130 — the extra PORT_AUDIT.md grep hits are the unrelated vendor filename 'fnirsi_sd_card_bootloader.bin', not these trees; CLAUDE.md/BOOT_NOTES.md never name them.

### fnirsi_1013d_startup_from_sd_card/ is doubly stale: upstream itself flagged it unmaintained

- **Where:** `fnirsi_1013d_startup_from_sd_card/fnirsi_1013d_startup_from_sd_card.c:13`
- **Severity:** low — **CONFIRMED**

The alternative single-stage loader (no splash; loads the scope directly from sector 48 to 0x80000000) was already declared stale by pecostm32 in the upstream README ('this project has not been adapted for the new display configuration setup nor has it been tested with the latest code' — repo README.md:42): it never loads sector 710, so a scope program relying on the DRAM display-config staging block would run on fallback timing only. Its build is self-contained (objcopy + mksunxi only, Makefile:61-62 — no cross-tree copies), so unlike the startup_screen tree it is harmless to build. Recording this prevents anyone from picking it as a 'simpler loader' base without knowing upstream abandoned it.

**Evidence:** fnirsi_1013d_startup_from_sd_card.c:13 '#define PROGRAM_START_SECTOR 48', loads to 0x80000000 (lines 79-96), no DISPLAY_CONFIG_SECTOR read anywhere in the tree; README.md:42 (upstream provenance text) flags it unadapted/untested; Makefile:55-62 shows no post-build copy steps.

**Suggested action:** Cover via the CLAUDE.md one-liner (proposal); no code change.

**Verifier:** Verified: repo README.md:42 carries the upstream caveat verbatim ('this project has not been adapted for the new display configuration setup nor has it been tested with the latest code'); fnirsi_1013d_startup_from_sd_card.c:13 PROGRAM_START_SECTOR 48, loads header+payload to 0x80000000/0x800001E0 (lines 59-96); grep finds no DISPLAY_CONFIG/sector-710 reference anywhere in the tree; its Makefile .build-post is objcopy + mksunxi only, with no cross-tree copy steps — harmless to build, unlike startup_screen.

### Design proposal (loader-trees)

```
One-line status verdicts for CLAUDE.md (repository-layout section), verified 2026-08-21:

- `fnirsi_1013d_sd_card_bootloader/` — pecostm32-upstream 1013D stage-1 SPL (BROM sector 16; loads the splash program from sector 48 to 0x81C00000 plus sector-710 display config to 0x81BFFC00). Byte-identical to upstream, provenance-only — not the shipped loader, unreferenced by the active build; its `make` copies its output into the startup_screen tree's dist.
- `fnirsi_1013d_startup_screen/` — pecostm32-upstream stage-2 "PECOs sCOPE" splash + scope loader (loads the scope from sector 92). Byte-identical to upstream, provenance-only — **do not run `make` here: its post-build overwrites `fnirsi_1013d_scope/dist/Debug/GNU_ARM-Linux/fnirsi_1013d.bin` with a scope-less loader image** (rebuild the intended scope variant last if it happens).
- `fnirsi_1013d_startup_from_sd_card/` — pecostm32-upstream alternative single-stage SPL (no splash; scope direct from sector 48), flagged unmaintained by upstream's own README (never adapted to the display-config setup). Byte-identical to upstream, provenance-only, self-contained build (no cross-tree writes).

Shared note: none of the three builds either committed loader binary — `bootloader_base.bin` (Atlan4 v0.8, binary-only, 0x5BC00 layout) and `bootloader_1014d_base.bin` (md5-matched rebuild of the vendored `FNIRSI_1014D_Firmware/fnirsi_1014d_startup`) contain nothing from these trees, and their sector map (48/92) is incompatible with both shipped chains (sector 80 / 0x5BC00).
```

**Coverage notes:** Checked: full recursive diff of all three trees against the vendored pecostm32 1013D upstream (byte-identical, including dist/ binaries and nbproject/ metadata); git history and authorship of every commit touching them (all pecostm32, 2022-2024, present on both PORT_A and atlan4-base, working tree clean); read all three main .c files in full, all three Makefiles' post-build steps, linker-script origins; hashed and size-compared all four tree dist binaries against both committed loader blobs plus a byte-substring scan (whole-file and 512-byte-prefix) of both blobs — no matches; eGON header layout of both blobs; verified BOOT_NOTES.md's bootloader_1014d_base.bin provenance claim by exact md5 match to the vendored fnirsi_1014d_startup dist binary; confirmed the active scope Makefile (lines 121-130) references only the committed base blobs; cross-checked the trees' sector map (16/48/92, upstream scope overlay -l 0x9800 = sector 92) against BOOT_NOTES.md's shipped chains (sector 80 / -l 0x8000 for 1014D; 0x5BC00 for 1013D-Atlan4); checked repo-root README provenance (fork-status banner added 204201d marks the upstream build instructions as provenance-only). Skipped: line-by-line review of the ~40 shared driver files per tree (byte-identical to the read-only reference, so per ground rules nothing to report on their content); no builds run (policy); no analysis of bootloader_base.bin internals beyond confirming none of the tree outputs are embedded (its source lives in the untracked 'Bootloader fw0.02...' snapshots, outside this task). Per policy, no findings are reported on the vendored reference trees themselves.

## mechanical claim closures

### acqprobe_analyze.py never received the review's fill-geometry units fix; FPGA_NOTES implies it did

- **Where:** `tools/acqprobe_analyze.py:330`
- **Severity:** medium — **CONFIRMED**

REVIEW-2026-08-21.md's capture-geometry finding (its lines 321-327) corrected the ring-address-vs-interleaved-sample unit mixing and its suggested action explicitly named analyzer lines 196/325/326. aea6f8a applied the correction to FPGA_NOTES.md and ROADMAP.md, and f0561ee touched the analyzer - but only to add the CH1-enable divisor comment (lines 210-214); the four wrong prints remain and I reproduced them live against bench/acqprobe/2026-07-19-trigger-probe/: (1) line 196 '4096-sample ring spans %s per ADC' % si_time(4096 / rate) uses the combined interleaved rate - prints '204.8 ms per ADC' at 5 ms/div where the true per-ADC span is 409.6 ms (addresses tick at rate/2); (2) line 330 window end '(disp_start + info.get("samplecount", 3000)) % n' adds 3000 interleaved samples as ring addresses - prints 'display window ring 161..3161 (raw-750)' where the corrected window is 161..1661 (= raw-750..raw+750, samplecount//2 addresses); (3) line 331 'trigger at ~%d%% width' computes int(100*750/samplecount) = 25% - the corrected figure is 50% (750 addresses = 1500 of 3000 interleaved samples, matching the firmware's own settriggerpoint == samplecount/2 '50%' label); (4) lines 334-336 'the gap sits past the display's right edge' - with the corrected window end (ring 1661) the gap starts ~950-1750 addresses past it with fresh off-window data in between (5ms run: gap start 2616 = 955 past). Meanwhile FPGA_NOTES.md:137-138 says 'the analyzer's capture fill geometry section now reports them: (units corrected 2026-08-21 ...)' immediately followed by the corrected bullets - a reader will assume the analyzer's output matches the corrected units. The next bench run's output will contradict the doc it is supposed to feed.

**Evidence:** Live run (python3 tools/acqprobe_analyze.py bench/acqprobe/2026-07-19-trigger-probe): 'settings: 5ms/div (idx 16), 20 KSa/s (idx 12); 4096-sample ring spans 204.8 ms per ADC' / 'raw 0x14 trigger at ring 911 ; display window ring 161..3161 (raw-750)' / '-> pre-trigger fresh = 910 samples (750 on-screen, trigger at ~25% width)' / 'the 1481-sample gap sits past the display's right edge'. git show f0561ee -- tools/acqprobe_analyze.py: only the CH1-gating comment hunk. FPGA_NOTES.md:137-146 carries the corrected numbers (window 161..1661, 'mid-window, 50 %').

**Suggested action:** Apply the review's analyzer fix: line 196 use 4096/(rate/2); line 330 use info.get('samplecount',3000)//2 for the window end; line 331 report 50% (750 addresses = samplecount/2 interleaved pre-trigger samples); lines 334-336 say the gap starts past the readout window's right edge with fresh off-window data between (report the distance: (gstart - (disp_start + samplecount//2)) % 4096). Until then, add '(analyzer prints still pre-correction)' to FPGA_NOTES.md:137.

**Verifier:** Reproduced end-to-end. Live run of tools/acqprobe_analyze.py on bench/acqprobe/2026-07-19-trigger-probe prints exactly the four uncorrected outputs: '4096-sample ring spans 204.8 ms per ADC' (line ~196 uses combined rate; true per-ADC span 409.6 ms), 'display window ring 161..3161 (raw-750)' (line ~330 adds samplecount=3000 as ring addresses; corrected end 1661), 'trigger at ~25% width' (line ~331 computes 100*750/3000; corrected 50%), and 'the 1481-sample gap sits past the display's right edge'. git show f0561ee -- tools/acqprobe_analyze.py contains only the CH1-enable-divisor comment hunk (added before the nch computation). REVIEW-2026-08-21.md's finding explicitly named analyzer lines 196/325/326 in its suggested action. FPGA_NOTES.md:137-139 says 'the analyzer's capture fill geometry section now reports them: (units corrected 2026-08-21 ...)' followed by the corrected bullets (window 161..1661, mid-window 50%) — so the doc does imply the analyzer matches the corrected units while it does not.

### CLOSED/CONFIRMED: v0.8 loader FPGA-version wait is dead code (BOOT_NOTES claim re-derived independently)

- **Where:** `BOOT_NOTES.md:122`
- **Severity:** low — **CONFIRMED**

Re-derived the 2026-07-10 claim from scratch with arm-none-eabi-objdump -D -b binary -marm on fnirsi_1013d_scope/bootloader_base.bin (375808 B, md5 cc594b02e3d08609906b5cb3163e5d38). The image contains exactly two 0x00001432 literal-pool words (file offsets 0x13a0 and 0x126a0), anchoring two version-wait routines at exactly the claimed offsets: (a) 0x1340 (SPL region, unoptimized): sends cmd 0x06 via bl 0x1320, compares the readback against the 0x1432 literal, runs a counted-to-1000 delay loop, and retries forever (bne 0x1350); (b) 0x125dc (main stage, optimized/inlined): orr r3,#6 command write, reads two bytes, cmp r3, ip (ip = 0x1432 from 0x126a0), popeq on match, else 1000-count delay (subs/bne 0x12688) and b 0x125f0 forever. Caller search over the whole image: no branch of any kind (bl/b/blx/conditional) targets 0x1340, 0x1320, or any address in 0x125dc-0x12694 from outside the routines themselves; no fall-through is possible (0x133c is pop {fp,pc}; 0x125d0 is bx lr followed by literal words); a scan of every aligned 32-bit word for pointer-like references to either routine under plausible load bases (0x00xxxxx/0x80xxxxx/0x81xxxxx) found none (the only textual hits are font bitmap data). The adjacent routine at 0x126a4 is confirmed as the cmd-0x04 enable (orr r2,#4 after the bic-0xFF command pattern) and has exactly one caller (bl 0x126a4 at 0x29e7c) - matching BOOT_NOTES' 'branches only to adjacent FPGA functions' example. Residual caveat: a flat -marm disassembly cannot expose hypothetical Thumb-mode callers, but the image is uniform ARM GCC output and the pointer-literal scan came back empty, so the risk is negligible. BOOT_NOTES.md lines 117-128 stand as written, including the exact offsets.

**Evidence:** bl_base.dis (scratchpad): '1340: e92d4800 push {fp, lr}' ... '1370: e3530ffa cmp r3, #1000' ... '1384: e59f3014 ldr r3, [pc, #20] @ 0x13a0' / '13a0: 00001432'; '125dc: e92d4010 push {r4, lr}' ... '12608: e3833006 orr r3, #6' ... '1267c: e153000c cmp r3, ip' / '126a0: 00001432' ... '12694: eaffffd5 b 0x125f0'. grep for branches to 0x1340 and range 0x125dc-0x12694: only internal 'bne 0x12688' and 'b 0x125f0'. Single caller of adjacent 0x126a4: '29e7c: ebffa208 bl 0x126a4'.

**Suggested action:** No doc change required - optionally append a dated line to BOOT_NOTES' v0.8 section: 're-verified 2026-08-21: full re-disassembly reproduced both routines at 0x1340/0x125dc, zero call sites, no pointer references; CONFIRMED'.

**Verifier:** Fully re-derived from scratch: md5 cc594b02e3d08609906b5cb3163e5d38, exactly two aligned 0x00001432 words at file 0x13a0/0x126a0; routine (a) at 0x1340 (push {fp,lr}, bl 0x1320 command send, cmp #1000 delay, literal 0x13a0) preceded by pop {fp,pc} at 0x133c; routine (b) at 0x125dc (push {r4,lr}, orr #6 at 0x12608, cmp r3,ip at 0x1267c, bne 0x12688 delay, b 0x125f0 forever) preceded by bx lr + literal pool at 0x125d0-0x125d8. Branch scan over the whole image: no external branch targets 0x1340/0x1320 or 0x125dc-0x12694 (only internal bne/b and the SPL-internal bl 0x1320). Adjacent 0x126a4 confirmed as the cmd-0x04 enable (bic 0xFF / orr #4 at 0x126c0-0x126c4) with exactly one caller, bl 0x126a4 at 0x29e7c. Pointer-literal claim required care: a naive flat-base scan surfaces 0x8001246c-0x80012690 words at 0x1a47c-0x1a4d0, but these are ldrls-pc switch jump tables, and the image is multi-stage (eGON.BT0 SPL size 0x7a00 at file 0, eGON.EXE main stage at file 0x8000 loading at 0x7FFFFFE0) — under the true mapping (runtime = file + 0x7FF F7FE0) those entries resolve to the switch's own case bodies at file 0x1a48c-0x1a6b0, and a scan for the wait routine's real runtime range 0x8000A5BC-0x8000A674 finds zero references; SPL base-0 scan finds only the constant 0x1387 (=4999) in a literal pool. Dead-code conclusion and BOOT_NOTES.md:111-128 stand.

### CLOSED/CONFIRMED: bootloader_1014d_base.bin = byte-identical local rebuild; differs from pecostm32's shipped bits (as PORT_AUDIT already says)

- **Where:** `PORT_AUDIT.md:84`
- **Severity:** low — **CONFIRMED**

The committed fnirsi_1013d_scope/bootloader_1014d_base.bin (27136 B, md5 a281ab709ab305694b526af08cb3d41d) is byte-identical (cmp clean) to the vendor working tree's locally rebuilt FNIRSI_1014D_Firmware/fnirsi_1014d_startup/dist/Debug/GNU_ARM-Linux/fnirsi_sd_card_bootloader.bin (same md5; timestamps Jul 8 17:38/17:39 confirm the rebuild-then-commit sequence). It is NOT bit-identical to pecostm32's originally committed dist binary: extracting it from the vendor repo's git HEAD (b9ea2ae, 'git show HEAD:...fnirsi_sd_card_bootloader.bin', md5 5c3aea25fe3bc447fbcb615f65703cee, same 27136 B) shows 14335/27136 bytes differ, first at byte 13 (the eGON header checksum) - consistent with a different-toolchain rebuild of the same source, exactly as PORT_AUDIT.md:84-88 states ('md5-identical to the locally rebuilt ... differs from the GitHub-committed binary only because ours is a local GCC-14 rebuild'). So the 'hash-verified local rebuild' shorthand in CLAUDE.md:42 and BOOT_NOTES.md:5 is accurate when read as 'the committed file is hash-verified to be the local rebuild', though it could be misread as 'the rebuild hash-matches pecostm32's shipped binary' (it does not, and cannot with a different GCC). Bonus adjacent re-check: this loader's fpga_check_ready IS live - the wait routine at file offset 0x1330 (0x1432 literal at 0x1390) has exactly one caller, bl 0x1330 at 0x4bf4, and the vendored source (fnirsi_1014d_startup/bl_fpga_control.c:97-102 'while(fpga_get_version() != 0x1432)') matches - confirming BOOT_NOTES.md:129 ('our 1014D chain is NOT covered ... has the live wait').

**Evidence:** md5sum: a281ab709ab305694b526af08cb3d41d for both the committed file and the vendor dist rebuild; cmp exit 0. git show HEAD binary: md5 5c3aea25fe3bc447fbcb615f65703cee, 'differ: byte 13', cmp -l count 14335. bl_1014d.dis: wait routine at 0x1330-0x138c with '1374: ldr r3, [pc, #20] @ 0x1390' / '1390: 00001432'; caller '4bf4: ebfff1cd bl 0x1330'.

**Suggested action:** No correction needed - PORT_AUDIT.md:84-88 is precisely right. Optionally sharpen CLAUDE.md:42/BOOT_NOTES.md:5 to 'hash-verified local rebuild (byte-identical to our rebuild of his source; NOT bit-identical to his shipped binary - different toolchain)' to preempt the misreading.

**Verifier:** Every number reproduced: committed bootloader_1014d_base.bin md5 a281ab709ab305694b526af08cb3d41d = vendor working-tree dist rebuild (cmp clean, mtimes Jul 8 17:38/17:39); 'git show b9ea2ae:...fnirsi_sd_card_bootloader.bin' from the vendor repo HEAD gives md5 5c3aea25fe3bc447fbcb615f65703cee, same 27136 B, first difference at byte 13 (eGON checksum region), cmp -l count exactly 14335 — consistent with a different-toolchain rebuild, matching PORT_AUDIT.md:84-88 verbatim. Live-wait bonus check reproduced: disassembly shows the wait routine at 0x1330 with the 0x1432 literal at 0x1390 and exactly one caller 'bl 0x1330' at 0x4bf4; vendored source fnirsi_1014d_startup/bl_fpga_control.c:97-102 'while(fpga_get_version() != 0x1432)' matches, confirming BOOT_NOTES.md:129's 'our 1014D chain is NOT covered'.

### PORT_AUDIT section 1 import-fidelity figures are stale for 4 of 6 named files (legitimate post-audit divergence); needs a dated caveat

- **Where:** `PORT_AUDIT.md:37`
- **Severity:** low — **CONFIRMED**

Scripted re-diff of every file section 1 names with a byte-fidelity claim, against its named P14 origin (FNIRSI_1014D_Firmware/fnirsi_1014d_scope/). Still accurate: uart.c ('byte-identical modulo blank lines') - 0 non-blank diff lines today (2 raw, both blank-line/whitespace); uart.h ('P14 + an added GD_KEY_* table') - the 58-line delta is entirely the GD_KEY table, its documentation comments, and one trailing-whitespace difference on the uart1_wait_for_user_input declaration; bootloader provenance (separate finding, holds). Since diverged, all via documented feature/fix commits: clock_synthesizer.c (claimed 'byte-identical modulo blank lines', now 51 non-blank diff lines - the 2026-07-10 manual sampling-clock API, commit 77af34b), clock_synthesizer.h (12 lines, same commit), menu_1014d.c (claimed 29 diff lines vs user_interface_functions.c, now 533 - commits 77af34b, 3446773, ee3fc97, 87cf94b, d2fa75c, 1bbfce9, 100f9ff, f0561ee), sm_1014d.c (claimed 50 vs statemachine.c, now 490 - same commit set incl. today's f0561ee, expected). The audit header does pin the audit to commit 4a5fa61 / 2026-07-09, but section 1 itself carries no staleness marker, and the section 6 reproduction appendix invites re-running exactly these diffs - which today produce numbers an order of magnitude off the section-1 claims, risking a 'fidelity regressed?' false alarm in a fresh session.

**Evidence:** diff counts (non-blank) run 2026-08-21: clock_synthesizer.c 51, clock_synthesizer.h 12, uart.c 0, uart.h 58 (all GD_KEY/comments/whitespace), menu_1014d.c 533, sm_1014d.c 490. git log 4a5fa61..HEAD per file lists only the documented port commits; uart.c has none. PORT_AUDIT.md:37-41 ('clock_synthesizer.c/.h and uart.c are byte-identical to P14 modulo blank lines ... menu_1014d.c ... 29 diff lines ... sm_1014d.c ... 50 diff lines').

**Suggested action:** Add one dated line at the top of section 1, e.g.: '(Fidelity figures are the 4a5fa61/2026-07-09 snapshot. By 2026-08-21 deliberate 1014D work has grown the deltas - clock_synthesizer.c/.h ~51/12 non-blank lines, menu_1014d.c ~533, sm_1014d.c ~490, all from the section-5 fix passes; uart.c/.h still match as described.)'

**Verifier:** Re-ran every diff with non-blank counting and reproduced the figures exactly: clock_synthesizer.c 51, clock_synthesizer.h 12, uart.c 0 non-blank (2 raw), uart.h 58, menu_1014d.c vs user_interface_functions.c 533, sm_1014d.c vs statemachine.c 490 — an order of magnitude off PORT_AUDIT.md:37-41's 'byte-identical modulo blank lines ... 29 diff lines ... 50 diff lines'. The audit header pins commit 4a5fa61/2026-07-09 (lines 3-4) but section 1 carries no staleness marker, and section 6 'Reproduction appendix' (line 743) invites re-running exactly these two diffs (lines shown). git log 4a5fa61..HEAD confirms uart.c untouched and the diverged files changed only via the documented port commits (incl. 77af34b and f0561ee).

### seam_scan reports step positions 16-32 samples low (left-bin center instead of the bin boundary)

- **Where:** `tools/acqprobe_analyze.py:139`
- **Severity:** low — **CONFIRMED**

seam_scan bins the block (binsize=32) and ranks steps between consecutive bin means, but reports position 'i * binsize + binsize // 2' - the center of the LEFT bin - for a step that physically lies at or after the boundary (i+1)*binsize. Empirically (synthetic data): a hard step exactly at sample 1024 (a bin boundary) is reported '@1008' (16 low); a step at sample 1000 (mid-bin) reports its largest candidate '@976' (24 low). The section header ('position is samples from the raw trigger address') overstates the precision, and anyone comparing reported seam positions against window edges (750, 1661) inherits the bias. Two smaller notes: bin 0 includes the known-stale sample 0 (can synthesize a small false step at the first boundary - '@16' candidates appear in flat bench blocks), and with fully flat data the median step is 0 so ratios print 'inf x median' (guarded, cosmetic only).

**Evidence:** seam_scan([0]*1024 + [100]*3584) -> [(1008, 100.0, inf), ...]; seam_scan([0]*1000 + [100]*3608) -> [(976, 75.0, inf), (1008, 25.0, inf), ...]. Code: 'return [(i * binsize + binsize // 2, steps[i], ...)' at tools/acqprobe_analyze.py:139.

**Suggested action:** Report the boundary '(i + 1) * binsize' (optionally +/- binsize/2 as the stated uncertainty), start binning at sample 1 to skip the stale byte, and note the +/-1-bin resolution in the printed header.

**Verifier:** Code verified at tools/acqprobe_analyze.py:132-140: seam_scan reports '(i * binsize + binsize // 2, ...)' — the center of the left bin, while the step lies at/after boundary (i+1)*binsize. Synthetic tests reproduce the exact claimed values: seam_scan([0]*1024+[100]*3584) -> top candidate (1008, 100.0, inf) i.e. 16 low for a boundary step at 1024; seam_scan([0]*1000+[100]*3608) -> (976, 75.0, inf) then (1008, 25.0, inf), 24 low for a mid-bin step at 1000. The 'inf x median' flat-data behavior visible in the same outputs; bin 0 includes ring sample 0 (build_ring's stale slot); the section header 'position is samples from the raw trigger address' confirmed at line 291.

### CLOSED: analyzer parsing, tables, and mod-4096 ring math verified correct against the probe writer and all six bench runs (one +/-1 anchor caveat)

- **Where:** `tools/acqprobe_analyze.py:143`
- **Severity:** low — **CONFIRMED**

Positive closure of the rest of the task-4 review. (a) parse_txt matches the writer scope_functions.c:2046-2075 exactly: 'label=value\r\n' lines (acqprobe_line), repeated 'raw14=' collected into a list, 'ringdump_raw14=' as scalar; \r stripped by read_text/splitlines; all six bench acqprobe_*.txt files parse with every field consumed. (b) parse_bin matches the writer's ringdump block at scope_functions.c:2079-2103: 'ACQP' + six LE uint32 (version/timeperdiv/samplerate/rawdump/blocks=5/samples=4608) + 5 command bytes + 5x4608 data - offsets 4/28/33 in the parser line up, and all six bench ringdump_*.bin files load (23073 B, extra=0). (c) Tables: TIME_DIV_TEXTS matches variables.c:518 time_div_texts[35] entry-for-entry (including the duplicated 200ms-20ms long/short seam); SAMPLE_RATES' 18-entry index space matches sample_rate_time_per_div[18] (idx 0 = 200 MSa/s ... 17 = 500 Sa/s), cross-checked on bench data (tdiv 16 -> '5ms', idx 12 -> 20 KSa/s, and time_per_div_sample_rate[16] == 12 in variables.c:421); the p1b clock formula 800/(2*p1b+4) MHz matches clock_synthesizer.c's documented divider math ((P1+512)/128 = 2*p1b+4; 6->50, 5->57.1, 4->66.7, 3->80 MHz). (d) Ring math: build_ring starts at k=1 (skips the documented stale byte), covers all 4096 addresses from a 4608-sample dump (ring[rawdump] filled at k=4096; the 128 fallback is unreachable), and correctly mods raw 0x14 values >= 4096 (bench rawdump 5007 -> ring 911; raw14 lines up to 5086 handled); wrap_test/diff_count windows are exact (overlap len(b)-w); the W=4096 sample-0-only check is sound; longest_flat_run's doubled-array sliding window is a correct O(n) circular scan with the run<=n cap, and its outputs reproduce FPGA_NOTES' closed numbers (gap 2616..1 len 1481, pre 910, post 1705 on the 5ms run). One caveat to carry: build_ring assumes dump byte k sits at ring (raw+k) mod 4096, i.e. the stale byte occupied the ring[raw] slot; if the FPGA pipeline instead delays the whole stream by one clock, every absolute ring figure (trigger 911, fresh start ~1, window 161..1661) shifts down by exactly 1. Both models fit the observed b[k]==b[k+4096] wrap, and the standard firmware read path (fpga_read_adc_data, fpga_control.c:1078 - no first-byte discard) does not disambiguate. All relative counts (910 pre / 2507-1705 post / gap lengths) are unaffected.

**Evidence:** Writer format at scope_functions.c:2040-2103 (acqprobe_line loop, header[0..5], acqprobe_adc_commands, acqprobe_ring) vs parse_txt/parse_bin (analyzer lines 57-90). Bench cross-checks: acqprobe_tt5ms.txt 'timeperdiv=16 / samplerate_idx=12 / clock_p1b=6'; analyzer output '5ms/div (idx 16), 20 KSa/s (idx 12) ... 50.0 MHz (p1b=6)'; variables.c:421-434 time_per_div_sample_rate[16]=12; variables.c:518-533 time_div_texts; clock_synthesizer.c:110-118 divider comment. Ring: run output 'raw 0x14 trigger at ring 911', 'unwritten gap = ring[2616..1] length 1481', 'blocks 0 and 4 ... byte-identical over 4608 samples', 'W=4096 mismatches sit at sample 0 only'.

**Suggested action:** No code change needed for this part. Optionally add a one-line comment to build_ring recording the +/-1 absolute-anchor assumption (stale byte = ring[raw] slot) so future absolute-position claims (e.g. 'writer starts at ring ~0') carry the uncertainty.

**Verifier:** Positive closure spot-checked broadly and everything held: (a) writer scope_functions.c:2048-2075 emits 'label=value' lines incl. repeated raw14= and scalar ringdump_raw14=, matching parse_txt; (b) ringdump writer (2079-2100) = 'ACQP' + six LE uint32 + 5 command bytes + 5*ACQPROBE_RING_SAMPLES(=4608, scope_functions.c:1872) = 23073 B, matching parse_bin's offsets 4/28/33 and the bench files; (c) TIME_DIV_TEXTS matches variables.c time_div_texts[35] entry-for-entry incl. the duplicated 200ms-20ms seam; SAMPLE_RATES[12]=20e3 and time_per_div_sample_rate[16]=12 (variables.c:421-434) match the bench 5ms run; p1b formula 800/(2*p1b+4) matches clock_synthesizer.c:110-116's divider comment (6->50, 5->57.1, 4->66.7, 3->80); (d) build_ring starts k=1, covers all 4096 addresses from 4608 samples (128 fallback unreachable), mods rawdump 5007 -> ring 911; live analyzer run reproduces the closed numbers (blocks 0/4 byte-identical, W=4096 mismatches at sample 0 only, pre 910 / post 1705 / gap 1481 on the noisy 5ms run) and fpga_read_adc_data (fpga_control.c:1060-1090) indeed has no first-byte discard, so the +/-1 anchor caveat is fairly stated.

**Coverage notes:** All four assigned closures completed with hands-on verification; no caps hit. Task 1: full flat disassembly of bootloader_base.bin (65852 lines), literal-pool search for 0x1432 (exactly 2 hits), routine-body reads at 0x1340/0x125dc, exhaustive branch-target greps over the whole image for both routines and their helper (0x1320), fall-through inspection, and an aligned-word pointer scan under plausible load bases - claim CONFIRMED; residual limitation: -marm disassembly cannot see hypothetical Thumb callers (judged negligible: uniform ARM GCC code, pointer scan negative). Task 2: md5/cmp of the committed 1014D loader vs the vendor working-tree rebuild (identical) and vs pecostm32's git-HEAD-committed binary via git show (14335/27136 bytes differ); also re-verified the live fpga_check_ready call site (bl 0x1330 at 0x4bf4) and its vendored source. A rebuild was not attempted (out of scope per task; would need his exact toolchain to be meaningful). Task 3: scripted raw and non-blank diffs of all six section-1 import-fidelity files vs their named P14 origins, plus git log attribution of every divergence to documented commits; the seam-integrity/shared-core bullets of section 1 were NOT re-diffed (they claim guardedness, not byte-identity - outside the task's 'byte-diff list'). Task 4: line-by-line review of tools/acqprobe_analyze.py (383 lines) against the writer scope_do_acquisition_probe/fpga_dump_ring, live runs on both bench run-dirs (all 6 txt+bin pairs), firmware-table cross-checks (time_div_texts, time_per_div_sample_rate, sample_rate_time_per_div, p1b divider math), synthetic-input tests of seam_scan, and reproduction of the still-uncorrected units prints; confirmed f0561ee's analyzer hunk is the CH1-gating note only. Not run: make (per policy), the on-scope probe (no hardware). The fw1 '+3345 vs -750' branch asymmetry (implies a mod-4095 wrap while reads behave mod-4096) was noted but not pursued - FPGA_NOTES already flags it as 'a wrap at 4095+1' and it is a firmware/reference question, not an analyzer defect. Vendored reference trees were used read-only for comparison; no findings reported on them.

